---
id: T-058
title: The seeded-defect generator reports edits that never matched
type: fix
status: done
phase: review
parent: null
blocked_by: []
related: [T-016, T-005]
work_package: PH2
shipped_in: 0.1.5
owner: the project owner
business_value: medium
effort: s
created: 2026-08-09
updated: 2026-08-12
deliverables:
  - tools/examples/seed_defects.py
  - examples/reference-deck-seeded-defects.html
  - docs/PUBLISHING.md
---

# T-058 — The seeded-defect generator reports edits that never matched

## 1. Specify

**Outcome**
[`tools/examples/seed_defects.py`](../tools/examples/seed_defects.py) cannot report a seed it did
not apply. Every edit asserts it matched, the way
[`static_variants.py`](../tools/deck/static_variants.py)'s `build` already does, and the S2
disclosure removal either matches or the run stops.

**Why this one**
The generator builds the blindness fixture — the deck used to prove that five of the ten evaluation
dimensions are invisible to any mechanical check (**L-05**, DS-190, DS-191). **A fixture that
over-reports its own coverage is the same defect one level up:** the claim *this deck carries a
seeded defect on every dimension* is what the fixture is for, and one seed has been counted without
being applied.

**What was found, 2026-08-09 by [T-016](T-016-the-interaction-and-motion-layer.md).** The S2 seed
*slide 7 assumption marker removed* is a `re.sub` whose lookahead expects the disclosure to sit
immediately before `<p class="provenance">`. The bottom line is between them, so it has never
matched — and the `applied.append(("S2", ...))` on the next line runs unconditionally, so the run
prints the seed as applied. The regenerated fixture is byte-identical to the committed one in that
region, which is how it was noticed at all: **T-016 widened the pattern to admit `data-disc="…"`
and the output did not change.**

**Scope**
- In: every seed in the file asserting that it matched, and failing the run when one does not.
- In: the S2 disclosure removal — repointed to what it means to remove, or dropped with a reason.
- Out: the *choice* of seeded defects and whether each is representative. That is
  [T-005](T-005-build-check-the-gate-the-deck-must-pass.md)'s and the rubric's.
- Out: the other three variant suites. `build` already asserts its anchors; this file is the one
  that does not.

**Inputs**
- [`tools/examples/seed_defects.py`](../tools/examples/seed_defects.py) — `sub()` asserts, the
  bare `re.sub` calls beside it do not.
- [`tools/deck/static_variants.py`](../tools/deck/static_variants.py) — `build`, which is the
  shape to copy: an anchor that does not match exits.
- [`docs/LESSONS.md`](../docs/LESSONS.md) — **L-04**, **L-36**.

**Acceptance criteria**
- [ ] Every seed asserts it matched; a seed that does not stops the run rather than reporting itself
- [ ] The S2 disclosure removal applies, or is removed with the reason recorded
- [ ] The dimension count the run prints is a count of edits that landed, not of edits attempted
- [ ] Regenerating the fixture from an unchanged reference deck is a no-op, so the committed
      artefact and the generator cannot drift silently

**Open questions**
- ~~Should the fixture be regenerated by a check rather than by hand?~~ **Settled 2026-08-10: the
  check already exists and the gap was that nobody ran it.** `--check` was added by T-044 for exactly
  this drift; what it lacked was a place in any routine. It is now in the release gate list
  ([`PUBLISHING.md`](../docs/PUBLISHING.md) §8) rather than being rebuilt.

## 2. Plan

| # | Step | Output |
| :-- | :--- | :--- |
| 1 | Give the regex edits the assertion the literal ones already have | `subre`, in the generator |
| 2 | Repoint the S2 disclosure removal at what actually follows it | the generator |
| 3 | Assert the two whole-block seeds, D1 and D2 | the generator |
| 4 | Regenerate the fixture and prove `--check` is a no-op afterwards | the committed fixture |
| 5 | Put `--check` where a release will run it | `PUBLISHING.md` §8 |

## 3. Implement

**Decisions & assumptions**
- **`subre` beside `sub`, rather than converting the regex edits to literals** — 2026-08-10. The S2
  removal spans twelve lines with a `data-disc` value that changed once already, so a literal would
  be brittle in exactly the way the pattern is not. What was missing was never the pattern; it was
  the assertion, and now both helpers carry it.
- **The S2 lookahead now names the bottom line, not the provenance mark** — 2026-08-10. That is what
  follows the block, and has been since the deliverable moved into the bottom line. The old lookahead
  was correct when written and silently false afterwards; `subre` is what stops the next such move
  being silent.
- **Three edits were unguarded, not one.** §1 named the S2 removal. D1's reorder and D2's slide split
  also appended to the ledger unconditionally: D1 would have claimed a defect if the permutation were
  ever edited to the identity, and D2's `next(...)` would raise an unreadable `StopIteration` if the
  slide were renamed while its two `str.replace` calls could no-op in silence. Both now fail with a
  message naming what moved.

**What changed in the fixture**

    STALE: examples\reference-deck-seeded-defects.html no longer derives from examples\reference-deck.html
           regenerating would change 12 line(s) (+0/-12).

Twelve lines, all of them the assumption marker the S2 seed had claimed to remove since the day it
was written. **The rest of the fixture was already current**, so the drift was entirely this defect
and not an accumulation. After regenerating, `--check` reports the fixture is exactly what
regenerating produces, at 244000 bytes, and the ledger prints all ten dimensions.

**Outputs produced**
- [`tools/examples/seed_defects.py`](../tools/examples/seed_defects.py) — `subre`, the repointed S2
  lookahead, and assertions on D1 and D2.
- `examples/reference-deck-seeded-defects.html` — regenerated; the slide 7 assumption marker is gone,
  which is what the ledger has always said.
- [`docs/PUBLISHING.md`](../docs/PUBLISHING.md) §8 — `--check` added to the release gate list.

## 4. Review

| Acceptance criterion | Result | Note |
| :--- | :---: | :--- |
| Every seed asserts it matched; one that does not stops the run | met | Every literal goes through `sub`, every pattern through `subre`, and the two whole-block seeds carry their own exit with a message naming what moved. |
| The S2 disclosure removal applies, or is removed with the reason | met | Applies. The lookahead now names the bottom line, which is what follows the block. |
| The dimension count is a count of edits that landed | met | `applied` is only appended to after an assertion that would have exited, so the two are now the same list rather than two claims about it. |
| Regenerating from an unchanged reference deck is a no-op | met | `OK - examples\reference-deck-seeded-defects.html is exactly what regenerating produces (244000 bytes)`, and the command is now in the release list so it is run rather than remembered. |

**Child fix tasks raised**
- none

## Log

| Date | Status change | Note |
| :--- | :--- | :--- |
| 2026-08-10 | → done | **Three unguarded edits, not the one §1 named.** Reading for the S2 case found D1's reorder and D2's split appending to the ledger with nothing checked either, which is the same defect twice more in the same file — the rule is *an append next to an unasserted edit*, and it was worth looking for the shape rather than fixing the instance. Regenerating removed exactly the twelve lines the ledger had always claimed, and nothing else, so the fixture's other nine dimensions were never in question. The open question closed without building anything: `--check` already existed and had no place in any routine, so it went into the release gate list. |
| 2026-08-10 | (specify) | **Estimated `medium`/`s`.** `medium` because the blindness fixture's entire claim is *a seeded defect on every dimension* and one seed has been counted without ever matching, so the claim is over-reported at its source; `s` because `static_variants.py`'s `build` already carries the assertion this needs. **Stays in `PH2`.** |
| 2026-08-09 | → proposed | Created by [T-016](T-016-the-interaction-and-motion-layer.md), which widened the S2 seed's pattern to admit the new `data-disc` value and found the regenerated fixture unchanged — because the seed had never matched in the first place. **PH2**: the fixture's other nine dimensions are seeded and the gate's blindness claim rests on those; this is a fixture reporting one seed it did not apply, not a gate reporting a defect it cannot see. |
