# Pre-release audit — htmldeck

**The register.** One row per finding, and the only home for a finding's statement. The method is
[`AUDIT-METHOD.md`](AUDIT-METHOD.md); the run is
[T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md), whose §2 holds the cycle
program this document records the results of.

**Status: running.** Cycles 0 to 26 and 39 are done, and eleven of stage 7 are skipped with a stated reason, so **32 of 43 are settled** - **stages 1 to 6 complete**, so what an adopter receives, how this project governs itself, the product, the decks and the specifications they were built from, the whole written record and both surfaces outside git have all been read. **Cycle 39 is done, the stage 7 checkpoint is taken, and cycle 34 is next.** The owner ruled on 2026-08-29, with 39's result in hand: **cycle 34 only, then cycle 40** - stage 7's other eleven cycles are skipped and the coverage ledger carries the reason. The owner ruled the run order on 2026-08-29 and [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 2 carries it: the figure and version sweep first, because it was the only remaining cycle whose subject is the live tree, then the checkpoint taken with its result rather than before it. **What 39 hands the checkpoint**: the live tree's figures hold wherever a command binds them and drift wherever prose carries them, and both of its findings were invisible to every instrument here - so stage 7's twelve cycles of closed record are a reading for sentences, not a sweep for numerals. The baseline is below. The tables carry what one
cycle found and nothing more — a subject with no row has not been audited, it has not been
reached.

**Two remedies have landed mid-run, and they are the only ones that will.** `PR-06` closed on 2026-08-23
through [T-223](../tasks/T-223-derive-the-audit-cycles-membership-instead-of-counting-it.md), ahead
of cycle 40's triage, because it gated every remaining cycle rather than only the run's closing
claim: the plan stated a count per cycle and never a membership, so *re-measure the cycle's file
list* had no list. **Which files a cycle reads is now `python tools/docs/cycles.py --cycle <n>`**,
and it reports the whole partition's verdict before it answers. `PR-01` closed the same day through
[T-224](../tasks/T-224-give-the-blindness-fixture-its-own-instrument-in-cycle-17.md), on the same
clause: cycle 17's brief pointed the deck gates at a fixture built to score 0, so the cycle could not
be run as written. **Everything else waits for cycle 40, and no exception remains.**

---

## 1. The run

| | |
| :--- | :--- |
| Requested | 2026-08-22, by the owner |
| Target release | **the next one.** Reordered by the owner on 2026-08-22: cut the release first, audit afterward. That release was `0.6.0`, cut 2026-08-23. The number the audit's own release takes is [`PUBLISHING.md`](PUBLISHING.md) §8's to decide at the time, not this document's to predict |
| Baseline | **Both green on a frozen tree at `62c3ab3`, 2026-08-23.** `python tools/check_all.py` — exit 0; 35 commands ran, 2 skipped with their reasons, 0 failed; 49 tracked tools, 0 unclassified, 0 stale; 294.5 s. `python tools/tasks/lint.py` — exit 0, all four steps, 3.3 s, with eleven advisories `taskmd check` prints and does not fail on (a duplicate index in [`RELEASE-PHASES.md`](RELEASE-PHASES.md), ten unresolved section references), baselined here so a later cycle can tell a new one from an old one. **The seconds are a dated record, not a stated figure** (**L-95** point 4) — re-run the command, never cite this cell |
| Id space | `PR-nn`, never reused, never renumbered |

---

## 2. Coverage ledger

One row per cycle, written when the cycle ends. Every tracked file lands in exactly one of **read**,
**skipped with a stated reason** or **produced a finding**; a file in none of the three is a gap in
the audit ([`AUDIT-METHOD.md`](AUDIT-METHOD.md) §2).

| Cycle | Subject | Files | Bytes read | Findings | Skipped, and why | Session |
| :--- | :--- | ---: | ---: | :--- | :--- | :--- |
| **0** | Instruments and baseline — `check_all.py`, `findings.py`, `lint.py`, `query.py` | 4 | 61,356 | `PR-01` | none: all four read whole, and the file list re-measured to the planned 61,356 bytes exactly | 2026-08-23 |
| **1** | The human-facing set — `README.md`, `examples/README.md`, `LICENSE`, `shell/README.md`, `examples/sources/README.md`, and the two manifests under `.claude-plugin/` | 7 | 55,103 | `PR-02`, `PR-03`, `PR-04`, `PR-05` | none: all seven read whole. The plan's 54,585 was exact at its own commit; `README.md` and `examples/README.md` both grew on 2026-08-22 after it was measured, so the subject is **+518 bytes** and the re-measure is what caught it. [`PUBLISHING.md`](PUBLISHING.md) section 2 was *applied* as the authority on the covered set, not read — it stays cycle 4's file, and `PR-05` is recorded here so cycle 4 does not raise it twice | 2026-08-23 |
| **2** | The skill and the prompt — `SKILL.md`, its four reference documents, and `reference/example-prompt.md` | 6 | 58,890 | `PR-07`, `PR-08`, `PR-09`, `PR-10` | none: all six read whole, and the subject re-measured to the planned 58,890 bytes **exactly** - the first cycle whose figure has not moved since the plan, and the first measured by `python tools/docs/cycles.py --cycle 2` rather than by hand. `README.md` is cycle 1's file and was read there; `PR-07` and `PR-10` both name a line in it, recorded here because what they are about is the skill's path mechanism and the worksheet's own count | 2026-08-23 |
| **3** | Tier 1 and the brief — `CLAUDE.md` and `BRIEF.md` | 2 | 60,449 | `PR-11`, `PR-12`, `PR-13`, `PR-14` | none: both read whole, and the subject re-measured to 60,449 against the plan's 60,103 - `CLAUDE.md` grew 346 bytes after the grade table was taken. **Four claims were checked and hold exactly**: the three files a session receives unasked (this session received precisely those three); the bound's two terms, 15,572 against 13,324, and the stated debt of 2,248; the gate's 92 of 122 with 30 named; and `0.6.0` as the published version. `EVALUATION.md` and `SKILL.md` are cycles 8's and 2's files and each carries one instance recorded above, so neither cycle raises it twice. `DESIGN-RATIONALE.md`'s *twenty-nine conflicts* was checked against its source and the brief reports it faithfully; whether the rationale's own sixteen matches its eleven `C-nn` ids is **cycle 20's** question and is not answered here | 2026-08-23 |
| **4** | The release machinery — `PUBLISHING.md`, `RELEASE-HISTORY.md`, `.gitignore`, `.gitattributes` | 4 | 60,607 | `PR-15`, `PR-16`, `PR-17`, `PR-18` | none: all four read whole. **The plan said six files and 69,189 bytes**; the subject's own line names four, and the two it cannot name are `PR-06`'s unlocatable half, now derived rather than reconciled. **Three checks passed**: section 8 step 7 against `gh release list`, which returns a published page for all seventeen tags; every `Tasks` figure in `RELEASE-HISTORY.md` section 1 against the command that document names; and section 8.1 carrying a row for every release since `0.2.0` with no `*next*` row left after a tag. `PR-05` is this file's and was raised at cycle 1, so it is not raised again | 2026-08-23 |
| **5** | The tracker's own rules — `README.md`, `TASK-WORKFLOW.md`, `TOOLING.md`, the two templates, `.taskmd/config.md`, `.handoff/config.md` | 7 | 112,267 | `PR-19`, `PR-20`, `PR-21`, `PR-22`, `PR-23` | none: all seven read whole, the board's generated block included. The subject is **+248 bytes** on the plan's 112,019, which is `.handoff/config.md` growing when [T-222](../tasks/T-222-derive-the-reconcile-sweeps-membership-instead-of-enumerating-it.md) replaced its enumeration. **Three checks passed against all 223 task records**: every task at `specified` or later declares a deliverable, which `TOOLING.md` section 3 calls a convention no gate enforces and which nothing violates today; `.handoff/config.md`'s dated measurement of fifteen documents and four subdirectories under `docs/` is exact; and `taskmd check` prints the same eleven baselined advisories and no new one | 2026-08-23 |
| **6** | The release plan — `RELEASE-PHASES.md` | 1 | 179,575 | `PR-24`, `PR-25`, `PR-26` | none: read whole, 590 lines. **The subject is +2,133 bytes on the plan's 177,442.** The strongest result is a pass: **all 154 rows carrying a task id agree with that task's record on open versus closed** - no struck-through row is still open and no plain row has closed - which is a 179 KB hand-kept decision record in step with 223 task files. `findings.py --check` separately holds its execution order to `1..n`. **The audit fixed its own instrument here rather than raising a finding**: the cross-check returned one answer it could not explain, and the cause was `cycles.py` splitting front matter on the substring `---`, which `T-107`'s title quotes - so that `PH1` record was assigned to cycle 38 instead of 28. The partition was complete and one file was in the wrong cycle, which no check here could see. Fixed, asserted in the self-test, and recorded in [T-223](../tasks/T-223-derive-the-audit-cycles-membership-instead-of-counting-it.md); it takes no `PR-nn` because the instrument is this run's own and was built the same day, not found in the tree the audit came to read | 2026-08-23 |
| **7** | The unreleased work, and this audit's own record — `AUDIT-METHOD.md`, this register, and the four open or unreleased task records | 6 | 123,250 | `PR-27`, `PR-28` | none: all six read whole. **The subject is 123,250 bytes against the plan's 296,551, and the cycle did not shrink - its subject moved**: `0.6.0` released thirteen of the eighteen records the plan sized, on the morning cycle 1 ran. **Three checks passed.** The next release requires nothing of a deck and [`PUBLISHING.md`](PUBLISHING.md) §8.1 has no `*next*` row, which is correct rather than missing - §8.1 says a row *may* be written when work lands and step 5 is where it is owed. [T-057](../tasks/T-057-the-3d-class-the-frame-rate-figure-and-ds-140s-fifth-motion.md) still names `T-214` in `blocked_by` and `T-214` shipped in `0.6.0`; that is the documented behaviour rather than a defect, because taskmd `0.3.0` filters closed tasks out of both edge columns ([`../tasks/TOOLING.md`](../tasks/TOOLING.md) §1). And `AUDIT-METHOD.md` §2's *five tracked `.html` files are 1,773,568 bytes* is exact today and is `PR-01`'s subject already, so it is not raised twice. **This cycle also brought its own subject current**: the register's status line still read *cycles 0 and 1 of 43*, which cycles 2 to 6 had made false. Corrected rather than raised - a document this run writes into is this run's to keep true | 2026-08-23 |
| **8** | The design system and the evaluation - `DESIGN-SYSTEM.md` and `EVALUATION.md` | 2 | 146,650 | `PR-29`, `PR-30`, `PR-31`, `PR-32`, `PR-33` | none: both read whole, and the subject re-measured to the plan's 146,650 bytes **exactly** - the second cycle whose figure has not moved since the plan. **The brief's first question is answered by a run rather than by reading, and it passes**: `python tools/deck/check.py examples/reference-deck.html --sources examples/sources` exits 0 and its coverage account partitions all **122** owned rules - 92 checked, 0 failing, 3 excused in the rules, 27 excused here, 0 undecided, 0 silent - so every `DS-nnn` a gate owns is decided or named with a reason, which is the claim [`../CLAUDE.md`](../CLAUDE.md) and [`EVALUATION.md`](EVALUATION.md) section 2 both make. **Two further checks passed**: no id cited in a rubric dimension is absent from the ruleset, and the clause-level account names its own six unreached clauses across five conjunction rules rather than reporting them checked. **The second question - the ten dimensions against what an instrument can reach - is where four of the five findings came from**, and they are about the document's own bookkeeping rather than about the dimensions: a false invariant, a checklist sized three ways, five stale cells and a duplicated section number. `PR-12` already holds this file's *164 as of 2026-08-09* block and is not raised again; `PR-32` states what it does and does not cover | 2026-08-23 |
| **9** | The three contracts - `COMPONENT-CONTRACT.md`, `THEME-CONTRACT.md`, `MOTION-GUIDE.md` | 3 | 76,319 | `PR-34`, `PR-35`, `PR-36`, `PR-37`, `PR-38`, `PR-39`, `PR-40`, `PR-41` | none: all three read whole, and the subject re-measured to the plan's 76,319 bytes **exactly**. **The cycle's brief asks for a contracted part nothing enforces, and the answer came from running each contract's own instrument rather than from reading**: `component.py check` and `theme.py check` are both green on the reference deck - 102 parts, 100 styled classes with 0 uncontracted, 17 motion rules with 0 gaps, 133 tokens with 0 problems, 51 literals all exempt - and **every finding here is something those two runs are built not to see.** Three checks passed and are worth recording: every `hard` band `theme.py check` states holds in the shipping theme; the reference deck's own eight state attributes each name a governing rule; and `themes/quarto.css` conforms whole. **Two files outside this cycle carry a finding** - `themes/lattice.css` is cycle 16's and `tools/deck/theme.py` is cycle 12's - both reached through the contract that names them and recorded here so neither cycle raises it twice. One claim was checked and stands: section 1's *a part with no styling is still a part* names `.disc-label` as the one proving it, and five parts are in that state today; the sentence reads as an existence proof rather than a count, so it is noted rather than raised | 2026-08-23 |
| **10** | The gate's code - `check.py`, `content.py`, `contract.py`, `contrast.py`, `density.py`, `figgrid.py`, `glitchfree.py`, `markhits.py`, `ruleset.py` | 9 | 240,446 | `PR-42`, `PR-43`, `PR-44`, `PR-45`, `PR-46`, `PR-47` | none: all nine read whole, and the subject re-measured to the plan's 240,446 bytes **exactly**. **The brief's first question - is each undecided rule's stated reason still true - was run over all thirty and two failed**, both in `PR-46`; the other twenty-eight hold, including the two that rest on a deck having no subject. `DS-087`'s *no deck in the repository has an appendix* was tested rather than assumed: the string appears in all five decks and every occurrence is a comment about the colophon rule. **The second question - does a decided rule decide what it says - produced four of the six**, and the pattern in them is one thing: a check whose **subject** is narrower than its rule's, reported with a denominator that reads as the whole. One diagram per slide (`PR-42`), one clause of three (`PR-43`), three class names rather than a declaration (`PR-44`), one deck's unit vocabulary (`PR-45`). **Three checks passed and are worth recording**: the coverage partition sums to 122 on every run; `producer_split` classifies all twelve non-static producers with a written reason each and fails the run on one in neither half; and `glitchfree.py`, `markhits.py` and `contract.py` each self-test both directions, including the absent-subject case that returns `None` rather than a pass. `themes/lattice.css` is cited in `contract.py`'s tolerance comment as a live second theme, which is `PR-37`'s subject and is not raised again | 2026-08-23 |
| **11** | `audit.py` and `critique.py` | 2 | 198,718 | `PR-48`, `PR-49`, `PR-50`, `PR-51`, `PR-52` | none: both read whole, and the subject re-measured to the plan's 198,718 bytes **exactly**. **The brief's three questions were each answered by a measurement rather than by reading.** *What is dead*: parsing every module-level definition and searching the whole tracked tree for each returns **two** dead names in 181,662 bytes and **none** among the 80 functions - `PR-51`, and the honest result is that the file is not full of dead code. *What is duplicated from `check.py`*: comparing every compiled pattern in `audit.py` against `check.py`, `content.py`, `component.py`, `density.py`, `theme.py` and `ruleset.py` returns **no shared literal**, and the one constant that looks duplicated - `QUICK_VIEW` - is an alias to `content.py`'s with the reasoning recorded beside it. *Does the stage split still match the document*: **no**, and that is `PR-50`. **The two Highs and Mediums came from a fourth question the brief did not ask** - whether each `hard` `auto` predicate implements the rule it cites - and the answer is two that do not, in opposite directions: one word short (`PR-48`, four instances shipped) and one licence narrow (`PR-49`, nothing failing yet). **Three checks passed and are worth recording**: `ABSENCE_IS_A_PASS` and `ABSENCE_IS_A_FAIL` declare every row that survives an empty measurement with its shape and subject, and the self-test verifies a *guarded by* claim rather than trusting it; `Measurement` records which keys a row read, so an unmodelled key is a named failure rather than a silent `.get()` default; and `critique.py` refuses an incomplete worksheet, which is the one thing a program can enforce about a judgement. `PR-43`'s subject is visible from this side too - DS-091's own comment records the same defect being fixed once, in the one place `check.py`'s rule says an excusal must not live | 2026-08-23 |
| **12** | The build path - `component.py`, `preflight.py`, `presenter.py`, `render.py`, `shell.py`, `spec.py`, `theme.py` | 7 | 268,414 | `PR-53`, `PR-54`, `PR-55`, `PR-56` | none: all seven read whole, and the subject re-measured to the plan's 268,414 bytes **exactly** - the third cycle whose figure has not moved. **The brief's first question came back almost empty, and that is the result rather than a shortfall**: parsing every exception handler in the seven returns **four**, all typed, and every one is either inside a self-test asserting a refusal or a documented `None` return - there is no silent failure path in the build. What the question found instead is one **loud** failure with a short search behind it (`PR-56`) and one loud failure blaming the wrong thing (`PR-53`). **The third question is answered by construction**: the presenter build shares **nothing** of the build path - it post-processes a built deck by injecting at `</body>`, takes every length from the deck's own tokens, matches notes by `data-name` rather than by position with no positional fallback, writes beside the deck, and `.gitignore` anticipates that output with the argument written down. **Four checks passed**: `python tools/deck/shell.py check` is clean on all four gated decks; the seven tokens the presenter panel reads are all declared in the shipping theme; `shell.py`'s self-test compares its dispatched commands, its usage list and its per-command help against each other, read out of its own source; and `render.py`'s self-test sweeps the package directory for a second copy of the motion pin or the console trap rather than trusting a list. **Two findings from cycle 9 name files in this cycle and are not raised again** - `PR-34` on `component.py`'s unchecked sources and `PR-38` on `theme.py`'s crashing refusal path. One thing was checked and noted rather than raised: `render.calibrate` prints a warning and returns when the corrected viewport does not land, so a measurement can be taken at a size nobody asked for - it has never fired here, and the deviation it would allow is unbounded | 2026-08-23 |
| **13** | The rest of `tools/deck/` - `chrome_row.py`, the four `*_variants.py` suites, `contents_bound.py`, `fps.py`, `longdeck.py`, `paths.py`, `printgeom.py`, `printpages.py`, `quickview.py`, `rulerstrip.py` | 14 | 270,560 | `PR-57`, `PR-58`, `PR-59`, `PR-60`, `PR-61`, `PR-62`, `PR-63`, `PR-64` | none: all fourteen read whole, and the subject re-measured to the plan's 270,560 bytes **exactly** - the fourth cycle whose figure has not moved since the plan. **The brief's question is answered by a measurement rather than by reading, and the partition has no third bucket**: every one of the fourteen is named with a reason by `check_all.py`'s report, so *neither* is empty. Three appear in no document at all - `longdeck.py`, `rulerstrip.py` and `paths.py` - and `paths.py`'s reachability is real, because fourteen tools import it. What the question actually found is `PR-62`: the one table naming the other two does not say how to run them. **Four checks passed.** `paths.py`'s self-test is green and `longdeck.py --self-test` splices the 13-slide deck to 17, 25 and 43 with the stage set unchanged. **The T-074 question was asked of all five tools that anchor their output at the package directory rather than at `paths.output_root` - the four variant suites through `render.OUT`, and `print_variants.py` directly - and none reopens it**: each builds from a fixed `examples/reference-deck.html` that lives in this repository, so the output does go to the deck's own project. Every one of the eight `NOT_RUN` entries covering this cycle states a reason that is still true. **One finding names a file this cycle does not own and says so** - `PR-61`'s second file is `presenter.py`, read at cycle 12; it is raised here because the measurement that found it is this cycle's. **And one earlier finding is visible here and is not raised again**: `contract_variants.py`'s comment about sampling the default rather than one slide is `PR-53`, which cycle 12 owns | 2026-08-23 |
| **14** | `tools/docs/` - `cycles.py`, `figures.py`, `lessons.py`, `refcheck.py`, `screening.py` | 5 | 188,895 | `PR-65`, `PR-66`, `PR-67`, `PR-68`, `PR-69` | none: all five read whole, and the subject re-measured to the plan's 188,895 bytes **exactly** - the fifth cycle whose figure has not moved. All four gate instruments were run directly first and all four are green. **The brief's named question - `refcheck.py`'s skip list - is answered by measurement, and the skip is honest.** It reports 3,021 section marks *not bound to a document and skipped*, which reads like three quarters of the subject going unchecked. Of the 2,848 that sit inside a document numbering its own headings, **1,628 would resolve against the containing document and 1,220 would not** - and the 1,220 are overwhelmingly cross-document references written without an adjacent name, which is precisely what the adjacency rule exists to refuse, so binding a bare mark to its own document would report about twelve hundred problems and almost every one would be wrong. **The one sub-family where binding is unambiguous was measured separately**: `DOC.md` followed by a second mark - *section a and section b* - is 57 marks that refcheck does not bind, and **all 57 resolve**. So the gap is real, named, and has caught nothing. **Three more checks passed.** `FLOOR` is the one hand-kept table in `figures.py` with no `missing_*` guard beside it, and it needs none: an entry that stops matching makes its block compared *exactly* rather than as a floor, so the run reddens instead of going quiet - the opposite direction from `ARTIFACTS`, `ACCOUNTS` and `MEASURED`, which is why those three have one. The README's `refcheck` block has drifted from 3,357 pointers to 3,762 and from 2,959 skipped marks to 3,021, and is reported rather than failed, which is the floor working as written. And `screening.py`'s two anchors both fail loudly rather than quietly if either document renumbers - an empty screening table disagrees with the stated partition, and an empty catalogue says the cross-document check covers nothing. **One finding named a file outside this cycle and was withdrawn**: `PR-66`'s subject is cycle 17's, and it is `PR-01` - raised at cycle 0 and not checked for before this cycle raised it again. The evidence it added is on that row now, and the cycle's real count is four | 2026-08-23 |
| **15** | The remaining tools - `assets/` (three), `examples/` (two), `kb/extract.py`, `plugin/check_scaffold.py`, `portability/` (two) | 9 | 265,632 | `PR-70`, `PR-71`, `PR-72`, `PR-73`, `PR-74`, `PR-75`, `PR-76` | none: all nine read whole, and the subject re-measured to the plan's 265,632 bytes **exactly** - the sixth cycle whose figure has not moved. The three that are gates were run directly first and all three are green. **The brief asks whether `check_all.py`'s classification matches what each one actually does, and the answer is a High.** Two of `check_scaffold.py`'s eight checks have no live subject: check 7 reads nought of the skill's eighteen documented commands and check 5 resolves one path against forty-eight, because the skill moved to its own `$HTMLDECK` placeholder on 2026-08-20 and the patterns did not follow (`PR-70`). **Three more measurements were taken and two of them passed.** Only one of the nine is imported by another tool - `chart_probe.py`, by the gate `portfolio_charts.py` - which is right and unstated (`PR-73`); three of fifty tracked tools define no self-test and two of the three are this cycle's (`PR-74`); and every `$HTMLDECK` path in the skill resolves in this clone, so the package is sound and only the instrument is blind. **The classification itself holds for eight of the nine**: each is a gate, a builder, or a research instrument for the task its reason names, and each reason is still true. **Two things were checked and noted rather than raised.** `run_probes.py` reads the value after `--page` without a bound test, which is `PR-64`'s shape in a second file and not worth a second row. And `measure.py` carries a `raise last` after its retry loop that no path reaches, because the third attempt raises inside the loop - dead, harmless, and the sort of thing a row would bury `PR-70` under | 2026-08-23 |
| **16** | The shell and the themes - `components.css`, `deck.js`, `icons.svg`, `shell.html`, `quarto.css`, `lattice.css` and the three embedded faces | 9 | 272,593 | `PR-77`, `PR-78`, `PR-79` | none: all nine read whole, and the subject re-measured to the plan's 272,593 bytes **exactly** - the seventh cycle whose figure has not moved. The three `themes/faces/*.css` are one embedded woff2 each behind an OFL 1.1 header, and reading them whole means the header and the licence rather than the base64. **All three of the brief's questions were answered by measurement, and two came back clean.** *Dead selectors*: **none** - all 112 class selectors in `components.css` are used by the shell, the script or the five decks, and the only apparent unused ids were hex colours the scan mistook for selectors. *Tokens nothing reads*: 7 of the 138 the shell and the two themes declare have no `var()` reader, and **four of the seven are live** - `--motion-kind`, `--motion-long`, `--motion-subject` and `--pager` are read by `audit.py`, `density.py`, `shell.py` and `static_variants.py`, because a declaration an instrument reads is still read. Two of the three left are `PR-36`'s and are not raised again; the third is `PR-77`. *A theme value that is not parametric*: every literal in the shared block - 8 colours and 16 lengths - is inside `@media print` or the degraded state, which is the one block documented as literal-only and for a stated reason, so the question yields one Low about **whose** colours they are rather than a systemic finding. **`PR-37` was re-run and is not raised again**: `python tools/deck/theme.py validate themes/lattice.css` still exits 1 and `themes/quarto.css` still conforms, exactly as cycle 9 recorded. **Two checks passed that were worth taking.** The chrome tail's missing element was found by reading `shell.html` against `deck.js` and then **disproved as a build defect** - `shell.py`'s two tail forms both carry all five ids, so a deck built the documented way cannot reach it, which is what turned `PR-78` from a broken build into a disagreement between three documents. And the font-licence rule holds: three faces, three OFL headers, repeated with Lucide's in `shell.html` | 2026-08-23 |
| **17** | The shipped decks and the blindness fixture - `reference-deck.html`, `measure-first.html`, `portfolio-review.html`, `sort-window.html` and `reference-deck-seeded-defects.html` | 5 | 1,773,568 | `PR-80`, `PR-81`, `PR-82` | none, and no file was read: **grade C, two instruments, and each ran on the whole of its own half.** The four decks re-measured to 1,451,311 bytes and the fixture to 322,257, so the subject re-measured to the plan's 1,773,568 **exactly** - the sixth cycle whose figure has not moved. **What the instruments measured, not that they ran** (`AM-11`): `check.py` green on all four **with the content half run rather than skipped** - `--sources` supplied, 133 to 134 verdicts each, `undecided, no subject 0`, and the two 134s differ from the two 133s by one per-instance subject (`DS-143`, `DS-140`) rather than by coverage; `audit.py` green on all four; `glitchfree.py` 0 failing conditions across 4 decks, 3 of 3 declared faces loaded on each, 0 scrolling stages, and every slide reached over 12, 12, 13 and 14, with `GF-7` NO SUBJECT on all four because none draws a canvas; `printgeom.py` green on all four - 12, 12, 13 and 14 contents cards, each over one sheet, none intersecting and none reaching the footnote. **All four decks were printed rather than two, and all 55 printed pages were looked at** - which is what found `PR-80`, on the pages `PRINT-2` and `PRINT-3` call green. The fixture's own instrument, `python tools/examples/seed_defects.py --check`, reports it byte-exact against regeneration at 322,257 and carrying a seeded defect on all ten dimensions, D1 to D4 and S1 to S6; a deck gate's reds on it would be it working, so no deck gate was pointed at it. **Two of cycle 18's files were read as evidence and are not claimed here**: `examples/sources/cost-model.md` and `examples/sources/programme-timetable.md`, which `PR-81` cites and `PR-82` is about | 2026-08-23 |
| **18** | The deck specifications and sources - three `.foundation.md` / `.slides.md` pairs, ten source documents, and the adopter deck's six diagram files | 25 | 301,861 | `PR-83`, `PR-84`, `PR-85`, `PR-86`, `PR-87`, `PR-88`, `PR-89` | none: all twenty-five read whole, and the subject re-measured to the plan's 301,861 bytes **exactly**. **Both of the brief's questions were answered by running an instrument before reading anything, and the second is where the High came from.** *Each specification against the deck built from it*: `python tools/deck/spec.py` on all three pairs **with the built deck supplied** returns SPEC-1 to SPEC-5 pass on each - and SPEC-5 is the only rule that reads the deck, so every ledger row's `Used on` reaches text on the slide it names, across 58 rows on sort-window, 29 on portfolio-review and 38 on measure-first. *Each source against what the deck claims it says*: `quickview.py check` returns 3 of 3 match on sort-window and 5 of 5 on measure-first, and **2 of 12 on portfolio-review**, which is `PR-83`. The reference deck carries no quick view, so `check_all.py`'s stated reason for skipping it there is still true, and its three sources were checked the other way instead: **all 30 figures they state appear in the deck's own text**, from the $5.6M grant to the 2028 crossover, and every derived one reproduces - 6+11+13+4 is the 34-minute trip with 15 minutes of it not moving at 44%, and 4,200+2,600+5,400 = 12,200 against 14,800+16,300+4,900 = 36,000. **The sanitisation rule was re-run rather than cited**, over all thirteen `measure-first` files and every file type: case identity, place names, training context and local-system paths are zero, and both BPMN `targetNamespace` values carry the sanitised name, which is **L-111**'s repair still working. The seventh class does not hold, and it is `PR-86`. **Two of this cycle's files were read at cycle 17 as evidence and are its own subject here**: `examples/sources/cost-model.md` and `programme-timetable.md` were re-read whole and `PR-81`'s $1.5M contradiction is confirmed in the source exactly as that row states it, so it is not raised again (`AM-12`). **Four things were checked and noted rather than raised.** Section 3 says its rows are *ranked by severity, then by the reader's cost* and all 89 stand in raise order, which is what a register written one cycle at a time produces and what cycle 40's triage is for. The *nth cycle whose figure has not moved* series in the rows above disagrees with itself - cycle 16 reads *seventh* and cycle 17 *sixth*, and cycles 9, 10 and 11 re-measured exactly without being counted - so this row states the fact and adds no ordinal. `D5`'s 2x2 labels its low-value quadrant `DEPRIORITISE` where its own table beneath and the deck both say *leave it*. And **every euro figure in `D4` reproduces from the arithmetic printed beside it** - the six value categories sum to the stated EUR 1.2m, EUR 18.6m at 3.2 turns gives the EUR 59.5m that makes EUR 95m of revenue a 63% cost ratio, 264 days at EUR 400 is the productivity line, and the three-year cumulative reaches +EUR 1.66m - so the adopter deck's numbers hold and only its references do not | 2026-08-23 |
| **19** | The prior audits - `CONTEXT-AUDIT.md` and `RULESET-AUDIT.md` | 2 | 119,302 | `PR-90`, `PR-91`, `PR-92`, `PR-93`, `PR-94`, `PR-95`, `PR-96` | none: both read whole, and the subject re-measured to the plan's 119,302 bytes **exactly**. **The brief says `findings.py --check` decides part of this cycle and not all of it, so what it measured is recorded rather than that it ran** (`AM-11`): it reads §6's thirteen rows of [`CONTEXT-AUDIT.md`](CONTEXT-AUDIT.md) against the tasks naming them and returns *13 linked, 14 task(s), execution order consecutive* - all thirteen closed, every one served by at least one task, so its both-directions check had a subject on every row and the branch that skips an unserved finding never fired. **What it does not reach is most of the brief**: it never opens [`RULESET-AUDIT.md`](RULESET-AUDIT.md), it decides a row's *status* against a task rather than its *statement* against the tree, and it says nothing about §6.1, §7, §8, §9 or §10. Those were read and checked by hand, and six of the seven findings came from there. **Four checks passed and are worth recording.** All **165** `Check/Reach` pairs in `RULESET-AUDIT.md` §5 still agree with [`DESIGN-SYSTEM.md`](DESIGN-SYSTEM.md) exactly, so the one column that document promises stays true has - which is also what makes `PR-90`'s twelve missing rows a membership gap rather than general drift. **The audit's own re-runnable expression for its central finding returns 0 today**: of 48 `judge` rules, 19 are not `hard`, and every one of the 19 is named or ranged by [`EVALUATION.md`](EVALUATION.md) or under `skills/`, so the thirteen once-uninstrumented rules are still instrumented after twelve more rules landed. Every one of the **27** tasks `CONTEXT-AUDIT.md` names resolves to a record and all 27 are closed, so no row is stale in the open-versus-closed sense and the staleness is entirely in prose describing what those closures changed. And §10.4 carries the closure note §4.1 lacks, which is what makes `PR-91` a defect rather than this document's house style. **Two things were checked and noted rather than raised.** §3's skill and shell figures have all moved - `SKILL.md` 5,206 to 6,291, the four references 36,407 to 51,403, `shell/` 114,931 to 160,282 - but §0 dates the whole document 2026-08-13 and §3's conclusion is structural rather than sized, so the growth does not touch it. And §6.1's `CE-13` row and §2.4's inventory both carry the *three example decks, 810,746 bytes* figure that `PR-11` raises against tier 1; here it sits inside a dated measurement table rather than a live rule, so it is not raised twice (`AM-12`) | 2026-08-24 |
| **20** | The design rationale - `DESIGN-RATIONALE.md` | 1 | 69,194 | `PR-97`, `PR-98`, `PR-99`, `PR-100` | none: read whole, and the subject re-measured to the plan's 69,194 bytes **exactly**. **Cycle 3's parked question is answered and the answer is a pass**: §2's *sixteen were found by reading the sources against each other* is exact - the table holds 16 rows, 11 keyed `C-01` to `C-11` and 5 keyed by their `R2 §12.n` section - so the sixteen never was a count of the `C-nn` namespace, and §1's *a named contradiction C-01...C-11* is the subset it says it is. **The brief's question - a decision recorded here that the product no longer implements - was answered by reading every recorded decision back against the rule that carries it, and the ruleset came back clean.** All **98** `DS-nnn` this document cites still exist in [`DESIGN-SYSTEM.md`](DESIGN-SYSTEM.md), with no dangling id; `DS-000` is the ruleset's own departure clause and is the only cited id with no rule row. Sixteen rule texts were read against the decision recorded for them - `DS-011`, `DS-030`, `DS-034`, `DS-035`, `DS-036`, `DS-045`, `DS-071`, `DS-131`, `DS-141`, `DS-168`, `DS-216`, `DS-217`, `DS-219`, `DS-227`, `DS-228`, `DS-230`, `DS-231` - and **every one still says what this document says it says**, including the four amendments §3 attributes rule by rule and the four the 2026-08-17 audit made. **So the one decision the product has left behind is not in a rule at all**: it is `PR-97`'s §2.2, where the rules moved and the record of their cost did not. **Five more checks passed.** `contract.py`'s `SAMPLE = [0, 4, 7, 11]` is §3's *slides 1, 5, 8 and 12* exactly *(the constant became `sample_for` on 2026-08-29 under `PR-53`; it returns those four indices for a twelve-slide deck, so the check this cycle ran still holds and now holds for a deck of any length)*, and `examples/README.md` still quotes the 40 and 84 §3 says it does. `DESIGN-SYSTEM.md` still ends at §9, which is §5.5's *it has ended at §9 in every commit of its life*. `check.py` still carries `DS-041` as an excusal with an `amendment` closing condition, so §6.0's *the check itself is not written* holds. The reference deck carries exactly the ten tier-two panels §5.10 reads its four kinds off - 3 `derivation`, 3 `scope`, 3 `condition`, 1 `instances`. And all **21** tasks this document names resolve to a record and none is open. **One earlier ledger row was re-tested rather than trusted and it holds**: cycle 18's *the reference deck carries no quick view* survives a second instrument - the deck has two `<template class="qv-src">` elements, but `python tools/deck/quickview.py list examples/reference-deck.html` returns **0 quick view(s)** and the deck carries no `data-file` attribute at all, so the templates are shell scaffolding and `check_all.py`'s stated skip reason is still true. **One thing was checked and noted rather than raised**: §6.0's *165 examined, 161 untouched, 4 changed, 0 deleted* is the same 165 `PR-90` raises against [`RULESET-AUDIT.md`](RULESET-AUDIT.md), but §6.0 is explicitly *the record of what moved* in that run rather than a claim about today's ruleset, so it is correct here and not raised twice (`AM-12`) | 2026-08-24 |
| **21** | Lessons `L-01` to `L-77`, and the index - `LESSONS.md` plus 77 files under `lessons/` | 78 | 167,770 | `PR-101`, `PR-102`, `PR-103`, `PR-104`, `PR-105`, `PR-106`, `PR-107` | none: all 78 read whole. **The subject did not re-measure to the plan's 167,650 - it is 167,770, and chasing the 120 bytes is what produced `PR-101` and `PR-102`**, which are cycle 7's subject and are recorded against it (`AM-09`). **What the instrument decided, rather than that it ran** (`AM-11`): `python tools/docs/lessons.py` returns *137 lesson(s), 137 row(s) in the index, 2408 citation(s) across 388 file(s) resolved*, and its three checks are the id/heading/category shape of each file, the index against the files **in both directions** including title and category, and every `L-nn` written in a tracked `.md` or `.py` resolving to a file. **What it does not reach is the whole of the brief's first half and most of its second**: it never reads a lesson's body, so *a lesson whose mechanism the tree no longer has* is entirely a reading; and of the index it decides the generated block only, never the preamble above it. Four of the six findings came from reading and two from the step-2 command. **Six checks passed and are worth recording.** The category partition is exact - 76 `Evidence and verification`, 52 `Tooling`, 9 `Writing`, summing to 137, with all three sections present in the generated block, so the tool's named-not-discovered category list still describes the set. The preamble's two figures are **correctly framed rather than stale**: *983 citations across 165 files* is dated 2026-08-14 and followed by *run it to see the current count rather than trusting this one*, which is `L-52`'s remedy working, and it is why `PR-103` names two lesson bodies and not the index. **The mechanisms the lessons name were checked against the tree and came back present**: `contract.py`'s two directions are `component.py`'s **completeness** claim and `theme.py`'s *declared and the contract does not name it*; `component.py` cuts `@media print` out rather than truncating at it and self-tests the far side (**L-68**); `shell.py`'s self-test asserts the cut round-trips on a real deck (**L-49**); the icon reference pattern is still three exact forms with a comment refusing to loosen it (**L-50**); `contrast.py` still carries the pair count citing **L-36** (**L-44**); `figures.py` still cites **L-05** in its success line and nothing narrower has been built, so that lesson's *nothing is built yet* holds. **Two things were checked and noted rather than raised.** `L-72` says the contract's `vocabulary` source emptied and [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) agrees, which is `PR-100`'s subject read from the other end and not raised twice (`AM-12`). And the index preamble's *`L-07` and `L-11` are cited from `tools/assets/build_probe_deck.py`* is still literally true while **49** tracked `.py` files now cite an `L-nn` - the sentence understates the population its own rule protects by 49 times, and it makes no false claim, so it is left as a note here | 2026-08-28 |
| **22** | Lessons `L-78` to `L-137` - 60 files under `lessons/` | 60 | 161,852 | `PR-108`, `PR-109`, `PR-110` | none: all 60 read whole. **The subject is 60 files and 161,852 bytes against the plan's 59 and 158,429** - `PR-101` names this cycle as one of its six drifted rows and predicted the file count exactly, so step 2's command was taken as the authority and the cell was not trusted. **The cycle could not start, and the instrument was fixed rather than a finding raised** (cycle 6's precedent): `python tools/docs/cycles.py --cycle 22` exited **1** with *the partition no longer holds: 28 file(s) in no cycle* - the whole of `docs/adopter-reports/claimai/`, 88,610 bytes that arrived with [T-225](../tasks/T-225-triage-the-claimai-adopter-report.md) on 2026-08-28 and belonged to no rule. The rule `docs/adopter-reports/*` was added to cycle 24, whose subject is the other received and non-research documents under `docs/`; that cycle is `pending`, so no closed cycle was disturbed, and it goes from 10 files to 38 and 305,209 bytes - inside the programme's own ~300 KB sizing. `cycles.py` then exits 0 and the whole-tree run reports every tracked file in exactly one cycle. **It takes no `PR-nn` because the instrument is this run's own**, not something the audit came to read. **What the instrument decided, rather than that it ran** (`AM-11`): `python tools/docs/lessons.py` returns *137 lesson(s), 137 row(s) in the index, 2477 citation(s) across 389 file(s) resolved* - against cycle 21's 2408 across 388, so the citation graph grew by 69 and stayed whole. It decides the id, heading and category shape of each file, the index in both directions, and that every `L-nn` written anywhere resolves. **It reads no lesson body**, which is the whole of this cycle's brief, so **all three findings came from reading and none from the command**. **Three checks passed and are worth recording.** `L-109`'s *all 27 live entries* still measures 27 - `check.py`'s `DEFERRED` holds exactly 27 keys today, so the count that carried its argument has not moved. `L-88`'s *five of them* still describes the tier-2 set, which [`../CLAUDE.md`](../CLAUDE.md) still names as five documents, so the comparison set `L-94` closed is intact. And `L-135`'s *named two documents and one subdirectory* agrees with [`../.handoff/config.md`](../.handoff/config.md)'s own dated note that it previously enumerated `RELEASE-PHASES.md`, `CONTEXT-AUDIT.md` and `docs/upstream/`. **Two things were checked and noted rather than raised** (`AM-12`). [`lessons/L-125.md`](lessons/L-125.md) reads *the account still counts it among the 121* and `python tools/deck/ruleset.py` prints **122** rows - but that population is already `PR-103`'s stated subject, *one population is stated three ways in three documents and no two agree*, so a fourth instance is recorded here rather than given a row. And nine of this cycle's files end on a trailing `---`; `PR-106` names all nine and records the class whole under cycle 21 (`AM-09`), so they are not raised twice. **One finding is recorded against another cycle** (`AM-09`): `PR-110`'s subject is [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md)'s own counts table, which is cycle 7's file, and it was found while writing this cycle's rows into it | 2026-08-28 |
| **23** | Research `R1` to `R4` - the corpus conventions, the rules register, the external principles and the prior art | 5 | 153,311 | `PR-111`, `PR-112`, `PR-113` | none: all five read whole, and the subject re-measured to the plan's 153,311 bytes **exactly** - `PR-101` does not name this cycle among its six drifted rows and it was right not to. **What the instruments decided, rather than that they ran** (`AM-11`): `python tools/docs/refcheck.py` returns *3917 document pointer(s) checked, 0 broken* and *993 link label(s) name a path, 0 disagree*, which covers every pointer and every path label in these five files. `python tools/docs/figures.py` returns *0 stale figure(s)* - and **no research document is in its artifact manifest at all**, so it watches no figure here and its zero is a statement about its own scope rather than about this subject (**L-114**). **So the instruments decided the pointers and none of the arithmetic, and all three findings are counts taken by hand.** **Three checks passed and one of them is the strongest result in the cycle.** [`R4`](research/R4-prior-art.md)'s provenance partition is exact in every direction: 154 rules marked `O` 86, `I` 42, `D` 22, `O/S` 4; its fourteen-section cluster table sums to those four totals in all four columns; and **every section's row total equals that section's rule count in the register** - `M` 11, `A'` 11, `B` 25, `F` 22, `E` 16, `H` 7, `K` 7, `G` 12, `C` 14, `D` 7, `A` 10, `L` 6, `J` 4, `I` 2, summing to 154. A hand-kept 154-row partition in step across two documents and four columns is the opposite of what this cycle found one file over. [`R3`](research/R3-exemplar-decks.md)'s three headline counts are exact - 14 archetypes, 5 presenter-carried exclusions, 12 anti-patterns - and its *R1 section 10 names 13 layout archetypes* is exact too, at 7 plus 6 across the two corpus specs. **One thing was checked and noted rather than raised** (`AM-12`). [`R3`](research/R3-exemplar-decks.md)'s bottom line and its section 5 both say **six** corpus archetypes match external practice, and no reading of the table under it produces six: eight rows carry an R3 archetype - five graded *No, shared ancestry* and three *Yes* - and a ninth carries none. The number may be counting corpus archetype *names* under a pairing this cycle could not reconstruct, and the conclusion it supports - *only three overlaps are real* - is unaffected either way, so it is left here for cycle 41 rather than given a row (**L-86**). **Two findings are recorded against other cycles** (`AM-09`): `PR-111`'s second home is [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md), which is cycle 20's file, and `PR-112`'s whole subject is [`../CLAUDE.md`](../CLAUDE.md) rule 1, which is cycle 3's - a cycle that read `CLAUDE.md` and `BRIEF.md` together and checked four other claims across them | 2026-08-28 |
| **24** | Research `R5` to `R9`, upstream, sketches, adopter reports | 38 | 305,209 | `PR-114`, `PR-115`, `PR-116`, `PR-117`, `PR-118` | none: all 38 read whole, the sketch as its title, description and labels rather than its path data (**AM-10**), and the subject re-measured to the plan's 38 files and 305,209 bytes **exactly** - `PR-101` names this cycle among its six drifted rows and both cells were re-printed at cycle 22, so the row and the command now agree. **The cycle re-read its brief against its membership before running it - `AM-08`'s remedy - and two of the brief's four clauses had no subject.** *The three upstream reports* is four documents: `taskmd-audit-method.md` was added on 2026-08-23 by this run itself, which is `AM-08`'s cycle-14 case exactly, and it was read whole and produced nothing. *What T-225 decided about each* has no subject at all - [T-225](../tasks/T-225-triage-the-claimai-adopter-report.md) is `proposed` at phase `specify` and has decided none of the twenty-seven. Recorded rather than corrected: editing the brief cell after the cycle ran would be rewriting the question to fit the answer. **What the instruments decided** (**AM-11**): `python tools/deck/check.py examples/portfolio-review/portfolio-review.html --sources examples/portfolio-review/sources` exits 0 with `DS-218` reading `False (present: True, 0 looping)` and **passing**, against `True` with 1, 3 and 1 looping motions on the other three shipped decks - so adopter record `018`'s central claim reproduces and the shipped example satisfies the rule vacuously. That is cycle 17's subject and is not raised here (**AM-09**); it is the evidence its triage will need. **Every adopter claim naming a file, a line or a symbol was re-run and all reproduce, line numbers included**: `deck.js` has zero modifier-key tests, `density.py:179` is the `tag[:-1]` splice, `spec.py:103` splits on both separators, `render.py:597` and `:602` are the two seek branches, `components.css` carries no `.doc .sources-item` rule, and `T-198`, `L-01` and `DESIGN-RATIONALE.md` section 5.7 all exist and say what is cited. **Four further checks passed.** The handoff register's disposition holds - all seven `O-H` rows landed and the installed core carries them by name: section 4's dated `O-H4` correction, section 6.2's arrival check, section 3's *derived values decay*, section 3a's *a floor, never a ceiling*, and section 1's *never delete one*. `AUDIT-METHOD.md`'s *`pre-release-audit.md` ships in taskmd `0.6.0` at 11,371 bytes* is exact. R6 section 8's nine glitch-free conditions are `GF-1` to `GF-8` in the tool and the ninth is the person, which is what row 9 of that table says it is. And R5's `anime.js` recommendation is **unexercised rather than contradicted** - it is vendored in no shipped artifact, and the recommendation was conditional on a deck asking for orchestrated motion. **R9's two deck denominators have drifted** - 396,062 and 313,926 against 397,867 and 314,405 today - by under half a per cent, which moves none of the four ratios it states, and it is `PR-113`'s mechanism rather than a second row | 2026-08-28 |
| **25** | Memory and the handoff record - 42 memory entries plus the index, and 98 archived handoffs | - | - | `PR-119`, `PR-120`, `PR-121` | none, and no tracked file was read: **the first cycle whose subject is outside git, and the membership instrument says so rather than answering.** `python tools/docs/cycles.py --cycle 25` exits 0, names no tracked file, and prints the stated reason its `unstated()` check requires; the partition over the tracked tree still holds, because the complaint is silent. But nothing enumerates *this* cycle's subject and nothing fails when an entry is added to it, which is `AM-01`'s remedy meeting the half of the scope git cannot reach - recorded upstream as `AM-13` rather than as a finding here. **`AM-08` ran first and one of the brief's four clauses has no subject**: *the handoff config* is `.handoff/config.md`, which `cycles.py` assigns to cycle 5 and cycle 5 read on 2026-08-23. Recorded rather than corrected, on cycle 24's precedent; it was read as context anyway and produced nothing, every key resolving - the `local-markdown-dir` binding is installed, and `tracker_dir`, `tracker_template`, `tracker_lint` and [T-222](../tasks/T-222-derive-the-reconcile-sweeps-membership-instead-of-enumerating-it.md) all exist. **What the instruments decided, rather than that they ran** (`AM-11`) - three written for this cycle and thrown away after it (**L-04**). *The index against its entries, both directions*: **42 bullet lines, 42 files**, no missing target, no unindexed file, no duplicate, every `name:` equal to its filename and every `type:` one of the schema's four. Two `[[wikilinks]]` resolve to no entry and **both are correct** - `verify-in-the-delivery-environment` is the forward marker the store's own convention allows, and `[[link]]` is that convention being quoted. *Named files*: **63 path-like tokens** pulled from the 42 bodies and tested against `git ls-files` and the working tree - every token naming something in this repository resolves, and each bare tool basename (`check_all.py`, `contents_bound.py`, `figures.py`, `findings.py`, `quickview.py`, `render.py`, `shell.py`) resolves to exactly one tracked file; the remainder are the corpus, other clones, URLs and file extensions. *Named ids*: **15 cited across the store** - `T-nnn`, `L-nn`, `DS-nnn`, `CE-nn` - resolved against 225 task files, 137 lessons, 177 `DS` rows and 13 `CE` rows, and **all 15 resolve**. *The archive*: **1,348 relative pointers across 98 files, 1 broken**, which is `PR-121`. **So the brief's *a named file, function or flag that no longer exists* came back clean on the first word and produced both of its other findings on the other two** - `PR-119` is a function and `PR-120` a claim about a passage. **Four checks passed and are worth recording.** No archived handoff carries the core's `## Untracked specifics` section, so the one shape a handoff may hold with no durable home has never been passed on here, across 98 records; and not one is `discarded_`, so every handoff written was consumed. `.handoff/config.md`'s `docs/**/*.md` pattern is doing exactly what its own dated note predicted - `docs/` held four subdirectories when that note was written on 2026-08-23 and holds five today, `docs/adopter-reports/` having arrived on 2026-08-28, and the sweep covered it with no edit. `looking-at-rendered-output`'s `.assets-cache/` is genuinely in `.gitignore`, and **L-07** still says the standard-library rule it is cited for. And `check_all.py` still runs `tools/tasks/lint.py` as its first step, which is `freeze-the-tree-before-the-evidence-run`'s ordering claim re-run rather than cited. **Two things were checked and noted rather than raised** (`AM-12`). `simulated-surface-is-a-different-surface` is very nearly [`lessons/L-76.md`](lessons/L-76.md) - four of its five facets are in that lesson, the quoted *do not discharge printing and looking* included, which is **L-76** point 2 and not a claim about `contents_bound.py`'s text today. It survives the store's own spent test on its fifth facet, the generalisation past this project, so it is that test **passing** rather than a finding. And the archive carries three naming forms - 65 timestamped, 30 letter-suffixed, 3 word-suffixed - which puts name order out of step with write order at **5 of 97 adjacent pairs**; the handoff core's section 1 makes an archive a record and forbids relocating one, so no remedy exists short of rewriting 33 files that nothing reads by name. **One constraint was met while writing this cycle up and is worth the sentence**: `PR-121` quotes a dead pointer, and both `taskmd check` and `refcheck.py` bind on the path string rather than on the link syntax - so the row failed the gate twice, as a link and then as inline code, and the citation had to become a bare slug. `refcheck.py` has a skip list and using it for one citation would cost more than the wording did, so this is recorded rather than raised | 2026-08-28 |
| **26** | The untracked surface - what `.gitignore` hides, and `.claude/settings.local.json` | - | - | `PR-122`, `PR-123` | none, and no tracked file was read: **the second and last cycle whose subject is outside git**, and `python tools/docs/cycles.py --cycle 26` exits 0 with the reason *outside git by definition - a tracked path here would be a contradiction in the cycle's own subject*. `AM-13`, written at cycle 25, is this cycle's condition too and is not raised again (`AM-12`). **`AM-08` first: the brief re-read against the membership, and all four of its clauses have a subject** - the first cycle in three where that is true. **What the instruments decided, rather than that they ran** (`AM-11`), all three written for this cycle: `git status --ignored=matching` reports **116 ignored entries**, of which 98 are the handoff archive cycle 25 read and are not re-read here (`AM-09`), leaving 18 - nine `__pycache__` directories, three presenter builds, two PDFs, `.kb/`, `.assets-cache/`, `.claude/settings.local.json` and `.claude/worktrees/`. `git check-ignore -v` was run on each of the seven distinct kinds and **named three different source files**, which is `PR-122`. And each of the ten `.gitignore` rules was tested against the working tree *and* against the whole history: **eight match something, two have never matched anything**, which is a third of `PR-123`. **The brief's three questions, answered in order.** *Anything that should be tracked*: **nothing**. `.kb/` is 7 files and 141 KB of corpus extraction, `.assets-cache/` is 1,623 files and 170 MB of font and probe caches, the three presenter builds are [T-211](../tasks/T-211-scope-speaker-notes-and-decide-what-ds-088-becomes.md)'s by design and the two PDFs are R6's - every one of them correctly local. *Anything that must never publish*: **nothing new**. `.kb/` holds the corpus extraction `tools/kb/extract.py` writes and is ignored by the tracked `.gitignore` rather than by either off-repo source, so the rule that matters most is the one that ships; whether tracked files reproduce corpus content is cycle 18's sanitisation check, which came back zero on all seven classes and is not re-run here (`AM-09`, `AM-12`). *Anything left behind by a tool that no longer runs*: **three, and they are `PR-123`**. **Three checks passed and are worth recording.** There is no `reference-deck-presenter.html` and that is correct rather than missing - `tools/deck/presenter.py` takes a `.slides.md` and the reference deck is hand-built, so three presenter builds for four decks is the tool's own arithmetic. `.claude/worktrees/` exists and is **empty**, so the off-repo rule protecting it is currently protecting nothing, which is why `PR-122` is written about where the rules live rather than about a leak. And no second `.gitignore` sits anywhere in the tree: `git ls-files '*.gitignore'` returns the one file, so the tracked half of the rule set is genuinely one document | 2026-08-28 |
| **39** | The figure and version sweep - every number, count and version string in the tree against what the tree is | - | - | `PR-124`, `PR-125` | none, and no tracked file was read: **the third cycle with no membership by construction, and the first whose brief says why**. `python tools/docs/cycles.py --cycle 39` exits 0 and names no file - *synthesis, over material cycles 0 to 38 have already read* - so the subject is the whole live tree and the cycle built its own instruments. **Scripted first, then read, as the brief asks - and the first script was wrong in the way this project has a lesson about.** Bound on vocabulary, a bare numeral matches `DS-025`, `T-041` and `L-43`: 280 hits for one figure. Rebound on **structure** - a numeral governing a countable noun - it returns **28 hits across 277 live files**, and the partition is the point: **6** are code strings rather than claims (`flex:0 0 auto`, a print label), **1** is correct and my filter compared it to the wrong field, **6** are history written as history, **12** already carry a row (`PR-10`, `PR-12`, `PR-32`, `PR-103`), **1** is a dated provenance note in `check.py`, and **2** are new. `figures.py` decided the fenced ones: **exit 0, 0 stale, one floor block grown**, which is that category working. **Neither finding came from an instrument, and that is the brief's fourth clause**: `PR-125` is in a `.py` file, which `DECLARED_DOCS` does not reach, and `PR-124` is L-05's family - a sentence about a tracked state with no numeral in it at all. **The second new hit was not raised** (`AM-12`): `ruleset.py` line 237 states 43 `judge` rules against 48, which is a third instance of `PR-47` and is recorded on that row. **Five checks passed.** `plugin.json`, `marketplace.json`, the `v0.6.0` tag and the README's *The current release is 0.6.0* agree, and no live document states a version in the present tense that is not `0.6.0`. Tier 1's own bound - the only self-measured figure `figures.py` watches - is exact at 15,572 against 13,324. `reference/` is 1,196 bytes against `CLAUDE.md`'s *one 1.2 KB file*. The ruleset partition closes: 130 plus 42 plus 5 is 177 rows, 178 with `DS-000`. And the README's *29 `hard` rules that need a person* is exact, three lines from the two figures `PR-10` names as wrong. **`AM-08` found all four of the brief's clauses with a subject**, the second cycle running. **The run's own record was corrected rather than raised**, on cycle 7's precedent. [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 3's counts table read Medium **69** raised and **67** open where the register held **68** and **66** - it counted the withdrawn `PR-66` as raised and never took it back out of open. `PR-124` makes the raised cell right by arithmetic, which is why an uncorrected table would have looked maintained; the open cell was the one that had to move, and it moves back to 67 with `PR-124` in it. `PR-14`'s status cell gave the tier-2 floor as 5,024 bytes and tier 1's debt as 10,548; [`AUDIT-METHOD.md`](AUDIT-METHOD.md) is **5,762** and the debt is **9,810**. And section 1 gave stage 7 as **47% of the reading** - the figure the owner's checkpoint rests on. The 3,366,873 bytes are exact; the plan now totals 9,359,208 over 532 files, so stage 7's 219 files are **36%**. **Nothing in stage 7 moved and the denominator did**, cycle 7 to 420,677 and 88,610 bytes of adopter report on 2026-08-28: a share falls while its subject stands still, which is the one direction nobody re-checks and the shape none of this cycle's instruments would have caught | 2026-08-29 |
| **27 to 33, 35 to 38** | The closed record - `PH1`, `PH2`, `PH3`, `WP1`, `WP2`, `WP3` and the two cancelled stubs | 194 | 3,065,032 | none | **Skipped, by the owner's checkpoint of 2026-08-29** - the partition's third bucket, stated here so 194 files are not a gap in it ([`AUDIT-METHOD.md`](AUDIT-METHOD.md) section 2). The decision was deferred from cycle 26 to be taken with cycle 39's result, and 39's result is what decided it: both of its findings were in prose no instrument binds, and stage 7 is 3,366,873 bytes of exactly that about work that has already shipped. **Cycle 34 is run and the rest is not**, because 34 holds the 25 records `0.6.0` shipped and live documents cite them; the other eleven cycles are the oldest and least cited, and by this programme's own words a contradiction in them cannot reach the release. **What the skip costs is written down rather than assumed**: any claim in these 194 files that a live document repeats is still reachable through the live document, which stages 1 to 6 and 39 have read; what is lost is a contradiction that exists only in a closed record and is quoted nowhere. Cycle 41 re-reads whatever the remedies break, so the skip is not load-bearing on the run's closing claim | 2026-08-29 |
| **34** | `PH3` closed, `T-187` onward - the records `0.6.0` shipped, and the only part of stage 7 a live document cites | 25 | 301,841 | `PR-126`, `PR-127`, `PR-128` | none: `python tools/docs/cycles.py --cycle 34` names 25 files and all 25 were read | 2026-08-29 |
| **40** | Triage, rank, raise the tasks | - | - | none raised; **one withdrawn** (`PR-96`) and **28 child tasks raised** | none, and no tracked file: `python tools/docs/cycles.py --cycle 40` names none - *synthesis over the register, not over the tree* | 2026-08-29 |

---

## 3. The ranked findings

Ranked by severity, then by the reader's cost. A closed row keeps its id and its statement; its rank
cell is struck through rather than deleted.

| # | Aspect | Sev | Finding | Evidence | Where the fix lives | Remedy — a hypothesis | Effort | Task | Status |
| :-- | :--- | :---: | :--- | :--- | :--- | :--- | :---: | :--- | :--- |
| `PR-01` | project method | ~~**Med**~~ | The plan counts the blindness fixture among *the five shipped decks* and hands it the deck gates as its instrument. `examples/reference-deck-seeded-defects.html` carries one seeded defect per evaluation dimension **on purpose**, at score 0; `check_all.py` classifies it `NOT_A_DECK` and gates it with `seed_defects.py --check`. Cycle 17 as written renders a fixture built to fail and reads the reds as findings. *Six* `.html` files are tracked, not five — `shell/shell.html` is the other exclusion. **Re-measured at cycle 14 against a tool that did not exist at cycle 0**: `tools/docs/cycles.py` - the rule table that now decides which files a cycle reads - states cycle 17's subject as *The shipped decks and the blindness fixture*, which is right. So three documents disagree with the membership rather than with a classification, and the correct wording is already written down in the one place that is derived | `python -c "import importlib.util;s=importlib.util.spec_from_file_location('c','tools/check_all.py');m=importlib.util.module_from_spec(s);s.loader.exec_module(m);print(len(m.DECKS),sorted(m.NOT_A_DECK))"` prints `4 ['examples/reference-deck-seeded-defects.html', 'shell/shell.html']`, and `git ls-files '*.html'` lists six. The 1,773,568-byte figure is reached **only** by counting the fixture; `python tools/docs/cycles.py --cycle 17` names the five and its own subject line names the fixture separately | [`AUDIT-METHOD.md`](AUDIT-METHOD.md) — section 2, and [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) — section 1's grade table and section 2's cycle 17 | Cut cycle 17 to the four decks `DECKS` names, and give the fixture a row of its own instrumented by `seed_defects.py --check`. **A hypothesis**, per the skill's `pre-release-audit.md` section 5: the fixture may still be worth looking at, and its instrument may be the rubric rather than the deck gates — whoever implements this measures before committing to it | `xs` | [T-224](../tasks/T-224-give-the-blindness-fixture-its-own-instrument-in-cycle-17.md) — raised and closed ahead of cycle 40 on this row's own clause; `parent:` is the link, per section 3's decision in T-219 | **closed 2026-08-23.** Half the remedy was refused after measurement: *a row of its own* is a cycle number, and 99 citations of cycles 18 to 43 sit in tracked files, 85 of them `cycle 40`. The membership `cycles.py` derives is kept; the **instrument** is split instead, which is the half that was wrong — the four decks keep the deck gates, the fixture gets `seed_defects.py --check` and nothing else |
| `PR-02` | product documentation | **High** | **A shipped deck is in neither human-facing document.** `examples/portfolio-review/` is tracked with its full specification pair, its sources and a per-deck line in `check_all.py`'s `DECKS`, and it is 397,867 bytes of gated deck. [`../examples/README.md`](../examples/README.md) opens *"Four decks."* and its table lists three decks plus the seeded-defects fixture — which the same table calls *"A test fixture, not an example to copy"*. [`../README.md`](../README.md)'s *What is actually here* table sends the reader there for **"Every shipped deck, with what was measured on each"**. That is a completeness claim the tree does not support, and `0.6.0` shipped with it already false | `grep -c portfolio README.md examples/README.md` prints `0` for both. `git ls-files examples/portfolio-review/` lists five tracked files. `figures.py` names three decks in its artifact manifest and not this one, so none of its properties is watched by any document | [`../README.md`](../README.md)'s *What is actually here* table, and [`../examples/README.md`](../examples/README.md) | Give the deck a section in `examples/README.md` and an artifact-manifest entry, and correct the front page's row. **A hypothesis**: the deck is `0.6.0`'s chart-engine example ([T-113](../tasks/T-113-evaluate-an-embeddable-chart-library-against-hand-authored-svg.md)) and the front page describes that feature without pointing at it, so the section may be worth writing from the feature's end rather than the deck's | `s` | [T-226](../tasks/T-226-a-shipped-deck-is-in-neither-human-facing-document.md) | **closed 2026-08-30.** The remedy's hypothesis held and was taken: the section is written from the feature's end - `T-113`'s question about a chart library - rather than from the deck's topic. **One thing was added that the remedy did not name**, because a section written today does not stop the next deck arriving unwritten: the count is now held to `DECKS` by a check, `decks_not_in_examples_readme()` in `check_all.py`, so a deck this repository ships and does not describe fails the run. The opener also counted the **fixture** as one of its four decks, which this row quotes without naming - corrected to *four decks and one test fixture* |
| `PR-03` | product documentation | **Med** | [`../README.md`](../README.md)'s second paragraph says the repository holds *"plus two decks built strictly to them"*. Four decks ship. The same page's own table implies three — *"the hand-built one above, one assembled through build mode, one an adopter built elsewhere, and a fixture"* — so the front page disagrees with itself two screens apart. Section 2's plan predicted *it says two decks and three are shipped*; the gap has widened by one release since | `python -c "...print(len(m.DECKS))"` prints `4` (the command in `PR-01`'s row). `README.md` line 8 | [`../README.md`](../README.md) | One number. **A hypothesis**: *two* may have meant *built in this repository*, which is now three, so the sentence may need rewording rather than recounting | `xs` | [T-234](../tasks/T-234-the-front-pages-own-figures-and-claims.md) | open |
| `PR-04` | product documentation | **Med** | **A wrong figure on the front page, in the one place the figure watcher does not look.** [`../README.md`](../README.md) line 206 states `sort-window` is **307 KB**; it is **305 KB**, 312,384 bytes, which is what [`../examples/README.md`](../examples/README.md) line 348 correctly says. 307 KB is the *reference deck's* figure. The durable half is the mechanism: `figures.py` binds `sort-window`'s KB claim to exactly one document and it is the correct one, so the front page's instance is bound to nothing and can drift with every rebuild | `python tools/docs/figures.py --values` reports `sort-window KB 1` against `reference-deck KB 2`, and `0 stale figure(s)` while line 206 is wrong. `stat -c %s examples/sort-window/sort-window.html` prints `312384` | [`../README.md`](../README.md), and the artifact manifest in [`../tools/docs/figures.py`](../tools/docs/figures.py) | Correct the value **and** bind the instance, not one or the other — fixing only the number leaves the next rebuild free to falsify it again. This is the class [T-060](../tasks/T-060-check-that-the-readmes-pasted-figures-still-match-their-commands.md) exists to prevent | `xs` | [T-234](../tasks/T-234-the-front-pages-own-figures-and-claims.md) | open |
| `PR-06` | project method | ~~**Med**~~ | **The run's own coverage does not add up, and the plan cannot say where.** [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 1's grade table gives the tree as **492 files, 8,786,384 bytes**; section 2's thirty-seven sized cycles sum to **491 files, 8,769,356 bytes**. The six unsized cycles cannot absorb the difference — 25 and 26 examine surfaces outside git and add no tracked file, and 39 to 42 are synthesis over material other cycles have already read. **No single tracked file is 17,028 bytes**, at the plan's commit or today, so this is one unreconciled measurement rather than one omitted file. The sharper half: section 2 states a *count* per cycle and never a *membership*, so the difference cannot be located from the document at all — only re-derived | The sum is computed from the task record itself rather than transcribed: parse section 2's six-cell rows, keep the numeric ids, total the Files and Bytes columns. It prints `491` and `8,769,356` against section 1's `492` and `8,786,384`. `git ls-tree -r -l 'HEAD@{2026-08-22}'` filtered to size `17028` returns nothing, and so does the same filter over the tree today | [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 2's cycle programme, and section 1's grade table | **A hypothesis, and the project has learned this shape twice already**: do not hand-reconcile the two tables. Assign every tracked path to a cycle by a rule and let a command decide the partition, the way `check_all.py` already does over `tools/**/*.py` — a tool no table names fails its run. That is **L-135** and [T-222](../tasks/T-222-derive-the-reconcile-sweeps-membership-instead-of-enumerating-it.md) applied to the audit's own boundaries. Whoever implements it measures first: the counts may reconcile once membership is derivable, in which case the defect was only ever that it was not | `s` | [T-223](../tasks/T-223-derive-the-audit-cycles-membership-instead-of-counting-it.md) | **closed 2026-08-23.** Raised ahead of cycle 40 on the owner's instruction, because it gated every remaining cycle rather than only the run's closing claim: section 2's step 2 asks for a file list the document did not hold. `python tools/docs/cycles.py` now assigns every tracked path to exactly one cycle and fails on one that belongs to none; T-219's two tables are printed by it. Raised between cycles 1 and 2 by summing the plan, not by a cycle, so no coverage-ledger row carries it |
| `PR-07` | product documentation | **High** | **The front page offers an adoption route whose mechanism the skill removed, and nothing resolves it.** [`../README.md`](../README.md) says *"Copying `skills/htmldeck/` into a plugin of your own works as well. Every path the skill resolves goes through `${CLAUDE_PLUGIN_ROOT}`, which is what lets a copied directory find its own references."* **No path in the skill goes through that variable.** All 22 go through `$HTMLDECK`, and [`../skills/htmldeck/SKILL.md`](../skills/htmldeck/SKILL.md) section 0 says in bold not to write the variable in a command, because [T-189](../tasks/T-189-resolve-the-plugin-root-in-every-documented-command.md) measured it expanding to nothing. Section 0 resolves `$HTMLDECK` two ways - an installed htmldeck plugin, or a clone - and **a copy inside somebody else's plugin is neither**: the discovery glob requires a directory named `htmldeck`, and their plugin is not called that. So the route the front page offers has no resolver, and the failure is silent, since a bare path resolves against the adopter's own project | `grep -c 'CLAUDE_PLUGIN_ROOT}/' skills/htmldeck/references/*.md` prints `0` for all four; the only occurrence in the whole skill is the counter-example inside `SKILL.md`'s own prohibition. `grep -rho '[$]HTMLDECK/[A-Za-z0-9_./-]*' skills/ ; sort -u` lists 22 targets, and every one of them resolves today | [`../README.md`](../README.md)'s install section, and [`../skills/htmldeck/SKILL.md`](../skills/htmldeck/SKILL.md) section 0 | Two halves, and **neither alone**. Correct the front page to name the discovery command rather than the variable, **and** give section 0 a third branch for a copied directory. **A hypothesis**: a skill file may be able to locate itself from its own path, in which case the third branch is one line and the other two become fallbacks - whoever implements this measures whether a run can actually see it, because if it cannot, the honest fix is to withdraw the copy route rather than document it harder | `s` | [T-227](../tasks/T-227-the-front-pages-adoption-route-names-a-variable-the-skill-removed.md) | open |
| `PR-08` | project method | **Med** | **Two bare `python tools/...` commands survive in skill documents, and the check written to catch them cannot see them.** [`../skills/htmldeck/references/artifacts.md`](../skills/htmldeck/references/artifacts.md) line 129 and [`../skills/htmldeck/references/build.md`](../skills/htmldeck/references/build.md) line 77 both write a command with no `$HTMLDECK` in front of it, so both resolve against the adopter's project - T-189's defect class, which `0.5.0` existed to remove. **The durable half is the mechanism**: [`../tools/plugin/check_scaffold.py`](../tools/plugin/check_scaffold.py) rule 4 exists for exactly this, and its `BARE_RE` requires the backtick to be immediately followed by the path, so a span opening `python tools/...` slips past. Its rule 5 then reports *every pointer resolves* over a set of **one** - the counter-example in `SKILL.md` - while 22 real ones are watched by nothing | `grep -rn 'python tools/' skills/` prints the two lines. `python tools/plugin/check_scaffold.py` exits 0 and prints *every ${CLAUDE_PLUGIN_ROOT} pointer resolves* on the same tree | [`../skills/htmldeck/references/artifacts.md`](../skills/htmldeck/references/artifacts.md), [`../skills/htmldeck/references/build.md`](../skills/htmldeck/references/build.md), and [`../tools/plugin/check_scaffold.py`](../tools/plugin/check_scaffold.py) | Fix the two commands **and** widen the check to the paths actually written, or the next one is unwatched too - this is `PR-04`'s shape a second time. **A hypothesis**: the check may be better keyed on *a command naming a repository tool without a base* than on the variable's name, since the variable is now the thing the skill forbids | `xs` | [T-235](../tasks/T-235-the-skills-documents-against-the-tools-they-describe.md) | open |
| `PR-09` | product documentation | **Med** | **The three documents that specify a slide's fields do not agree how many there are, and the gate takes the larger side.** [`../skills/htmldeck/references/pipeline.md`](../skills/htmldeck/references/pipeline.md) line 101 says *Seven fields per slide* and lists them; [`../skills/htmldeck/references/artifacts.md`](../skills/htmldeck/references/artifacts.md) line 104 says *Nine fields per slide, and an optional tenth* and its template carries nine; [`../skills/htmldeck/references/build.md`](../skills/htmldeck/references/build.md) line 9 says nine. The two `pipeline.md` omits are **Archetype** and **Sources**, and `spec.py`'s `SPEC-1` gates *every slide answers Sources*. **`pipeline.md` holds the wrong count in the more authoritative place**: `SKILL.md` loads it first on every build run, and its line 101 is stage 4's own instruction - the stage that writes the file | `grep -n 'fields per slide' skills/htmldeck/references/*.md` prints seven, nine and nine. `grep -n 'SPEC-1' tools/deck/spec.py` shows the gate requiring the field | [`../skills/htmldeck/references/pipeline.md`](../skills/htmldeck/references/pipeline.md) stage 4 | One count and two field names, in `pipeline.md`, which is the file that disagrees with both others and with the gate. **A hypothesis**: *seven* may be older than `Sources` and `Archetype` rather than a miscount, in which case the fix is to point at the template instead of restating a list that has to be kept in step - which is why it drifted | `xs` | [T-235](../tasks/T-235-the-skills-documents-against-the-tools-they-describe.md) | open |
| `PR-10` | product documentation | **Med** | **The critique document states the hard-rule count twice, ten lines apart, and both figures are wrong.** [`../skills/htmldeck/references/critique.md`](../skills/htmldeck/references/critique.md) line 29 says *the 25 `hard` rules on the worksheet* and line 46 says *26 `hard` rules that no mechanical gate can reach*. The generated worksheet carries **29**, which is what [`../README.md`](../README.md) line 236 correctly says. **The durable half is again the binding**: `figures.py`'s `DECLARED_DOCS` names five documents, and of the six files a build loads only `pipeline.md` is among them - so `SKILL.md`, `artifacts.md`, `build.md` and `critique.md` may state any figure at all and nothing compares it to anything. `PR-04` is the same mechanism on the front page; this is it across the operative half of the plugin | `python tools/deck/critique.py examples/reference-deck.html --worksheet` writes a sheet whose answer lines carry 29 distinct rule ids. `python tools/docs/figures.py --values` reports the 29 bound to `ruleset.py --gates`, and `sed -n '111,117p' tools/docs/figures.py` prints the five declared documents | [`../skills/htmldeck/references/critique.md`](../skills/htmldeck/references/critique.md), and `DECLARED_DOCS` in [`../tools/docs/figures.py`](../tools/docs/figures.py) | Correct both numbers **and** declare the four unwatched skill documents, or the next release moves the count again. **A hypothesis**: the two figures may be reachable by pointing at the worksheet rather than counting it, which removes the instance instead of binding it - cheaper, and the reason `PR-04`'s row says fixing only the number leaves the next rebuild free to falsify it | `xs` | [T-235](../tasks/T-235-the-skills-documents-against-the-tools-they-describe.md) | open |
| `PR-11` | project documentation | **Med** | **Tier 1 states the shipped decks' size, and it is wrong by one deck and 640,565 bytes.** [`../CLAUDE.md`](../CLAUDE.md) rule 6 reads *"the three shipped decks are 810,746 bytes, and a question about what is inside one is answered by a tool"*. **Four decks ship**, and they total **1,451,311**. The rule's argument is untouched - it gets stronger, not weaker - but the figure a reader is handed to justify it is false, in the one file every session of every project pays for before it does anything. It is the **third** place the deck count has been found stale: `PR-02` and `PR-03` are the front page's, and this is tier 1's | `python -c "import subprocess,os,importlib.util;sp=importlib.util.spec_from_file_location('c','tools/check_all.py');m=importlib.util.module_from_spec(sp);sp.loader.exec_module(m);print(len(m.DECKS),sum(os.path.getsize(x) for x in m.DECKS))"` prints `4 1451311`. `grep -n '810,746' CLAUDE.md` prints the claim | [`../CLAUDE.md`](../CLAUDE.md) rule 6, and `DECLARED_DOCS` in [`../tools/docs/figures.py`](../tools/docs/figures.py) | Correct the figure **and** bind it. `CLAUDE.md` is already in `DECLARED_DOCS`, for the *coverage split* - so the file is watched and this sentence is not, which is the same half-binding as `PR-04`. **A hypothesis**: rule 6's point is that a deck is too big to read, and a bound (*over 300 KB each*) may serve it better than a total that moves whenever any deck is rebuilt | `xs` | [T-236](../tasks/T-236-tier-1-and-the-brief-against-what-they-measure.md) | open |
| `PR-12` | project documentation | **Med** | **The brief's ruleset figures have been stale since fourteen rules were added, inside the paragraph that says to re-derive them.** [`../docs/BRIEF.md`](../docs/BRIEF.md) states **163** rule rows and **164** counting `DS-000`, `Check` as `auto` (68) - `render` (45) - `judge` (43) - none (7), and `Reach` as `yes` (109) - `off-gate` (3) - `never` (1) - `—` (50). Today the ruleset is **177** and **178**, `auto` 75, `render` 47, `judge` 48, `—` 7, `yes` 119, `off-gate` 3, `never` **0**, `—` 55. **Seven of the ten are wrong**, and the same paragraph carries the instruction *Counts are derived and go stale when a rule is added - re-derive, never adjust by hand*, plus a dated note recording that these very figures went stale twice in one day. [`../docs/EVALUATION.md`](../docs/EVALUATION.md) line 90 carries the same `164 as of 2026-08-09` and the same instruction; it is **cycle 8's file** and is recorded here so cycle 8 does not raise it twice | `python tools/deck/ruleset.py --counts` prints every current figure. `grep -n '163 rules' docs/BRIEF.md ; grep -n '164 rules' docs/EVALUATION.md` prints the stale pair in both | [`../docs/BRIEF.md`](../docs/BRIEF.md)'s three-documents table, and [`../docs/EVALUATION.md`](../docs/EVALUATION.md) | **A hypothesis, and the instruction is the evidence against the obvious fix**: *re-derive, never adjust by hand* was written here and followed by nobody for a fortnight, so correcting the ten numbers restores exactly the state that decayed. What may serve is deleting them and pointing at `ruleset.py --counts`, which is what these two documents already tell the reader to run | `s` | [T-236](../tasks/T-236-tier-1-and-the-brief-against-what-they-measure.md) | open |
| `PR-13` | product documentation | **Med** | **The skill tells an adopter a deck is under 200 KB and no deck this repository ships is.** [`../skills/htmldeck/SKILL.md`](../skills/htmldeck/SKILL.md) says *"Not a size problem - a full deck with three embedded faces is under 200 KB"*, and [`../docs/BRIEF.md`](../docs/BRIEF.md)'s *Delivery mode* row says `portable` is *~190 KB typical*. The four shipped decks are **314,405 - 312,384 - 426,655 - 397,867**, averaging 363 KB, and the smallest is 1.56x the stated typical. **The instructive half is the third instance**: [`../CLAUDE.md`](../CLAUDE.md) rule 1 states the same measurement as *Measured and settled 2026-08-06 ... is 192 KB*, and that sentence is still true, because a dated measurement is history and a live typical decays - **L-95** point 4, with all three instances inside one release | `for d in examples/reference-deck.html examples/sort-window/sort-window.html examples/measure-first/measure-first.html examples/portfolio-review/portfolio-review.html; do stat -c '%s %n' $d; done` prints the four | [`../skills/htmldeck/SKILL.md`](../skills/htmldeck/SKILL.md), and [`../docs/BRIEF.md`](../docs/BRIEF.md)'s *Delivery mode* row | Restate both as what `CLAUDE.md` already does - a dated measurement of a named artifact - rather than as a property of decks in general. **A hypothesis**: the *Delivery mode* decision rests on *embedding is cheap*, and it survives at 363 KB, so this is a wrong number under a right decision and the fix is the sentence, not the decision. The `SKILL.md` instance was in **cycle 2's** subject and that cycle did not catch it; it is recorded here rather than left to the re-read at cycle 41 | `xs` | [T-235](../tasks/T-235-the-skills-documents-against-the-tools-they-describe.md) | open |
| `PR-14` | project method | **Med** | **The tier-2 set is an enumerated list, and a document that meets its own test is not in it - so tier 1's budget has no decided value.** [`../CLAUDE.md`](../CLAUDE.md) gives two tests for tier 3 and they disagree: *what tier 2 loads one at a time* admits a method document, and *never to start work of a kind* excludes it. [`../docs/AUDIT-METHOD.md`](../docs/AUDIT-METHOD.md) is the case that exists - [`../tasks/TASK-WORKFLOW.md`](../tasks/TASK-WORKFLOW.md) section 8 loads it when an audit is asked for, which **is** work of a kind - and it is not among the five files the tier-2 set names. **The consequence is arithmetic, not philosophy**: as a term the bound's floor falls from `TASK-WORKFLOW.md`'s 13,324 to 4,430, and the stated debt rises from 2,248 to **11,142**. Nothing in the file says which test governs, so the budget it exists to enforce has no value. And the list is closed *by design* - which is **L-135**'s shape with the reason for it written in | `python -c "import pathlib;[print(p.stat().st_size,p) for p in map(pathlib.Path,'CLAUDE.md tasks/TASK-WORKFLOW.md docs/AUDIT-METHOD.md'.split())]"` prints 15,572, 13,324 and 4,430. `grep -n 'AUDIT-METHOD' tasks/TASK-WORKFLOW.md` prints the section 8 line that loads it | [`../CLAUDE.md`](../CLAUDE.md)'s tier section | **The owner answers, and the plan already says so** - this is section 1's first open question, assigned to this cycle. **A hypothesis**: the two tests are trying to say one thing, that a tier-2 document is entered *at the start of work* and a tier-3 one *during* it; if that is the rule, `AUDIT-METHOD.md` is a term and the debt is four times what the file admits. The alternative is that the closed list is the rule and the tests are commentary - in which case the file should say the list is closed by decision rather than by test, because a test nobody applies is what let this sit | `s` | [T-236](../tasks/T-236-tier-1-and-the-brief-against-what-they-measure.md) | open, and **the definition is settled**. The owner ruled on 2026-08-23: *a tier-2 document is entered at the start of work of a kind*, so [`AUDIT-METHOD.md`](AUDIT-METHOD.md) **is a term**. The floor is therefore [`AUDIT-METHOD.md`](AUDIT-METHOD.md)'s **5,762** bytes and tier 1's debt is **9,810**, not the 2,248 the file states. *Corrected at cycle 39 from 5,024 and 10,548, which were measured before that day's last edit to the file - the register is this run's own record to keep true.* **The edit is deliberately not made here** - a remedy is a hypothesis and this one changes tier 1, which every session of every project pays for; it lands with the other remedies rather than mid-run. What the ruling removes is the ambiguity, not the work |
| `PR-15` | project method | **Med** | **The repository does not ignore `.claude/`; one machine's global ignore file does.** [`../.gitignore`](../.gitignore) names ten things and `.claude/` is not among them. On this machine `.claude/settings.local.json` never appears as untracked, because the owner's user-level ignore file excludes it - a file that is not in the repository and is not cloned. `.git/info/exclude` carries `.claude/worktrees/` and is machine-local in the same way. **So on any other machine, and in any fresh clone, `git add -A` stages a machine-local settings file into a public repository** - and `../CLAUDE.md`'s publishing constraint is *free of personal, client and machine data*. Nothing in the repository prevents it; the protection is somebody's home directory | `git check-ignore -v .claude/settings.local.json` names the user-level ignore file rather than `.gitignore`. `git -c core.excludesfile=/dev/null check-ignore -v .claude/settings.local.json` exits non-zero: **the repository's own rules do not ignore it** | [`../.gitignore`](../.gitignore) | Add the two entries. **A hypothesis**: `.claude/` may want to be ignored whole rather than by file, since a worktrees directory and a local settings file are both machine state, and the tracked half of that folder is empty here. Cycle 26 examines what the file *contains*; this row is only about what hides it | `xs` | [T-252](../tasks/T-252-the-ignore-rules-come-from-three-files-and-one-ships.md) | open |
| `PR-16` | project documentation | **Med** | **A blank line splits the release-requirements table, so nine of its twelve rows render under a release row used as column headings.** Line 370 of [`../docs/PUBLISHING.md`](../docs/PUBLISHING.md) is empty, between the `0.2.2` and `0.2.3` rows of section 8.1. Markdown ends a table at a blank line, so `0.2.0` to `0.2.2` render as one table and **`0.2.3` becomes the header of a second one** - its four cells drawn as column names, the release itself gone from the body, and `0.2.4` to `0.6.0` listed beneath headings that are prose about `0.2.3`. This is the table step 7 copies into a release note verbatim and the one an adopter reads before upgrading, on a public forge. **Nothing catches it**: a checker counts cells inside a table and a blank line simply ends the table, so the rows after it are still well-formed | `grep -n '' docs/PUBLISHING.md ; sed -n '369,371p'` shows the empty line 370 between two data rows. `python tools/tasks/lint.py` passes on the same file | [`../docs/PUBLISHING.md`](../docs/PUBLISHING.md) section 8.1 | Delete the blank line. **A hypothesis about the durable half**: this is a one-character defect that every gate here reports as clean, and section 8.1 is twelve rows of paragraph-length cells where one more is added at every release - so the check worth having may be *a table interrupted by a blank line*, which is cheap and which no tool in this repository asks | `xs` | [T-237](../tasks/T-237-the-release-machinery-and-its-record.md) | open |
| `PR-17` | project documentation | **Med** | **The release chronology opens by contradicting the table underneath it.** [`../docs/RELEASE-HISTORY.md`](../docs/RELEASE-HISTORY.md) section 1 begins *"Eleven releases over six days"*. The table below it has **seventeen** rows, and the tags run 2026-08-09 to 2026-08-22 - **fourteen days**. The section's own note warns that the two counted **columns** are derived and must be re-derived rather than trusted; the sentence introducing them is outside that warning, and the columns are in fact **exact** - all seventeen `Tasks` figures match the document's own command today. So the maintained half is right and the unmaintained sentence is wrong, which is the reverse of what the warning prepares a reader for. [T-220](../tasks/T-220-derive-the-release-chronologys-task-count-instead-of-typing-it.md) is open against that column and its own text says *seventeen numbers typed by hand* - so the correct count is already written down in a task while the header keeps the wrong one | `git tag -l ; wc -l` prints `17`; `git for-each-ref --sort=creatordate --format='%(creatordate:short)' refs/tags` runs 2026-08-09 to 2026-08-22. `grep -h '^shipped_in:' tasks/*.md ; sort ; uniq -c` matches every row of the third column | [`../docs/RELEASE-HISTORY.md`](../docs/RELEASE-HISTORY.md) section 1's opening sentence | Two numbers. **A hypothesis**: the sentence restates what the table already shows, so deleting it may beat correcting it - it will be wrong again at the next release, and this is the exact class [`../docs/BRIEF.md`](../docs/BRIEF.md) cites as its reason for the reconciliation pass, *a summary paragraph contradicting the table printed directly above it* | `xs` | [T-237](../tasks/T-237-the-release-machinery-and-its-record.md) | open |
| `PR-18` | project documentation | **Low** | [`../docs/PUBLISHING.md`](../docs/PUBLISHING.md) section 6 explains figure binding with *"The gate prints `checked   84`, so a sentence saying `checks` names that field"*. The gate prints **92**. It is an illustration rather than a claim about the ruleset, which is why this is a Low - but it is written in the present tense as what the command prints, inside the section whose whole subject is holding a pasted figure to its command, and `figures.py` reports `0 stale` because this document is not in its declared set | `python tools/deck/check.py examples/reference-deck.html --sources examples/sources` prints `checked               92`. `grep -n 'checked   84' docs/PUBLISHING.md` prints the line | [`../docs/PUBLISHING.md`](../docs/PUBLISHING.md) section 6 | **Batch it** with `PR-05`, which is the other Low in this file. Rewriting the example with no number at all would end the class; `PR-12`'s remedy note argues the same way about the brief's ten | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-19` | project method | **Med** | **The board's own prose routes a new task into a shipped phase.** [`../tasks/README.md`](../tasks/README.md)'s hand-written header says *"**PH2** is the dependencies and every minor and moderate fix; **PH3** is the bigger tasks and the new capabilities, everything estimated `l` or `xl`"*, and names `PH1` as the only phase that has shipped. **That is the rule as it stood before PH2 shipped.** [`../CLAUDE.md`](../CLAUDE.md) and [`../tasks/TASK-WORKFLOW.md`](../tasks/TASK-WORKFLOW.md) §3 both say `PH3` takes anything `l` or `xl` **and, since PH2 shipped, everything else that is not a defect in the published plugin**, because reopening a shipped phase is reserved for those. So an agent reading the board files a small improvement under `PH2` - reopening a shipped phase, which is the exact failure both phase splits exist to prevent. **And nothing corrects it**: this text is outside the generated markers, so `taskmd index` rewrites the tables under it and never the sentence over them | `grep -n 'minor and moderate fix' tasks/README.md` prints the claim; `grep -n 'since PH2 shipped' CLAUDE.md tasks/TASK-WORKFLOW.md` prints the rule in both. No `PH2` task is open: `grep -h '^work_package: PH2' -A0 tasks/*.md ; wc -l` counts 21 and every one is `done` | [`../tasks/README.md`](../tasks/README.md)'s hand-written header | One sentence, in the half of the board a generator never touches. **A hypothesis**: the header restates a routing rule that has two operative homes already, so deleting it and pointing at them may beat correcting it - it went stale precisely because it is a third copy, and the same page's next paragraph already points at `TASK-WORKFLOW.md` for the phase-versus-version rule and is still right | `xs` | [T-238](../tasks/T-238-the-trackers-own-rules-against-its-own-records.md) | open |
| `PR-20` | project method | **Med** | **The opening checklist's first step names a command an agent cannot run, and the substitute refuses it by name.** [`../tasks/TASK-WORKFLOW.md`](../tasks/TASK-WORKFLOW.md) §1 says *`taskmd index` prints the next free number* and §7's opening checklist step 1 says *take the next ID from `taskmd index`*. [`../tasks/TOOLING.md`](../tasks/TOOLING.md) §1 states that the bare `taskmd` command does not resolve in an agent shell and that **two commands cover all four** - but `query.py` **refuses `index` by name**, and `lint.py` runs it and prints only `Wrote tasks/README.md - 3 active, 220 closed`. **The next free number is the one output the substitute pair drops**, so an agent opening a task guesses the id from a directory listing, which is what the printed number exists to prevent. Demonstrated rather than argued: this cycle's own session took `T-223` from `ls tasks/` | `python tools/tasks/query.py index` prints *`index` is not run from here*. `python tools/tasks/lint.py ; head -2` prints the index line with no id in it | [`../tasks/TASK-WORKFLOW.md`](../tasks/TASK-WORKFLOW.md) §1 and §7, and [`../tools/tasks/query.py`](../tools/tasks/query.py) | Two candidates and they are not equivalent. **A hypothesis**: `query.py` could answer *next id* as a third question - it already refuses `index` deliberately, so the refusal message is the place that knows the question is being asked. The alternative is for `lint.py` to surface the line `taskmd index` already prints. Measure which one an agent actually reaches for before building either | `xs` | [T-238](../tasks/T-238-the-trackers-own-rules-against-its-own-records.md) | open |
| `PR-21` | project method | **Med** | **A cycle records three of the four things the method says it names, and the missing one is the instrument.** taskmd's `pre-release-audit.md` §3: *A cycle names what it examines, the brief for that subject, **the instrument**, and what it produced.* [`../tasks/_audit-umbrella-template.md`](../tasks/_audit-umbrella-template.md) carries an `Instrument` column and [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) §2's table does not; this register's coverage ledger never had one either. **It is not cosmetic, and `PR-01` is the proof**: cycle 17 is grade C, which the method defines as *examined by measurement or by use, never by reading*, and its instrument was left inside a prose brief - where it named four deck gates and a fixture built to fail them. A named instrument per cycle is the cell that question would have had to be answered in | The method's §3 is in the installed skill under `skills/taskmd/docs/method/`. `grep -n 'Instrument' tasks/_audit-umbrella-template.md` prints the column; `grep -c 'Instrument' tasks/T-219-pre-release-audit-of-the-whole-repository.md docs/PRE-RELEASE-AUDIT.md` prints `0` for both | [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) §2's table and this register's coverage ledger | Add the column to both. **A hypothesis, and it is a real one**: thirty-eight cycles are already written without it and back-filling thirty-eight instrument cells at cycle 40 is a description of what happened, not a plan - so the honest change may be to add it from the next cycle forward and say the earlier rows carry the instrument in prose. Whoever does it decides which. **Raised at cycle 5 from the template; the two audit documents are cycle 7's files and it is recorded here so cycle 7 does not raise it twice** | `s` | [T-239](../tasks/T-239-the-audits-own-record-against-what-it-did.md) | open |
| `PR-22` | project method | **Low** | [`../tasks/_audit-umbrella-template.md`](../tasks/_audit-umbrella-template.md) heads its coverage table *"**Coverage grades** — §3 of the method"*. The grades are the method's §**1**, *Scope is a grade, not a list*; its §3 is *An audit larger than one session runs in cycles*, which governs the table **below** it. A pointer that resolves to a real section and the wrong one is worse than a dead one, because a reader who follows it finds plausible content and nothing reports it - and nothing here can, since the target is in another repository | Listing the headings of taskmd's own `pre-release-audit.md`, in the installed plugin's method directory, prints `1. Scope is a grade, not a list` and `3. An audit larger than one session runs in cycles`. `grep -n 'of the method' tasks/_audit-umbrella-template.md` prints the citation | [`../tasks/_audit-umbrella-template.md`](../tasks/_audit-umbrella-template.md) | One digit. **Batch it.** It has not propagated: T-219 cites [`AUDIT-METHOD.md`](AUDIT-METHOD.md) section 2 for its grades and is right, so the template is wrong on its own | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-23` | project method | **Low** | A section sign is written as an em dash in [`../tasks/TOOLING.md`](../tasks/TOOLING.md) §1: a link to T-219 is followed by `—2's cycle table` where it should read `§2's`. The reference therefore is not one, and it is **invisible to the check this file's own §2 defines** - that check resolves `<document> §n`, and an em dash is not a section sign. **Swept rather than assumed**: it is the only instance in the tree, which is what makes this a Low rather than a class | A scan of every tracked `.md` for an em dash immediately followed by a digit returns exactly one match, in `tasks/TOOLING.md`. `python tools/tasks/lint.py` passes, and its section-reference step reports the same ten unresolved marks it reported at the baseline | [`../tasks/TOOLING.md`](../tasks/TOOLING.md) §1 | One character. **Batch it** with `PR-05` and `PR-18`. The scan above is worth keeping in the batch task rather than the fix, since it is the thing that turns *one typo* into *one typo and no others* | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-24` | project documentation | **Med** | **Fourteen phase tasks have no row in the decision record, and eleven of them shipped in `0.6.0`.** [`RELEASE-PHASES.md`](RELEASE-PHASES.md) opens *"one row per task, and it is the only place that reasoning is written down"*. Its last row is `T-201`; `T-203` to `T-222` carry a `PH1` or `PH3` package and appear nowhere in it - including **`T-208`, a `PH1` defect in the published plugin**, which is the class this document exists to make visible. [`PUBLISHING.md`](PUBLISHING.md) §8 step 8 names this file as one of **both** chronology homes a release must record into, and its stated proof is *the version appears in the record without anyone reconstructing it later*. **So a release step did not run and nothing said so** - it will not say so at the next release either | Cross-referencing every `T-nnn` the document names against the task front matter returns 163 ids named and 60 records absent. Forty-six of those carry `WP1`, `WP2`, `WP3`, `final` or `none` and predate the phases, which is correct; the remaining **14** carry `PH1` or `PH3`. `grep -c 'T-203' docs/RELEASE-PHASES.md` prints `0` | [`RELEASE-PHASES.md`](RELEASE-PHASES.md)'s `PH3` table, and [`PUBLISHING.md`](PUBLISHING.md) §8 step 8 | Fourteen rows, and then the step. **A hypothesis, and it is about the step rather than the rows**: step 8's proof is a version appearing in the record, which `RELEASE-HISTORY.md` satisfies on its own - so the half of the step naming *this* file has no proof of its own and is the half that silently stopped. What may be worth more than fourteen back-filled rows is a proof step 8 can fail on, of the shape `findings.py --check` already uses on this same document's execution order | `m` | [T-237](../tasks/T-237-the-release-machinery-and-its-record.md) | open |
| `PR-25` | project documentation | **Med** | **The size rule's stated exception is two rows and it is now eighty-eight.** [`RELEASE-PHASES.md`](RELEASE-PHASES.md)'s `PH3` section opens *"Each is a capability the system does not reach, and each is `l` or `xl`"*, then records honestly that the sentence *"stopped being true on 2026-08-11"* and names the deviation: *"Two rows below are here **against** the size rule - T-089 at `xs` and T-092 at `s`"*. `PH3` today holds **110 tasks: 22 `xs`, 41 `s`, 25 `m`, 21 `l`, 1 `xl`.** Eighty-eight are below the line the section says draws it, so the exception outnumbers the rule four to one while the document still names its two instances by id. The correction mechanism is right - it points at [`../CLAUDE.md`](../CLAUDE.md) for the rule a new task is placed by - and the **quantity** beside it was never revisited | Counting `effort` over every record whose `work_package` is `PH3` returns 22 `xs`, 41 `s`, 25 `m`, 21 `l`, 1 `xl`. `grep -n 'against.. the size rule' docs/RELEASE-PHASES.md` prints the two-row claim | [`RELEASE-PHASES.md`](RELEASE-PHASES.md)'s `PH3` section preamble | **A hypothesis**: naming two ids was right when there were two and is the thing that decays, so the fix may be to state the rule change and drop the enumeration rather than to recount it - **L-136** is the shape, and this is the second document in this audit found naming instances where it should have named a rule | `xs` | [T-237](../tasks/T-237-the-release-machinery-and-its-record.md) | open |
| `PR-26` | project documentation | **Med** | **The plan's closing paragraph says seven glitch-free conditions are unimplemented, and its own table 246 lines above says they shipped.** [`RELEASE-PHASES.md`](RELEASE-PHASES.md)'s *What PH1 shipped without* reads *"Seven of R6's nine glitch-free conditions unimplemented, and a console error that does not stop a deck still invisible to the gate"*, and closes *"**The last of them was built after all** - T-036 on 2026-08-13 ... the rest of this list still stands."* But [T-041](../tasks/T-041-implement-the-nine-glitch-free-conditions.md) built conditions 2 to 8 as `GF-2` to `GF-8` and shipped them in `0.6.0` - **the console error is `GF-2`** - and the same document's own `PH3` table carries that row, struck through and dated. So *the rest of this list still stands* is false for a second of its three items, and the evidence against it is in the file that says it | `grep -n 'glitch-free conditions unimplemented' docs/RELEASE-PHASES.md` prints line 582; `grep -n 'T-041' docs/RELEASE-PHASES.md` prints the struck row at line 336 reading **done 2026-08-22, shipped in `0.6.0`**. `python tools/deck/check.py examples/reference-deck.html --sources examples/sources` prints `GF-2` to `GF-8` as verdicts | [`RELEASE-PHASES.md`](RELEASE-PHASES.md)'s closing *What PH1 shipped without* paragraph | One sentence. **A hypothesis**: this is the class [`BRIEF.md`](BRIEF.md) cites as the reason a reconciliation pass exists - *a summary paragraph contradicting the table printed directly above it* - and it is the third instance this audit has found (`PR-17`, `PR-25`). A paragraph summarising a table in the same file may be the shape to stop writing rather than the sentence to correct | `xs` | [T-237](../tasks/T-237-the-release-machinery-and-its-record.md) | open |
| `PR-27` | project method | **Med** | **Two tasks closed carrying no `shipped_in` at all - the third recurrence of a defect this project has written down twice, and nothing gates it.** [`../tasks/TASK-WORKFLOW.md`](../tasks/TASK-WORKFLOW.md) §3: *`shipped_in` is set at close ... `unreleased` until there is one.* [T-221](../tasks/T-221-answer-the-three-defects-taskmd-0-6-0s-wider-check-set-found.md) and [T-222](../tasks/T-222-derive-the-reconcile-sweeps-membership-instead-of-enumerating-it.md) are `done` with the field **absent**. [`RELEASE-HISTORY.md`](RELEASE-HISTORY.md) §1 records the same defect twice: eight tasks closed 2026-08-19 *carrying no `shipped_in` at all, so they were counted by nothing*, and `T-187` in `0.6.0`, *which is how it nearly missed the set*. **Nothing asks for it** - `shipped_in` is a carried field, deliberately outside the schema's vocabularies, so `taskmd check` validates every enumerated field and never this one. **Measured consequence, today**: this audit's cycle 7 selects *the unreleased work* by open status or `shipped_in: unreleased`, so both records fall instead into a stage-7 band the method calls *the settled record* - work closed yesterday, scheduled to be read as history | `grep -L 'shipped_in' tasks/T-221*.md tasks/T-222*.md` lists both, and each carries `status: done`. `python tools/tasks/lint.py` passes on the same tree, and `python tools/docs/cycles.py --cycle 7` lists six files with neither among them | [`../tasks/T-221-answer-the-three-defects-taskmd-0-6-0s-wider-check-set-found.md`](../tasks/T-221-answer-the-three-defects-taskmd-0-6-0s-wider-check-set-found.md) and [`../tasks/T-222-derive-the-reconcile-sweeps-membership-instead-of-enumerating-it.md`](../tasks/T-222-derive-the-reconcile-sweeps-membership-instead-of-enumerating-it.md), and whatever ends up gating it | Two fields, and then the gate - **and the two fixes are not the same work**, which is why the field has been back-filled twice and recurred anyway. **A hypothesis**: the natural home is `taskmd check`, since the closing checklist already runs `lint.py`, and the rule *a closed task carries `shipped_in`* is exactly the shape of the `blocked`-implies-a-dependency check the schema already describes. That is upstream work, so the alternative is a local step in `lint.py`. Whoever implements it measures which, and back-fills the two either way | `s` | [T-238](../tasks/T-238-the-trackers-own-rules-against-its-own-records.md) | open |
| `PR-28` | project method | **Low** | **This register says its findings are ranked and they are in id order.** Section 3 opens *"Ranked by severity, then by the reader's cost"*. After seven cycles it holds 28 rows appended in the order they were raised: `PR-01` is a **Med** and sits above `PR-02`, which is a **High**, and the two Highs are the second and seventh rows of the table. A reader scanning for what matters - including the owner making the cycle-26 checkpoint decision - reads a sequence that carries no signal. **It is a Low because cycle 40 sorts it anyway**, and it is recorded rather than fixed in place because the ordering is the one thing in this document a later cycle is *scheduled* to redo | Listing this document's finding rows in file order prints the rows in id order; the `Sev` column reads Med, High, Med, Med, Med, High, Med, ... | This document, section 3 | **Batch it** with `PR-05`, `PR-18`, `PR-22` and `PR-23`. **A hypothesis**: the sentence may be the thing to change rather than the order - a register is appended to at every cycle and re-sorted at one, so *raised in order, ranked at triage* would be true continuously where the present claim is true only twice | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-05` | project method | **Low** | [`PUBLISHING.md`](PUBLISHING.md) section 2 says *"Today the test resolves to three things"*, lists three bullets, then closes *"The test is the rule; **those two** are only today's answer."* The closing sentence was not updated when the owner added `examples/README.md` to the set on 2026-08-11. Nothing depends on the word — the rule is the test, which the same sentence says — so this is a Low and is recorded rather than fixed | `grep -n "three things" docs/PUBLISHING.md; grep -n "those two are only" docs/PUBLISHING.md` prints lines 47 and 59 | [`PUBLISHING.md`](PUBLISHING.md) section 2 | One word. **Batch it** per the skill's `pre-release-audit.md` section 4 rather than raising a task for it. Found while cycle 1 applied section 2 as its authority; the file itself is cycle 4's subject | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-29` | product documentation | **Med** | **[`DESIGN-SYSTEM.md`](DESIGN-SYSTEM.md) section 9 records as an open gap the thing the task it cites closed by ruling, and the count is wrong in the same clause.** Its *What that build did not test* paragraph reads *“four of the nine deliverable-contract rules are named nowhere in `EVALUATION.md`”* and cites [T-048](../tasks/T-048-gate-the-hard-rules-only-judgement-can-reach.md) - which is the task that **decided they stay out**. T-048's log of 2026-08-09: *demoting DS-201, DS-204, DS-207 and DS-208 into the dimensions would convert the four rules the release gate exists for from defect into a point off a score*. It built section 1.1's hard-judge checklist to own them instead, and that checklist derives its membership from the ruleset, so being unnamed in [`EVALUATION.md`](EVALUATION.md) costs those rules nothing. **All nine have an instrument today, and four is not the number either**: seven of the nine appear nowhere in that file | Counting `DS-201` to `DS-209` in `docs/EVALUATION.md` returns **seven** at zero occurrences - DS-201, DS-202, DS-203, DS-205, DS-207, DS-208, DS-209; DS-204 appears once, in section 5's threshold prose rather than in a dimension list, and DS-206 twice. `python tools/deck/ruleset.py --gates` partitions the nine across their gates: DS-202 and DS-205 `auto`, DS-203 and DS-209 `render`, DS-201, DS-204, DS-207 and DS-208 `hard`/`judge` on the checklist, DS-206 `default`/`judge` in S4 | [`DESIGN-SYSTEM.md`](DESIGN-SYSTEM.md) section 9's closing paragraph | One sentence. **A hypothesis**: the half worth keeping is *the rules that matter most are the least exercised*, which is about the deck and is still true; the half that decayed is the evidence offered for it. So the citation may want turning round - T-048 named as what settled it rather than as what found it. **This is the fourth paragraph this audit has found outliving the table or task beside it** (`PR-17`, `PR-25`, `PR-26`) | `xs` | [T-241](../tasks/T-241-the-design-system-and-the-rationale-against-what-shipped.md) | open |
| `PR-30` | product documentation | **Med** | **The hard-judge checklist is sized three different ways, and none of them is today's number.** [`EVALUATION.md`](EVALUATION.md) section 1.1 opens *“twenty-five `hard` rules were declared gates and gated by nothing”* and corrects itself to *“Twenty-six now”*; section 8.1 calls the same checklist *“twenty-four yes/no judgements”*. The ruleset holds **29** rules whose label is `hard` and whose `Check` is `judge`. **The two prose figures also contradict each other at their own date**, so this is not only drift. **The size is not decoration here**: section 1.1 requires every rule in the checklist's jurisdiction to end in `pass`, `fail` or *excused in writing*, and states that *a rule in none of those three states fails the run* - so a scorer working to a stated twenty-four leaves five rules unstated and the run still reports complete. That is the exact failure section 1.1 was written to remove, surviving in the half no program checks | `grep -n "twenty-five" docs/EVALUATION.md ; grep -n "Twenty-six" docs/EVALUATION.md ; grep -n "twenty-four" docs/EVALUATION.md` prints lines 60, 61 and 486. Counting the ruleset rows whose label is `hard` and whose `Check` is `judge` returns **29**, and `python tools/deck/ruleset.py --counts` prints the partition it comes from | [`EVALUATION.md`](EVALUATION.md) sections 1.1 and 8.1 | **A hypothesis, and it is about the shape rather than the number**: `python tools/deck/ruleset.py --gates` already prints this set, and section 1.1 says of the split one line above *“the split is derived, never listed here”*. The same treatment applied one line down removes both figures instead of correcting them; a re-count restores a number that has now been wrong three times in one document | `s` | [T-240](../tasks/T-240-the-evaluation-document-against-itself-and-the-code.md) | open |
| `PR-31` | product documentation | **Med** | **[`EVALUATION.md`](EVALUATION.md) section 3 states an invariant that its own paragraph contradicts three lines later, and three rules stand outside it.** The block reads *“a `default` or `guidance` rule that is `judge` has **only** these lists, and if no list names it, nothing in this project ever will”*. **DS-212, DS-213 and DS-234 are `default`/`judge` and no dimension list names any of them.** Nothing is unowned - their instruments are the skill's pipeline and critique reference documents, which name all three - so the sentence is false as written, and the same block already records one exception in its own account of T-119: *“DS-213 to the pipeline beside DS-212”*. **The sentence is the test a later reader applies to decide a rule has no owner**; applied to DS-234 it asks for a dimension entry that would duplicate an instrument already in the tree | Expanding sections 3 and 4's ranges gives 58 `DS-nnn` ids; subtracting them from the ruleset's `default` and `guidance` rows whose `Check` is `judge` leaves exactly DS-212, DS-213 and DS-234. `grep -rn "DS-212" skills/htmldeck/references/pipeline.md ; grep -rn "DS-234" skills/htmldeck/references/critique.md` names two of the instruments, and the pipeline document's own note reads *This stage **is** DS-213's instrument, so it says so* | [`EVALUATION.md`](EVALUATION.md) section 3's preamble block | One sentence. **A hypothesis**: the true statement is narrower than the written one - a rule **the rubric must score** has only these lists - and all three exceptions are pipeline or review rules the rubric never scores. Scoping the sentence to what this document owns may be worth more than adding three list entries, which is what the sentence as written asks for | `xs` | [T-240](../tasks/T-240-the-evaluation-document-against-itself-and-the-code.md) | open |
| `PR-32` | product documentation | **Med** | **[`EVALUATION.md`](EVALUATION.md)'s ruleset figures are stale outside the paragraph `PR-12` names, and one current figure beside them makes the stale ones read as maintained.** Section 1's *Gate and score* table says **117** `hard`, **41** `default`, 6 `guidance`; section 2's pipeline table says **68** `auto` and **45** `render`. Today they are **130**, **42**, 6, **75** and **47**. `PR-12` names line 90's *164 as of 2026-08-09* block, which at least dates itself and carries the re-derive instruction; **these five are undated cells in the two tables the document opens with**. And section 2's very next sentence - *decides **92** of the 122 owned rules and names the other 30* - **is exact today**, so the one figure somebody maintained sits three lines under four nobody did | `python tools/deck/ruleset.py --counts` prints 130 `hard`, 42 `default`, 6 `guidance` declared, 75 `auto` and 47 `render`. `grep -n "Which rules" docs/EVALUATION.md ; grep -n "static analysis of the file" docs/EVALUATION.md` prints the two table rows carrying the five cells | [`EVALUATION.md`](EVALUATION.md) section 1's table and section 2's pipeline table | **The same hypothesis `PR-12` carries, and this row is what tests it.** That finding proposes deleting the figures and pointing at `ruleset.py --counts`; **if its remedy is scoped to the paragraph its own evidence names, these five cells survive it.** So the two rows want one fix over the whole file rather than two fixes over two paragraphs, and whoever takes `PR-12` takes this | `xs` | [T-240](../tasks/T-240-the-evaluation-document-against-itself-and-the-code.md) | open |
| `PR-33` | product documentation | **Med** | **[`EVALUATION.md`](EVALUATION.md) has two sections numbered 6.3, and both are cited.** *The regression sweep* is `### 6.3` inside section 6; *Figures no gate can take* was added later as `## 6.3` at the parent level and sits **after** `### 6.4 Cost`, so the document reads 6.1, 6.2, 6.3, 6.4, 6.3, 7. **Citations exist to both and they do not mean the same section**: [T-005](../tasks/T-005-build-check-the-gate-the-deck-must-pass.md) cites it beside sections 1 and 2 for where the gate sits in the pipeline, [T-215](../tasks/T-215-the-frame-rate-instrument-and-a-number-with-its-machine.md) four times for the frame-rate table's home. **The check that exists cannot see it**: `lint.py`'s section-reference step resolves a document-and-section reference, and both ids resolve - so a citation landing on the wrong section is indistinguishable from a correct one, and the eleven baselined advisories do not include it | `grep -n "^#* 6." docs/EVALUATION.md` prints 6.1 at line 303, 6.2 at 320, 6.3 at 341, 6.4 at 357 and **6.3 again at 386**, the last at `##` rather than `###`. `grep -rn "section 6.3" --include=*.md docs tasks skills` written with the section sign returns 21 lines, **seven of which also name `EVALUATION.md`** | [`EVALUATION.md`](EVALUATION.md)'s section 6 heading sequence, and the citations that move with the number | A number, and then the citations. **A hypothesis**: the new section is not a subsection of the loop at all - it holds the figures the whole document cannot decide - so a number outside section 6 may be worth more than 6.5, which only moves the next collision one append along | `xs` | [T-240](../tasks/T-240-the-evaluation-document-against-itself-and-the-code.md) | open |
| `PR-34` | product documentation | **Med** | **[`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 2.1 states a check for two of its four sources, and that check does not exist.** The table says a `script` row is *Checked for: its **rule** exists in the shared block* and a `print` row *the same*. `component.py`'s structural pass opens with `if p.source in ("script", "print"): continue`, and the only rule-existence check runs the other way - `missing_rows` iterates the **styled** classes and asks which have a row, so a contracted class with no rule is never examined at all. **Four rows carry those sources today** - `.contents`, `.disc-lead`, `.opening`, `.qv-doc` - and all four happen to be styled, which is why nothing has surfaced. **The document's own reason for the `vocabulary` source says why this matters**: *declared and unused is otherwise unfalsifiable*, and `script` and `print` are declared-and-unverified in exactly that shape | Building a `script` row whose class has no rule anywhere and passing it to `component.py`'s `structure()` returns `[]`, and the same for a `print` row; `missing_rows` iterates `styled.items()`, so neither row is reachable from it. `python tools/deck/component.py check examples/reference-deck.html` reports 98 authored parts required against 102 rows, and the four-row difference is precisely these | [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 2.1's table, and `tools/deck/component.py` | **A hypothesis, and the cheap half is a check rather than a wording change**: the stated obligation is one line - a `script` or `print` row's class appears in the shared block, or in the print block for `print` - and it would close the direction `missing_rows` cannot see. The alternative is to say the two sources are unchecked, which is what the document currently denies | `s` | [T-242](../tasks/T-242-the-contracts-against-the-checkers-that-decide-them.md) | **closed 2026-08-29.** The cheap half was taken and it is a check: `unstyled_rows` decides that a `script` row's class is styled in the shared block and a `print` row's in the print block, which is the obligation section 2.1 already stated. `print_css` reads the block `shared_css` cuts out, and the two share one brace-matching scan so the print block's nested `@page` is handled in one place. Four rows carry the two sources and the count travels with the verdict, so *0 problems* over four rows and over none are not the same fact. Both directions asserted in `component.self_test`, including a `print` row styled in the **shared** block, which must fail: a rule in the wrong block is what the row claims about. The alternative the row offered - saying the two sources are unchecked - was not needed |
| `PR-35` | product documentation | **Med** | **The motion-rule table has no completeness half, and two rules are animating with no row in it - the same defect [T-198](../tasks/T-198-give-affordance-motion-its-own-band-faster-than-content-motion.md) fixed on 2026-08-20 and nothing stops recurring.** [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 3.8 calls its table *that sentence made checkable*, and `component.py`'s `motion_gaps` iterates **the contract's rows** and asks whether each reads its tokens. Nothing iterates the CSS. `.arrow-pop` and `.dot-pop` in `shell/components.css` both animate on `--scale-dur` and `--scale-ease` and neither has a row. **The contract already knows this is the failure mode**: section 1 gives the *parts* table a completeness half and says why - *a class styled there and absent from the tables below fails the gate* - and section 3.8's own footnote records T-198 *adding the two rows below them, which were animating with no row here at all* | `grep -n "scale-dur" shell/components.css` prints lines 677, 678, 701 and 702, in the `.arrow-pop` and `.dot-pop` rules. `python tools/deck/component.py parts` ends *102 part(s), 17 motion rule(s)*, and the 17 are the table's; neither pop is among them. The DS-229 verdict *every rule the contract lists reads the motion tokens it lists: 17 rule(s), 0 gap(s)* is green on that same tree | [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 3.8's table, and `motion_gaps` in `tools/deck/component.py` | Two rows, and then the direction. **A hypothesis**: the rows are the smaller half and the reverse check is the finding - a CSS rule declaring an `animation` or a `transition` on a theme token, with no row here, is decidable from the same parse `motion_gaps` already does. Adding the rows without it buys until the next motion | `s` | [T-242](../tasks/T-242-the-contracts-against-the-checkers-that-decide-them.md) | **closed 2026-08-29.** The hypothesis was right that the rows are the smaller half: `unrowed_motions` iterates the CSS and reports a rule that starts a motion on a token with no row in section 3.8, which is the direction `motion_gaps` cannot see. **It found a third instance on its first run** - `.ruler[data-dense] .ruler-ring`, animating on `--afford-dur` and `--afford-ease` with no row, which this finding did not name. All three have rows now, and section 3.8 carries the completeness half in its own words, the way section 1's parts table always has. A rule switching motion **off** reads `none` and is not a motion, so the reduced-motion collapse, the preflight and the density gate need no rows and are not exceptions anybody has to remember |
| `PR-36` | product documentation | **Med** | **Two of the theme contract's motion tokens are read by nothing in the tracked tree, and a third names a reader that left.** [`THEME-CONTRACT.md`](THEME-CONTRACT.md) section 3.6 gives Turn a duration and an easing - *Turn: a card reveal (DS-140), inside the cap (DS-141)* - and **no tracked file reads either**: both themes declare them, all five tracked decks carry them, and `var(--turn-dur)` and `var(--turn-ease)` appear nowhere. So DS-140's starter set, which that rule calls *what a deck gets without designing anything*, ships one of its four names as two dials wired to nothing - and [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 5 tells an author wanting an overshoot on a card reveal to *reach for `--turn-ease`*. **`--scale-ease`'s cell is wrong in the other direction**: it reads *Scale's easing, and the ruler ring's, which borrows the pair*, and T-198 moved the ruler ring onto DS-240's affordance band on 2026-08-20 - which the component contract records and this one does not. The pair's real readers today are `.arrow-pop` and `.dot-pop`, which the cell does not name | `git ls-files` filtered to css, js, py and html, searched for `var(--turn-`, returns **no file**; searched for `--turn-dur` it returns seven - both themes and all five decks. `grep -n "scale-dur" shell/components.css` returns only the two pop rules. [`../docs/MOTION-GUIDE.md`](MOTION-GUIDE.md) section 3 states the intent independently: *`.arrow-pop` is Scale applied to an arrowhead rather than a new motion* | [`THEME-CONTRACT.md`](THEME-CONTRACT.md) section 3.6's `Governs` column | **A hypothesis, and the two halves want opposite fixes.** `--scale-ease` needs its cell corrected to the readers it has. Turn is a decision rather than an edit: either a component implements it - the reveal DS-140 names and no deck has ever built - or the two tokens go and DS-140's set loses a name. **Deleting a token is the cheaper answer and the one that loses something**, which is why this is a hypothesis and not an instruction | `s` | [T-274](../tasks/T-274-build-the-card-reveal-so-turns-two-dials-have-a-reader.md) | **deferred 2026-08-29**, the half that is a decision; the half that is an error is fixed. `--scale-ease`'s cell no longer names the ruler ring, which T-198 moved off the pair in 2026-08-20: it names the arrowhead and the matrix dot, which are the two components that read it. **Turn is not this session's to decide.** Either a component implements the reveal DS-140 names and no deck has built, or `--turn-dur` and `--turn-ease` go and DS-140's set loses a name - a `DS-000` question, and the remediation order's standing authority covers amending a row rather than answering a ninth rule question. What has changed is that the contract stops implying a reader: both cells say **no shipped component reads it**, `THEME-CONTRACT.md` section 3.6 records the choice and its two options, and `COMPONENT-CONTRACT.md` section 5 no longer sends an author to a live dial without saying so. Recorded with `PR-77`, which is the same question in the colour band **Ruled 2026-08-29, and not the way this row was leaning.** Put to the owner as *build the component, or retire the two tokens and let DS-140's set lose a name*, with retirement recommended as the cheaper answer: **the owner chose to build it.** So the starter set keeps its fourth name and gains a body, and `T-274` carries it. The remedy's own sentence - *deleting a token is the cheaper answer and the one that loses something* - was right about both halves, and the owner priced the loss higher than the cost . **Both halves are now closed.** `--scale-ease`'s cell was corrected by [T-242](../tasks/T-242-the-contracts-against-the-checkers-that-decide-them.md) on 2026-08-29, and the reveal was **built the same day** by [T-274](../tasks/T-274-build-the-card-reveal-so-turns-two-dials-have-a-reader.md): `.turn` in the shared block, its rows in the component contract, its collapse in all four modes, and the first card reveal on the reference deck's closing slide. Measured there - at rest with nothing running before the slide arrives, and once it has, `animationName` `turn`, `animationDuration` `0.42s`, `animationTimingFunction` `ease-in-out`. **The two dials have a reader**, which is the whole of this finding. How it *reads* is row 4 of [`OWED-LOOKS.md`](OWED-LOOKS.md) |
| `PR-37` | the product | **High** | **The second theme fails the contract it was written to demonstrate, by fifteen tokens, and no gate validates a theme file.** `python tools/deck/theme.py validate themes/lattice.css` exits 1 with fifteen *not declared*: the whole affordance and press band (`--afford-dur`, `--afford-ease`, `--press-dur`, `--press-ease`), the whole inter-slide transition below its duration and easing (`--slide-leave-fwd`, `--slide-leave-back`, `--slide-shift`, `--slide-scale`, `--slide-leave-shadow`), `--motion-density`, both `--pager-*` and the three pop tokens. **Every one arrived after the file did**, and `tools/check_all.py` runs `theme.py check <deck>` over the decks and **never `theme.py validate` over a theme**, so a tracked theme is in no check's subject. **[`THEME-CONTRACT.md`](THEME-CONTRACT.md) section 5 makes a claim the file cannot satisfy** - *`themes/lattice.css` is the demonstration: it overshoots Turn and eases Scale and the slide transition out* - when `--slide-leave-fwd` is what names the transition's keyframe and lattice does not set it. DS-235 is `hard` and makes the transition a named choice from a closed vocabulary; a deck swapped onto this theme has neither name | `python tools/deck/theme.py validate themes/lattice.css` prints *15 problem(s)* and exits 1, against *themes/quarto.css - conforms*. `grep -n "slide-" themes/lattice.css` returns `--slide-dur` and `--slide-ease` and nothing else; the same grep on `themes/quarto.css` returns seven. `grep -n "theme.py" tools/check_all.py` returns one line, and its argument list is `["check", deck]` | `themes/lattice.css`, and the missing step in `tools/check_all.py`. The file is **cycle 16's** subject and is recorded here because cycle 9 reached it through the contract that names it | Fifteen declarations, and then the gate. **A hypothesis, and it is that the declarations are the smaller half**: this file drifted for the same reason `PR-06`'s counts did - nothing ran over it - and re-declaring fifteen tokens restores a state with the same nothing behind it. `check_all.py` already discovers every checker and every deck; discovering every **theme** and validating it is the shape that would have caught this the day DS-240 landed | `s` | [T-228](../tasks/T-228-the-second-theme-fails-the-contract-it-demonstrates.md) | **closed 2026-08-29**, both halves, and **the row's own hypothesis held**: the declarations were the smaller half. Fifteen tokens declared in `themes/lattice.css` — the timing four **scaled to this theme's stated tempo rather than copied**, because lattice's header names motion as one of the four axes it moves and quarto's clock would have contradicted it; the rest copied, being shape, keyframe identifiers, or DS-238's shipped default. Then the gate: `check_all.py` **discovers** themes and runs `theme.py validate` on each, never a list — a hand-kept one would have gone stale at the same moment this file did. Two things the fix found that the row did not name. **`tracked()` takes a git pathspec, not a shell glob**, so `themes/*.css` matched the three font faces one level down; the self-test asserting the glob's two directions is what said so, rather than a failing run three steps later. And **`theme.py`'s self-test had no fixture for a *missing* token at all** — both its negative fixtures seed a value the validator can object to, so `not declared` was a verdict only ever produced by a real theme being wrong. That is the branch this defect lived in, and it now has a `drop_token` fixture that refuses a no-op |
| `PR-38` | the product | **Med** | **`theme.py swap`'s refusal path crashes instead of reporting, and it had never run.** The documented command `python tools/deck/theme.py swap examples/reference-deck.html themes/lattice.css -o <out>` exits 1 with a `TypeError: sequence item 0: expected str instance, tuple found` and writes nothing. `validate()` returns tuples; the **validate command** formats them into its two-column listing and the **swap command** does `"\n  ".join(bad)` on them directly. **The path is only reachable with a non-conforming theme**, and the one in the tree has been non-conforming without anything running over it - so `PR-37` and this defect have been hiding each other. The refusal itself is right: the deck is not written | The command above prints the traceback and `ls` shows no output file. `sed -n '765,768p' tools/deck/theme.py` is the join; `python tools/deck/theme.py validate themes/lattice.css` formats the same list correctly, which is what makes this the swap path's defect and not `validate()`'s | `tools/deck/theme.py`'s `swap` branch in `main`, and whatever self-test covers it. The file is **cycle 12's** subject and is recorded here because cycle 9 reached it through the contract it implements | One line. **A hypothesis about the self-test rather than the line**: a formatter that has never executed is a self-test that never built a failing input, and the same shape may sit behind other refusal paths in this file - which is worth one sweep at the time of the fix rather than one corrected join | `xs` | [T-245](../tasks/T-245-seven-tool-defects-in-tools-deck.md) | **closed 2026-08-29**, remedy as proposed. `swap`'s refusal joined `validate()`'s `(rule, message)` pairs as if they were strings, so the one thing the path exists to do raised `TypeError`; it now formats them the way the `validate` command already did. Measured: `theme.py swap examples/reference-deck.html themes/lattice.css -o <out>` printed a traceback and exit 1 before, and now prints seven named undeclared tokens and exit 1, writing no deck either way. **The row's wider hypothesis was swept and came back empty**: the three other joins over a checker's output in `tools/deck/` - `audit.py`, `contrast.py`, `presenter.py` - all hold strings, and `theme.py`'s own second use at line 652 already unpacked the pair. One instance, not a shape |
| `PR-39` | product documentation | **Med** | **[`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 3.2 says `data-stage="back"` is *the one value that is not a position*, and grants a second one thirty-four lines later.** The prose reads *Nothing else may: a slide the reader is meant to be argued to is in a stage*, and the note under the slide's own parts table reads *a slide with `data-stage="front"` - a lobby (DS-242) - carries no `.provenance`*. `shell/deck.js` has treated them as a pair since 2026-08-20: one line binds `back`, `front` and a `matter` flag that is true for either. **The cost is the table under the false sentence**, which is this section's operative statement of what a non-position value changes in each rendering - ruler tick, ruler label, stage count, contents box label, contents box mark - and it has a column for `back` and none for `front`. What a lobby does to the ruler and the printed contents page is therefore written only in DS-242 and in the script | `grep -n "is the one value that is not a position" docs/COMPONENT-CONTRACT.md` prints line 119 and `grep -n "Nothing else may" docs/COMPONENT-CONTRACT.md` line 121, against the `front` exemption at line 153. `grep -n "FRONT_MATTER" shell/deck.js` prints the constant and the ternary that selects it | [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 3.2's prose and its comparison table | A sentence and a column. **A hypothesis**: the table is the part worth spending on - `front` and `back` differ in exactly one of its six rows, the label at rest, so a third column states the whole of the difference and the sentence becomes *two values are not positions* rather than one correction that will need making again when a third arrives | `xs` | [T-242](../tasks/T-242-the-contracts-against-the-checkers-that-decide-them.md) | **closed 2026-08-29.** The hypothesis was taken: the comparison table has a `front` column and the sentence reads *two values are not positions*. The two differ in exactly one of the six rows - the contents box label, `Back matter` against `Front matter` - and every other row is identical, which is what `deck.js` says by binding both to one `matter` flag. Stated as a column rather than as a corrected sentence for the reason the remedy gives: a third value needs a column and would need the sentence corrected again |
| `PR-40` | product documentation | **Low** | A paragraph about **figure** motion is filed inside [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 3.4 *The chrome*, between the chrome's parts table and the rule about what may sit in the navigation container. It opens *`.arrow-pop` and `.dot-pop` are content motions, and they sit on the figure rather than on what moves*, and it carries the only statement of how `--m-rank` and `--dp` are assigned for those two - which belongs to section 3.6 *The figure*, where both parts have their rows, or to section 3.8 *Motion*. Nothing is wrong in it; it is in the wrong place, which is the same finding [`RULESET-AUDIT.md`](RULESET-AUDIT.md) recorded for DS-007 filed under *Portability* | `grep -n "arrow-pop" docs/COMPONENT-CONTRACT.md` prints the paragraph inside section 3.4 - which `grep -n "^### 3\." docs/COMPONENT-CONTRACT.md` places between 3.4 at line 320 and 3.5 at 395 - and the two parts' own rows inside section 3.6 | [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md), the paragraph's filing | A move. **Batch it** with `PR-05`, `PR-18`, `PR-22`, `PR-23` and `PR-28`. **A hypothesis**: it reads as having been written while section 3.4 was open for T-114 and never re-filed, so the batch is worth a scan for other paragraphs a task left where it was working | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-41` | product documentation | **Low** | **One row uses a `Count` value the contract's own vocabulary does not define.** [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 2 closes the column at *`1` · `0-1` · `1+` · `0+`*, and the chart-engine row in section 3.6 reads `0..1`. It is the only one of 103 count cells outside the set. Nothing is unenforced - the declaration's four keys, the `output=svg` rule and the SPDX test are `audit.py`'s under DS-122 - but the row is **not data**: `parse_part_row` drops it, so *this document, as data* does not carry a row the document prints as one. Its name has no leading dot either, which is the parser's actual reason and is defensible for a `meta`; the count is a slip in a closed vocabulary | Extracting every count cell returns 41 `1`, 41 `0+`, 13 `0-1`, 7 `1+` and **1** `0..1`, at `docs/COMPONENT-CONTRACT.md` line 467. `python -c` loading `component.py` and checking `'htmldeck-chart-engine' in parts` prints `False` against 102 parsed rows | [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) section 3.6's chart-engine row | Two characters. **Batch it** with `PR-40`. **A hypothesis**: the row's real problem is that it sits in a parts table and is not a part, so saying so above it may be worth more than fixing the count and leaving a row that still silently is not data | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-42` | the product | **Med** | **The diagram-placement gate measures one diagram per slide, and a shipped deck has a slide with two.** `figgrid.py`'s probe takes `querySelector('.body svg.fig')` - the **first** figure on each slide - so a second one is never measured and never counted. `examples/portfolio-review/` carries **ten** `svg.fig` elements and slide 10 holds two of them; DS-236's row for that deck reads *0 of **9***. **The denominator is what makes this a defect rather than a gap**: the module's own docstring says the count travels in the text precisely so *0 off, of 0* and *0 off, of 8* cannot read alike (**L-36**), and here it reads as the whole deck while one diagram was never looked at. **`markhits.py`, gated in the same run and reading the same slides, iterates every `svg` and skips only nested ones and icons** - so the correct idiom is in the cycle beside the defect | `python tools/deck/figgrid.py examples/portfolio-review/portfolio-review.html` prints *9 diagram(s)* and lists slides 2 to 11 with slide 10 once. Counting `svg` elements carrying the `fig` class in that file returns **10**, and grouping them by enclosing `section.slide` puts two on slide 10. The reference deck has 8 figures on 8 slides, which is why its own row is exact | `tools/deck/figgrid.py`'s probe, and DS-236's row | One selector. **A hypothesis about the denominator rather than the loop**: every other measuring tool in this cycle prints *n of m* where `m` is what it examined, and none of them prints what it **skipped**. A figure the probe declined - a second one on a slide, or one under the quarter-width icon filter - is invisible in both numbers, so reporting the skip may be worth more than the fix, and it is the same shape `markhits` already uses for its ungated pairs | `s` | [T-245](../tasks/T-245-seven-tool-defects-in-tools-deck.md) | **closed 2026-08-29**, and it moved a rule. The probe took `querySelector`, so it measured the first diagram on each slide; it now iterates, and counts what it declines. Measured: portfolio-review reads **10** where it read 9, and the other three decks read 8, 7 and 6 while declining 6, 5 and 7 icons that used to be invisible in both numbers. **The tenth diagram then failed DS-236 by +962.3 du** - the right-hand figure of a two-column slide, correctly placed in its own column - so the rule could not be satisfied by a correct figure and its population had never been complete. That is a rule question, which the order's section 4 does not cover; **put to the owner and ruled the same day**: a diagram answers to the text column it sits in. DS-236's row is amended and reversible, and the probe binds the column on geometry rather than on `.col`, which is a deck's vocabulary and not the shell's. After the amendment portfolio-review is **0 of 10** and `check.py` passes it with 0 failing |
| `PR-43` | the product | **Med** | **The clause-level account is a hand-kept table whose sweep is dated, and two `hard` rules report `checked` with clauses nothing decides.** `check.py`'s `CLAUSES` comment states *the sweep that produced this list read all 120 `hard` rules, 2026-08-18*; the ruleset holds **130** today, and **15** `hard` rows carry a date on or after 2026-08-19. Two of the arrivals are conjunctions with no row: **DS-242** states three separable things - a lobby is single and first, the counter counts the argument rather than the file, and matter keeps its box on the printed contents page - and `audit.py`'s function says in its own docstring *what is checked is its shape*; **DS-073** states three - all content travels, every panel renders open, **and the disclosure control is not rendered at all** - and has two verdicts, neither of which is the third. **This is the exact failure the device exists to prevent**, quoted from the same comment: *one satisfied row moves a rule into `checked`, so a clause nothing reaches disappears inside a rule the run reports as covered*. The machinery validates the clauses it holds and cannot see the ones it lacks | `python tools/deck/ruleset.py --counts` prints 130 `hard`; the run's clause block prints *5 rule(s) whose statement is a conjunction*, 14 clauses, 8 decided. Filtering `hard` rows whose text names a date on or after 2026-08-19 returns 15 ids. `grep -n "DS-073" tools/deck/audit.py` returns two verdict rows and no third; `sed -n '1084p' tools/deck/audit.py` is DS-242's *what is checked is its shape* | `check.py`'s `CLAUSES` table and the sweep behind it | Two rows, and then the sweep. **A hypothesis, and it is the same one `PR-25` reaches from the documentation side**: a dated sweep over a moving set is `L-136`'s shape, so what may be worth more than three new rows is a rule tying the sweep to the ruleset - every `hard` rule whose text carries more than one testable assertion needs a row or an excusal, decided the way `producer_split` already decides a producer in neither half | `m` | [T-244](../tasks/T-244-the-gates-own-coverage-account.md) | **closed 2026-08-29**, and **the remedy's sizing was refused by the sweep it asked for**. The mechanism landed as the row proposed: `SWEPT` records one digest per `hard` rule of the exact row somebody read, so an arriving or edited rule re-opens the question and a dated sentence can no longer go quiet under a moving set. It is watched failing in both directions. **Then the sweep ran, and the figure was not 15 but 24** - the row was written days earlier and the set kept moving, which is the finding restating itself. Reading those 24 found **eleven** conjunctions, not two. The two named here, `DS-073` and `DS-242`, have rows: DS-073's third clause and DS-242's second and third are unreached and now say so, where before each rule reported `checked` on one satisfied row. **The other nine are a counted backlog, not a silent one** - `CONJUNCTIONS_OWED` prints on every run, because `SWEPT` alone would have said only *somebody read this* and a reader would take that for *and found it states one thing*. [T-278](../tasks/T-278-write-the-clause-rows-the-sweep-found-owing.md) carries them: twenty clauses written quickly would be nine excusals saying *not checked*, which inflates the account rather than sharpening it |
| `PR-44` | the product | **Med** | **DS-239's ranking finds its subject by class name, which is the shape [T-214](../tasks/T-214-ds-142s-checker-is-an-allow-list-of-one-class-name.md) removed from DS-142's checker the day before, in the same file as the fix.** `density.py` ranks *every element carrying a content motion* by matching `CONTENT_CLASSES` - `pulse`, `arrow-pop`, `dot-pop` - and treats `rise`, `current`, `opening` as affordance by name. Twenty lines further down the **same module** decides DS-237 and DS-238 by reading `--motion-kind` off the rule that starts the motion, which is the declaration-based idiom T-214 restored. [T-187](../tasks/T-187-open-the-motion-vocabulary-into-a-style-guide.md) opened DS-140's vocabulary on 2026-08-21, so a conformant motion may now carry a name no list holds - it would get no rank, run at every density, and DS-239's row would still read *0 of n*, because the denominator is the list rather than the deck | `grep -n "CONTENT_CLASSES" tools/deck/density.py` shows the three-name table and `eligible()` keying on it; `grep -n "motion-kind" tools/deck/density.py` shows `KIND` reading the declaration for the other two rules. T-214's own record states the removed shape: *the checker tested `classList.contains('current')`, so this rule's subject was a class name* | `tools/deck/density.py`'s `CONTENT_CLASSES` and `AFFORDANCE_CLASSES` | **A hypothesis, and the file already contains its own answer**: `motion_rules()` finds every rule that starts a motion and reads its declared kind, so the ranking could take its subject from the elements those rules match rather than from three names. The cost is that a rank has to be assigned per element and the declaration is per rule, which is the reason the class list is there - so whoever implements it measures whether that mapping is decidable statically before committing | `s` | [T-243](../tasks/T-243-five-checks-bound-on-a-name-rather-than-on-structure.md) | **closed 2026-08-29.** The vocabulary is derived from the deck's own `--motion-kind` declarations; `CONTENT_CLASSES` is now an ORDER and no longer the denominator, and a declared class it does not name ranks after every one it does. **Two preconditions were found by measuring rather than by reading**: `css_of` returned the CSS with its comments, so 7 of the reference deck's 14 motion rules arrived with a comment glued to the head of their selector; and `switched_off` read `.rstrip("!important")`, a character SET, so `animation:pop` stripped to the empty string and its rule left the motion set carrying its `--motion-kind` with it. **`AFFORDANCE_CLASSES` was dead** - defined, read by nothing - so half this row's subject was a deletion. The shipped decks are unchanged at 2 content motions, which is what a re-binding should do; the difference is proved by a self-test fixture declaring a motion no table names |
| `PR-45` | the product | **Med** | **The figure ledger recognises one deck's vocabulary, so `FIG-1`'s denominator is figures the pattern admitted rather than figures on the slides.** `content.py`'s `UNITS` list is the reference deck's transit domain - *stations, routes, riders, trips, people, buses, corridors, stops, km* - plus the shape-based forms. **`examples/sort-window/` states *9 miles*, *14 miles*, *6 miles* and *14 nights* on its slides and none of them is in the ledger**, because `miles` and `nights` are not units the pattern knows; its row reads *figures on a slide that appear in no source: 0 of 73*. **The direction is what makes it a finding**: the module's docstring defends the heuristic on the ground that *it over-reports rather than passing a wrong number*, and its own T-169 comment calls the opposite outcome *a silent under-report, which is the direction the docstring above says these checks do not fail in* | Extracting slide text from `examples/sort-window/sort-window.html` and running `content.FIGURE` over it returns no entry containing *mile*; the same text contains ten numeral-and-*miles* phrases and one *14 nights*. `python -c` on the module prints `'miles' in content.UNITS` as `False`. All four gated decks pass `FIG-1` to `FIG-3` today | `tools/deck/content.py`'s `UNITS`, and what `FIG-1`'s denominator claims | **A hypothesis, and enlarging the word list is the answer it argues against**: a list of nouns is one deck's domain however long it grows, and the next adopter's is not in it. What may serve is the shape the pattern already uses for currency and separators - *a numeral followed by a word the sources also use beside a numeral* - which derives the vocabulary from the pair being compared instead of naming it. Failing that, the row's denominator should say what it excluded | `s` | [T-243](../tasks/T-243-five-checks-bound-on-a-name-rather-than-on-structure.md) | **closed 2026-08-29.** The domain nouns are derived from the sources being compared - any word a source uses beside a numeral - and the shape-based forms stay in the constant. The transit nouns are kept as the vocabulary of last resort, for the readers that have no sources (DS-231 compares a bottom line against the slide faces, and both sides are the deck). **Measured on the two decks the row named**: `sort-window` moved from *0 of 73* to *0 of 86* and `reference-deck` from *0 of 82* to *0 of 87*, both still passing. **Measured across all four gated decks**: 34->35, 73->86, 82->87 and 93->96 figures, all four still passing. **The derivation exposed three boundary defects on the way, in three rounds, and all three are fixed**: a numeral preceded by a colon minted `30 cut-off` out of `02:30`; a numeral ENDING in a comma minted `against` out of `18, against 22`, which then made `380 against` a figure the reference deck does not state; and `Phase 2 cannot start early` minted `cannot`, which the release gate caught after the four deck runs had not. The first two are fixed in the pattern. The third is a list, and the row's own argument is why that is allowed: unit NOUNS are an open class, which is why a table of them is always one deck's domain; English FUNCTION words are a closed one |
| `PR-46` | the product | **Med** | **Two excusals in the gate's coverage account are wrong about what decides the clause they excuse, and the account is the part `check.py` calls the part that matters.** **DS-226's** entry reads *since T-123 the printed page itself is measured: **PRINT-2 and PRINT-3 above** read the card rectangles out of the printed PDF* - unconditionally, while the same run's notes print *printed page count and geometry: NOT RUN - opt-in*. Those two rows exist only under `--print-pages`, and **DS-222's neighbouring entry gets the conditionality right** - *the count is checked here under `--print-pages`* - so the form was available. **DS-145's** entry excuses the reveal clause on the ground that *reveal motions are DS-140's starter set, checked statically there*; DS-140's decided rows are the theme's bands and *`Current` renders dashed*, neither of which decides that a reveal is an open, a turn or a scale - and T-187 opened the set on 2026-08-21, so *DS-140's starter set* is no longer a closed thing to be checked against | A default run of `python tools/deck/check.py examples/reference-deck.html --sources examples/sources` prints the DS-226 excusal naming PRINT-2 and PRINT-3, and prints *NOT RUN* for them three lines from the top. The same run's verdict list carries exactly two DS-140 rows. `sed -n '120,150p' tools/deck/check.py` holds both excusal texts | `tools/deck/check.py`'s `DEFERRED` entries for DS-226 and DS-145 | Two sentences. **A hypothesis about what would have caught them**: `closing_faults` already validates a closing condition and reports one that has come true - but only for the `rule` kind, and neither of these is one. A reason that cites another rule's verdict is a second binding of the same shape, and it is decidable on a run: the cited rule's rows are in the account already | `xs` | [T-244](../tasks/T-244-the-gates-own-coverage-account.md) | **closed 2026-08-29**, both sentences, and both were worse than the row put them. **DS-226** claimed PRINT-2 and PRINT-3 measure the geometry while the same run printed `NOT RUN - opt-in` for those rows three lines above; it now carries DS-222's conditional form, which was available all along. **DS-145 was wrong twice and also contradicted itself.** It excused the reveal clause on the ground that reveal motions are DS-140's starter set checked statically there - but DS-140's two rows test the duration bands and the dash, neither of which asks which motion a reveal uses, and T-187 opened the vocabulary on 2026-08-21 so there is no closed starter set left to check against. Meanwhile its closing condition asked for work on *dashed arrows on flow connectors*, the one clause DS-140 does decide. The entry now says the reveal clause is decided by nothing and its closing condition names that clause instead |
| `PR-47` | the product | **Low** | `tools/deck/ruleset.py` - the module whose whole purpose is that no count is kept by hand - carries three stale counts in its own prose. *Cycle 39 added the third rather than raising it again (`AM-12`)*: `self_test`'s docstring argues that a wrong `Reach` split *silently converts 43 `judge` rules into rules the gate must account for*, and `--counts` prints **48**. `off_table`'s docstring explains the two circulating totals as *the table holds 160 with 5 `guidance` rules, and the document declares 161 with 6*; the same module derives **177** and **178** today. The `OWNED` comment reads *`—` belongs to the **four** rules that bind whoever builds a check rather than the deck*, and there are seven rows with `Check` `—`, five of which are the group `DESIGN-SYSTEM.md` section 8 describes - that section records *It was four until 2026-08-17* in its own text. Nothing operational depends on any of the three: every published figure comes from `--counts`, which derives them | `python tools/deck/ruleset.py --counts` prints 177 rows, 178 declared and `—` 7. `grep -n "holds 160" tools/deck/ruleset.py` and `grep -n "the four rules" tools/deck/ruleset.py` print the two comments | `tools/deck/ruleset.py`'s `off_table` docstring and its `OWNED` comment | Two comments. **Batch it** with `PR-05`, `PR-18`, `PR-22`, `PR-23`, `PR-28`, `PR-40` and `PR-41`. **A hypothesis**: both sentences are explaining a mechanism and reached for an instance to explain it with, which is `PR-25`'s shape in code comments - so the batch may be worth scanning for the same move rather than correcting two numbers | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-48` | the product | **High** | **DS-106's check omits a word DS-106 names, and two shipped decks carry it.** The rule's own sentence bans *genuinely / actually / arguably / precisely*; `audit.ds106_no_banned_terminology` matches ten words and **`actually` is not among them** - it carries the other three. `examples/measure-first/` uses it **three** times in slide copy and `examples/reference-deck.html` once, and both decks pass. **DS-106 is `hard`**, so a violation is a defect rather than a preference, and the gate reports the rule `checked`. The wider gap is stated for context and is not the finding: the rule names **five categories** and the check implements a word list drawn from one of them, which is what DS-107 exists to say - but a word the rule writes down and the check does not read is a different thing from a category nobody has built yet | `python -c` importing `audit` and calling `ds106_no_banned_terminology` on each deck returns `True`; searching the same decks' slide copy for the word returns 3 in `measure-first` and 1 in `reference-deck`. `python -c` printing `DS-106`'s row from `ruleset.load()` shows the four words in the rule's own text | `tools/deck/audit.py`'s `ds106_no_banned_terminology`, and the four instances in two shipped decks | One word, and then four sentences of deck copy. **A hypothesis, and it is against the obvious fix**: adding `actually` closes this instance and leaves the class - the list is written twice, in the rule and in the regex, and nothing compares them. `DESIGN-SYSTEM.md` says the list is an **inline fallback** for machines where the `humanize-writing` skill is absent, so what may serve is deriving the fallback from the rule's own row the way `ruleset.py` derives everything else | `s` | [T-229](../tasks/T-229-ds-106s-check-omits-a-word-the-rule-names.md) | **closed 2026-08-29.** The hypothesis was taken: `banned_terms()` reads the italicised terms out of DS-106's own row and `banned_pattern()` builds the expression from them, so the list exists once. Thirteen terms come back where the regex had ten; `delve` was in the check and named by nothing, so it is written into the row rather than dropped, and a single word now matches from its stem — which is how `synergy` keeps catching `synergies` without the regex spelling it. Every derived term is seeded and watched to fire in `audit.self_test`, so a word the row names and the check does not decide fails the gate rather than a deck. **The row's instance count is corrected by the fix and the correction is the more useful finding**: `measure-first` had **two** in slide copy, not three, and what the third reading counted was the shell's own prose. The check read the whole file, so the word `actually` sat in **seven** CSS, script and HTML comments in *every* deck — `components.css` and `deck.js` argue their decisions in prose and every deck embeds them. Adding the missing word without narrowing the subject would have failed all five tracked decks for words nobody wrote as copy, which is the shape of the obvious fix this row warned about, one layer further down. The subject is now the deck's copy: quoted sources, stylesheets, scripts and comments cut. Three instances in two decks removed and both decks rebuilt |
| `PR-49` | the product | **Med** | **DS-032's check names one licence where the rule names a class of them, and the same file carries the right list four hundred lines away.** The rule reads *faces embed as base64 `@font-face`, latin subset, **licence permitting redistribution*** - and the `STATIC` predicate requires the literal string *Open Font License* to appear in the deck. A deck embedding an MIT, Apache-2.0 or CC0 face satisfies the rule and fails the gate, which is a `hard` check forbidding what the ruleset permits. **`audit.REDISTRIBUTABLE` is exactly the list this row wants** - nine SPDX identifiers *whose terms permit redistribution inside a single file* - and its own comment says so: *the same test DS-032 applies to an embedded face, one artifact along*. It was written for DS-122's chart engine and never reached back. **Nothing fails today** because all three shipped faces are OFL, which is why the direction matters: this is a false alarm waiting for the first adopter who picks a different face | `grep -n "Open Font License" tools/deck/audit.py` shows the predicate; `grep -o "Open Font License" themes/faces/*.css` returns one per face, all three OFL. `python -c` printing `audit.REDISTRIBUTABLE` lists the nine identifiers, and `DS-032`'s row text says *licence permitting redistribution* | `tools/deck/audit.py`'s `STATIC` entry for DS-032 | **A hypothesis, and it is that the fix is a move rather than an edit**: `REDISTRIBUTABLE` already encodes the rule's test and already carries the comment tying it to DS-032, so the row can read it. What is worth deciding at the same time is whether *the licence travels with the font* means an SPDX identifier or a notice - the check tests for a notice string, the list tests for an identifier, and a deck could satisfy one and not the other | `xs` | [T-243](../tasks/T-243-five-checks-bound-on-a-name-rather-than-on-structure.md) | **closed 2026-08-29.** DS-032 now reads the licence CLASS. The register's hypothesis was that the fix is a move - the row reading `REDISTRIBUTABLE` - and **the measurement refused it**: that list is SPDX identifiers and a face carries a NOTICE, so the three shipped faces (`SIL Open Font License 1.1`) match none of its text. Both alphabets are accepted, which answers the row's own open question. **A second measurement changed the scope**: searching the whole deck passed `reference-deck.html` on the word `MIT` - the Lucide icons' licence, four hundred lines from any font - so the right answer for the wrong reason. *The licence travels with the font* is a claim about proximity, so each face is now read in the window of comments attached to it |
| `PR-50` | the product | **Med** | **The stage-1 / stage-2 split the code implements is not the one [`EVALUATION.md`](EVALUATION.md) section 2 describes.** That table maps stage 1 onto *`auto` rules - static analysis of the file* and stage 2 onto *`render` rules - measurement*, and `audit.py`'s own opening sentence repeats it. Measured against the tree: **26 of the 75 `auto` rules are decided only by a rendered measurement** - DS-080 *a `<section>` per slide*, DS-081 *never fewer than 6*, DS-091's headline count, DS-092's sentence length, DS-202's bottom line and twenty-one more - and **2 `render` rules are decided with no browser at all**, DS-027 and DS-028. So `check.static_rows` decides **54** rule ids where the stage claims 68, and the `Check` column's *a build check can test it* is wrong for a third of the rules that carry it. **The consequence is the seeded-defect suite's reach**: `static_variants.py` seeds a defect into markup and requires the static half to notice, so those 26 rules are outside it by producer rather than by decision | Running `check.static_rows` over the reference deck and intersecting the ids it returns with the ruleset's `Check` column gives 54 decided, 26 `auto` absent and 2 `render` present. `grep -n "DS-081" tools/deck/audit.py` puts it in `render_verdicts`, reading `data` from the browser payload | [`EVALUATION.md`](EVALUATION.md) section 2's pipeline table, `tools/deck/audit.py`'s opening, and whatever decides which stage a rule belongs to | **A hypothesis, and it is that the document is what should move**: `DESIGN-SYSTEM.md`'s preamble already rules that *`Reach` never says which part of the gate does it - a rule checked in an unexpected stage is still `yes`*, and the same argument reaches `Check`. If it does, section 2's table should describe the stages by what they need - markup, a browser, a print, sources - which is the partition `check.NOT_STATIC` already maintains and fails the run over | `s` | [T-240](../tasks/T-240-the-evaluation-document-against-itself-and-the-code.md) | open |
| `PR-51` | the product | **Low** | **Two compiled regexes in `audit.py` are read by nothing, and the comment beside them argues for one of them as though it were live.** `PROVENANCE_MARK` and `MARK_HREF` are the only dead definitions in 181,662 bytes; `MARK_HREF` carries four lines explaining why it matches anchors rather than every `href` - *the denominator this row exists to make honest, made dishonest by the instrument* - for a constant no row reads. The comment immediately below says what happened, in the past tense: *These two used to sweep the region inside `<p class="provenance">`* - T-109 moved the component and `source_link_tags` replaced them | Parsing `tools/deck/audit.py` for module-level assignments and searching every tracked `.py` and `.md` for each name returns exactly these two at zero uses. The same sweep over all 80 functions returns none, which is the other half of the answer | `tools/deck/audit.py` lines 1141 and 1146, and the comment above them | Six lines. **Batch it** with `PR-05`, `PR-18`, `PR-22`, `PR-23`, `PR-28`, `PR-40`, `PR-41` and `PR-47`. **A hypothesis**: the argument in the dead comment is worth keeping and belongs on `source_link_tags`, which is the code that now carries the obligation - so this is a move of four lines and a deletion of two, not a deletion of six | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-52` | the product | **Low** | **`critique.py`'s docstring states the hard-judge checklist at 26 rules; the same file derives 29 and prints them.** Its opening argues that a reviewer should spend attention on *the five dimensions and **26** `hard` rules nothing mechanical can see*, and `judge_rules()` reads the count off the ruleset every run - deliberately, *so a rule added to `DESIGN-SYSTEM.md` has to change this worksheet without anyone editing this file*. **This is the third home of `PR-30`'s figure**: 26 in section 1.1 of [`EVALUATION.md`](EVALUATION.md), 24 in its section 8.1, 26 here, against a derived 29 | `grep -n "26 .hard. rules" tools/deck/critique.py` prints the docstring line; `python -c` calling `critique.judge_rules()` returns 29, and `python tools/deck/ruleset.py --gates` prints the same set under *gated by judgement* | `tools/deck/critique.py`'s module docstring | One number, and it should go rather than move. **Batch it** with `PR-51`. **A hypothesis**: the sentence works without a figure at all - *the five dimensions and the hard rules nothing mechanical can see* - which is what `PR-30` proposes for the two homes in the document, so all three want the same treatment and one decision | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-53` | the product | **High** | **The resolution contract samples four slides by fixed index, so every deck shorter than twelve loses DS-063 to *undecided* on every run - and the row blames the render.** `contract.SAMPLE` is slides 1, 5, 8 and 12, and the probe reaches a slide by clicking `next`, which `deck.js` clamps at the last slide. An eight-slide deck therefore measures slides 1, 5, 8 and 8 - the same slide twice - and `geometry()`'s duplicate guard, added by T-183 against a **dropped render**, correctly refuses to compare it. The verdict reads *the two renderings measured different slides, so the comparison is undecided* and advises *re-run; if it recurs the probe is dropping or repeating a slide* - which will recur forever, because nothing was dropped. **DS-082's default deck length is 8-12** and DS-081's floor is 6, so most of the legitimate band is affected; `undecided` is not a coverage fault, so the gate stays green | `grep -n "SAMPLE = " tools/deck/contract.py` prints the four indices, and `contract.audit` passes `which=None` through to them on the gated path. `sed -n '444p' shell/deck.js` is the clamp. Calling `contract.scale_verdicts_from` with a measurement whose last two rows carry one slide name returns DS-063 `None` with that message. All four shipped decks have 12 or more slides, which is why nothing shows here | `tools/deck/contract.py`'s `SAMPLE`, and DS-063's undecided message | **A hypothesis, and it is that the sample should be derived rather than widened**: `render.py` already reads a deck's slide count from the file and refuses to guess one, so four indices spread across *this* deck cost the same renders and cannot fall off the end. What is worth deciding at the same time is whether a duplicate the SAMPLER caused should read differently from one a dropped render caused - they need opposite fixes and the row currently says only the second | `s` | [T-230](../tasks/T-230-the-resolution-contract-samples-four-slides-by-fixed-index.md) | **closed 2026-08-29.** `SAMPLE = [0, 4, 7, 11]` is now `SAMPLE_SIZE = 4` and `sample_for(n)`, and `scale_verdicts` takes the length from `render.slide_count`, which reads the file and treats zero as fatal rather than rendering a guess. **The derivation was chosen for one property before any other: on a twelve-slide deck it returns `[0, 4, 7, 11]` exactly** - the constant it replaces, and the strongest evidence available that this is the right derivation rather than merely a different one. The self-test asserts that, and asserts the sample is duplicate-free and in range for every length 1-60. **Measured on decks truncated from the reference deck**: a six-slide and an eight-slide deck both get a **decided** DS-063 and both pass, while the old sample on the same eight-slide deck reproduces this row's *a run measured 'One transfer disappears' twice* - so the before and the after differ in the sample and in nothing else. **The row's second question is answered by making one cause impossible rather than by writing a longer message**: `sample_for` cannot produce a repeat, so a duplicate in the results is unambiguously the render's; a caller passing one is refused where the sample is known, with the opposite advice - *this is the sample and not the render: re-running reproduces it exactly*. A seeded dropped render still reads *only one run measured ...* |
| `PR-54` | the product | **Med** | **`theme.py`'s self-test builds its negative fixtures by editing the tracked theme's current text, so a legal change to the shipped theme makes the test accuse the validator.** Two assertions read `themes/quarto.css` and call `.replace("--lh-body:1.55", "--lh-body:2.6")` and a second one naming `--fs-lead`'s exact derivation. **Move the line height to 1.60** - inside DS-034's own 1.40-1.70 band, and exactly the change the parametric layer exists to permit - and the replace matches nothing, the fixture is the unmodified theme, `validate()` returns clean and the test exits *a line height outside DS-034's band validated clean*. `main()` runs the self-test before every command and `check_all.py` runs `theme.py check` once per deck, so **the release gate's step 1 goes red four times, naming the wrong thing.** `shell.py` fixed this exact pattern under T-176 and records it in a comment - *a self-test that reads it as current blocks `sync`, the one command that makes it current* | Reading `themes/quarto.css`, replacing `--lh-body:1.55` with `1.60`, and running the self-test's own two lines against the result: the second replace changes nothing, `theme.validate` on it returns no problems, and the assertion that demands problems fails. `sed -n '610,618p' tools/deck/theme.py` is the pair; `sed -n '770,779p' tools/deck/shell.py` is the fix one file over | `tools/deck/theme.py`'s `self_test`, the two `.replace` fixtures | **A hypothesis, and `shell.py` has already written it**: build the fixture rather than edit the repository's copy of it - a minimal `:root` with the tokens the assertion needs is a fixture nothing can move under. Where reading the real theme is the point, the assertion belongs on the theme conforming, which is the first of the three lines and is the one that should stay | `xs` | [T-243](../tasks/T-243-five-checks-bound-on-a-name-rather-than-on-structure.md) | **closed 2026-08-29.** The fixtures are built by naming the token rather than by matching its current value, and `set_token` **exits** on a substitution that changed nothing - the half that matters, since the defect was a silent no-op. Measured against the exact scenario the row describes: with `--lh-body` moved to 1.60, inside DS-034's own band, the old form's fixture caught nothing and the new form catches it and names DS-034 |
| `PR-55` | the product | **Med** | **`shell.py` states how many regions a deck varies in three different ways, and one of them is printed directly above the list that contradicts it.** `python tools/deck/shell.py parts` prints *The deck varies in **eleven** regions, and nowhere else:* and then lists **twelve**. The module docstring says `check` *cuts the same **ten** regions*; the `SKELETON` failure message an adopter reads says *the markup outside the **ten** regions differs*; the `SLOTS` comment and `sync`'s docstring both say **twelve**, which is the number. The two additions that made it twelve each label themselves in place - *Eleventh, added by T-019* and *Twelfth, added by T-114* - so the count was updated at the definition and nowhere a reader meets it | `python tools/deck/shell.py parts` prints the sentence and twelve rows. `grep -n "ten regions" tools/deck/shell.py` returns lines 20 and 651, `grep -n "twelve regions" tools/deck/shell.py` returns 63 and 419, and `python -c` printing `len(shell.SLOTS)` returns 12 | `tools/deck/shell.py` - the module docstring, `parts_report`, and the `SKELETON` message | Three numbers, and they should be one derived one. **A hypothesis**: `parts_report` already iterates `SLOTS`, so it can count them; the failure message can too. This is `PR-26`'s shape at one line's distance instead of 246, and the third document in this audit found stating a count beside the thing that answers it | `xs` | [T-245](../tasks/T-245-seven-tool-defects-in-tools-deck.md) | **closed 2026-08-29**, remedy as proposed. The count is derived from `len(SLOTS)` at both places a reader meets it - `parts`'s sentence and the `SKELETON` refusal - and the two docstrings that stated a number now name `SLOTS` instead of counting for it. Measured: `shell.py parts` printed *eleven* above twelve rows and now prints **12** above 12, with `len(SLOTS)` 12. No written-out region count remains in the file |
| `PR-56` | the product | **Med** | **The browser search names five Windows paths and four Linux binaries, and no macOS install - so a macOS adopter with Chrome installed is told there is no Chrome.** `render.BROWSERS` expands `%ProgramFiles%` and friends, then falls back to `shutil.which` over `google-chrome`, `chromium`, `chromium-browser` and `microsoft-edge`. On macOS Chrome installs to `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` and puts none of those four names on `PATH`, so `find_browser()` returns `None` and every render gate exits *no Chrome or Edge found - this harness measures in a real browser on purpose*. **The refusal is loud and correct in shape; the search is what is short.** [`../README.md`](../README.md) states the tooling as *pure Python standard library plus real Chrome* and *real Chrome or Edge*, with no platform caveat, and [`../CLAUDE.md`](../CLAUDE.md)'s publishing rules require the repository to be out-of-the-box - *clone and run, no path editing* | `grep -n "BROWSERS" tools/deck/render.py` prints the five Windows patterns; the `which` fallback three lines below names the four POSIX binaries. `grep -in "chrome" README.md` returns two lines and neither names a platform | `tools/deck/render.py`'s `BROWSERS` and `find_browser`, and whichever document should carry the platform statement | One path, or a sentence. **A hypothesis, and the choice is which**: adding the macOS application path costs one line and makes the claim true; saying on the front page which platforms the *tooling* runs on costs one sentence and makes the claim honest. **They are not the same fix and only the first keeps *clone and run* whole**, which is why this is a finding rather than a documentation note | `xs` | [T-245](../tasks/T-245-seven-tool-defects-in-tools-deck.md) | **closed 2026-08-29**, and the row's choice was decided rather than split. It offered one path or one sentence and said only the first keeps *clone and run* whole, so the path is what landed: `BROWSERS` gains the three macOS application bundles and the per-user Chrome beside them. The README needs no caveat because there is now none to state. Not measured on macOS - there is no macOS here - so this is a search that now names the right places rather than a verified install |
| `PR-57` | the product | **Med** | **Two of the four variant suites accept an anchor that matches more than once, and one anchor matches twice today - so `str.replace(old, new, 1)` decides which of two redundant paths the seeded defect lands in, and nothing says so.** `contract_variants.py` and `deliverable_variants.py` require an exact occurrence count and write down why: *a rename that silently hit a different number of elements than the variant's author believed would make the variant test something nobody wrote down*. `content_variants.py` and `static_variants.py` test `count(old) < 1` instead, in `build()` and in their self-test alike. The live instance is `static_variants.py`'s `reduced-motion-solidifies-the-flow`, whose anchor occurs at line 1025 of the reference deck under the `data-motion` attribute selector and again at line 1040 inside the `prefers-reduced-motion` media query. The seed takes the first, which is the path that wins - and the neighbouring variant in the same list carries the comment that says so: *seeding only the media query changed nothing measurable, the attribute rules carry higher specificity*. **Correct by accident**, which is `chrome_row.py`'s own phrase for the state this file is in | A scan of both suites' anchor lists against `examples/reference-deck.html` returns one anchor with count 2 and every other with count 1. Searching for the occurrence test prints an exact-count form in two files and a `< 1` form in the other two | `build()` and `self_test()` in `tools/deck/content_variants.py` and `tools/deck/static_variants.py`, and the one anchor that is not unique | Tighten both to the siblings' exact-count form, then re-anchor the DS-143 variant on the enclosing selector so it names which path it seeds. **A hypothesis in one respect only**: whether the media-query path should be seeded as well, as `reduced-motion-leaves-the-slide-blank` does, is a question about DS-143's coverage rather than about the anchor | `xs` | [T-243](../tasks/T-243-five-checks-bound-on-a-name-rather-than-on-structure.md) | **closed 2026-08-29.** Both suites take the siblings' exact-count form, in `build()` and in `self_test()`, and the DS-143 variant is re-anchored on `:root[data-motion="off"] \.current{animation:none}` - the path the seed already took, now said out loud. **A scan of both suites' anchor lists against the reference deck returned exactly one anchor with count 2 and every other with count 1**, so the change is safe and the one it named was the one. `static_variants.py` still ends 29/29, 10/10, 1/1, 2/2, 7/7, 1/1 |
| `PR-58` | the product | **Med** | **`rulerstrip.py` builds an exit code for every failure it can have and then throws it away - a run that found no browser prints its reason and exits 0.** Twelve of this cycle's fourteen tools end by passing `main()` to `sys.exit`; `longdeck.py` and `rulerstrip.py` call `main()` bare. In `longdeck.py` nothing is lost, because every failure there is already a `sys.exit`. In `rulerstrip.py` `run()` returns `3` for no Chrome and `2` for a marker render that produced no image, a scanner that returned nothing, a capture whose size disagrees with the located crop, and a probe that returned no result - five failure paths, all discarded. **The fix is not the one word it looks like**: both files return a path from `main()` on success, so wrapping the call as it stands would print the path and exit 1 | `rulerstrip.main([])` with `render.CHROME` set to `None` returns `3`, and line 490 calls `main` with no `sys.exit`. Searching every tool in the cycle for the entry-point call separates the two from the twelve | the entry points of `tools/deck/rulerstrip.py` and `tools/deck/longdeck.py`, and what `run()` and `write()` return | Return `0` on success and let the path go to stdout where it already goes, then pass `main()` to `sys.exit`. **The hypothesis is whether `longdeck.py` is worth touching at all** - it has no code to lose, so the case for changing it is uniformity rather than a defect | `xs` | [T-245](../tasks/T-245-seven-tool-defects-in-tools-deck.md) | **closed 2026-08-29**, remedy as proposed, including the half the row called a hypothesis. `rulerstrip.run()` returns `0` on success - the path still goes to stdout, where it already went - and `__main__` passes `main()` to `sys.exit`. Measured: `rulerstrip.main([])` with `render.CHROME` set to `None` returns **3**, which the shell now sees and previously did not. **`longdeck.py` changed too**, on the row's own uniformity argument rather than on a defect: nothing is lost there today, and the next failure path added would have been discarded in silence. Neither tool's exit code is read by `check_all.py`, which skips both with a stated reason |
| `PR-59` | the product | **Med** | **`quickview.py` escapes the source title in three places and not in two, so a deck whose markup is correct is refused and told its specification is wrong.** `wired_pattern`, the control's `data-qv` and its `data-file` all escape the title; `item_pattern` matches the raw one, and the control's own label is written raw. A source titled with an ampersand - *Fleet and cost model* written with the sign, *Costs and benefits* likewise - is therefore matched against markup that must carry the entity, no item is found, and `wire()` raises the T-069 refusal: *no provenance item reads it in this deck ... if the slide does not cite it, that is a specification question and not this tool's*. **The blame is the defect**: the slide does cite it, and the author is sent to fix a specification that is already right. The mirror half writes an unescaped title into the button label, so a title carrying an angle bracket goes into the deck as markup | Calling `wire()` with an ampersand in the title, against a deck citing the same title as an entity, raises `Refused` with the T-069 wording. The asymmetry is three escaped uses against one raw pattern and one raw label in the same function pair | `item_pattern` and `wire`'s control template in `tools/deck/quickview.py` | Escape the title before it becomes a pattern, and escape the label. **A hypothesis in scope**: `carried()` returns the escaped title and `check` compares it against the raw one from `--source`, so the same edit has to decide which form is the tool's currency rather than fixing two call sites | `xs` | [T-245](../tasks/T-245-seven-tool-defects-in-tools-deck.md) | **closed 2026-08-29**, and the row's in-scope hypothesis is what decided it. The fix is not two call sites but one ruling: **the raw title is this tool's currency**, escaping happens at the markup boundary and is undone at it. So `item_pattern` escapes, the control's label escapes, and `carried()` unescapes what it reads out of `data-qv` - which is what stops `check` reporting a carried view as MISSING. Measured on a source titled with an ampersand: `item_pattern` now matches the markup a correct deck carries, `wire()` no longer raises the T-069 refusal, and `carried()` reads the title back as itself. Four self-test fixtures hold it |
| `PR-60` | the product | **Low** | **`printgeom.py`'s footnote clearance reports a pass when it found no footnote, in a file whose other two guards refuse exactly that.** `verdicts()` tests that a footnote was both read and declared before measuring, so a sheet the deck says carries no footnote - or a printed page whose text runs the reader did not find - contributes nothing to the collision list, and `PRINT-3` prints *footnote clearance: every card ends above it* and passes. Beside it, `sheet_problem` makes nought cards read against thirteen declared a failure of the gate, and `printpages.sheet_count` says in its own docstring that *zero is a problem rather than an answer*. **Latent rather than live**: `shell/deck.js` appends the footnote to every sheet unconditionally and says why, so no deck this shell builds can reach it | The footnote is one unconditional `appendChild` in `shell/deck.js` with no branch above it; the guard is the two-term test in `verdicts()`. T-051 is the rule, and `PR-53` at cycle 12 is the same class caught live | `verdicts()` in `tools/deck/printgeom.py` | Make a sheet that declares a footnote and reads none an unread entry, the way a card-count disagreement already is. A sheet that genuinely carries none is a different answer and should say so rather than pass silently | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-61` | the product | **Low** | **Two tools build their self-test out of bare `assert`, which the interpreter's optimise flag removes - and one of them is the instrument whose whole argument is that a check nobody has watched fail is a claim about the instrument.** `fps.py` carries eight `assert` statements in `self_test()` and `presenter.py` is the only other file under `tools/` that does; the remaining forty-eight exit with a `SELF-TEST FAILED` message, which no flag can strip. Optimised, `fps.py` reports its build and its instructions with nothing whatever asserted about the injection - including the two silent failures its own docstring names | A search for a statement-initial `assert` across every tracked tool returns exactly `tools/deck/fps.py` and `tools/deck/presenter.py`. `fps.py` holds eight | `self_test()` in `tools/deck/fps.py`, and `tools/deck/presenter.py` for the same reason | Replace both with the exit form the other forty-eight use. **Not a hypothesis about the fix, only about the boundary**: whether the repository wants a stated rule that a self-test may not be an `assert` is cycle 40's to decide | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-62` | the product | **Low** | **The one document naming `longdeck.py` and `rulerstrip.py` does not say how to run either, and the same table hands six other tools a command line.** This cycle's brief asks which tools are reachable from a documented command, which only from a task record, and which from neither. Measured across the tracked tree, three of the fourteen appear in no document at all - `longdeck.py`, `rulerstrip.py` and `paths.py`. `paths.py` is reachable by import from fourteen callers, so its reachability is real. The other two are named only by `check_all.py`'s not-run reason and by T-178, which is `done`. **And of this cycle's eight not-run entries exactly one gives a command** - `fps.py`'s *Run it by hand* - against six elsewhere in the same table: `density.py`, `figgrid.py`, `glitchfree.py`, `markhits.py`, `presenter.py` and `cycles.py` | A scan of every tracked file for each tool's file name, grouped by where the hit lands, returns nothing outside `tasks/` and `check_all.py` for those three. Counting invocation lines inside the not-run bodies gives the one against six | the not-run bodies for `longdeck.py`, `rulerstrip.py`, `chrome_row.py`, `print_variants.py`, `printgeom.py`, `printpages.py` and `paths.py` in `tools/check_all.py` | Add the invocation to each body, in the form six entries already use. **The hypothesis is whether that is enough**: a tool whose only home is a table of things that do not run is documented in the place a reader goes last | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-63` | the product | **Low** | **The paragraph explaining the authored-box constant in `contents_bound.py` sits about forty lines above the constant, with the whole split-rule block between them.** The comment runs *13, not 12, and the difference is a section rather than a slide*, then *Deliberately not derived*, and then ends - and what follows it is the split rule and its three invariants. The constant and its check come after all of that. A reader arriving at the constant finds no reason beside it, and a reader arriving at the comment finds the split rule | Reading `self_test()` in `tools/deck/contents_bound.py` puts the *13, not 12* paragraph and the constant either side of the split-rule block | `self_test()` in `tools/deck/contents_bound.py` | Move the constant and its check up to the paragraph that explains them, or the paragraph down. No behaviour changes either way | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-64` | the product | **Low** | **`quickview.py` raises `IndexError` when the output flag is the last argument, where every other argument in the same parser answers with a usage line.** `main()` reads the value at the flag's index plus one with no bound test, three lines after `--source` is read with an explicit bound test and a missing value exits with *--source takes title=path*. So a command ending in a bare output flag ends in a traceback rather than in the message the same function was written to give | The unguarded index-plus-one read in `main()` against the guarded bound test on the line above it | `main()` in `tools/deck/quickview.py` | The same bound test the neighbouring branch already has, and the usage line the rest of the parser gives | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| ~~`PR-65`~~ | project method | **Med** | **Four documents say the coverage partition reports its verdict *before* it answers, and the code prints it after - including the tool's own docstring.** `complaint()` in `tools/docs/cycles.py` says *it answers only after saying whether the partition still holds*; `check_all.py`'s not-run entry, [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 2 step 2 and this register's own header all say *reports the whole partition's verdict before it answers*. `main()` prints the cycle header, the file list and the byte total, and only then returns `complaint(...)`, which is where the printing happens. **The stated reason is what the ordering was for**: section 2 says a tracked file that has fallen outside every cycle *stops the reading rather than surviving it*, and a reader who takes the file list and starts reading has already passed the place the complaint appears **Raised a second time as `PR-96` at cycle 24 and withdrawn at cycle 40**, whose id is spent and not reused; the one thing it added is here - on a partition that holds, `--cycle` prints **no verdict line at all**, in every mode but the default, so the ordering is not merely late but absent and only the exit code carries it. | Seeded in memory - one tracked path no cycle's rules match, with nothing on disk changed - `--cycle 14` prints the five files and the total first, then `UNASSIGNED` and the verdict, and exits 1 | `main()` in `tools/docs/cycles.py`, plus the three documents that restate the claim | Two lines: compute the verdict, print it, then answer. That makes all four documents true at once and is why it beats correcting four sentences. **The hypothesis is whether the exit code was ever the real guard** - it is 1 either way, and if that is what stops the reading then the ordering claim should go rather than the code move | `xs` | [T-246](../tasks/T-246-two-docs-instruments-misdescribe-their-own-behaviour.md) | **closed 2026-08-29** — the code moved rather than the four sentences, and the row's own hypothesis was tested: **the exit code was never the guard.** Nothing consumes it — `check_all.py` names this tool as not-run with the reason, and every consumer in the tree is a person or an agent reading the printed output at a cycle's step 2 — so the printed ordering is the whole of it and the ordering claim stays. `verdict()` is now the first thing every mode prints, and `complaint()` prints a line on a holding partition instead of returning 0 in silence. Two guards added and both seeded red: a mode returning the verdict last, and a holding partition that says nothing. **Running the fixed command found a live breach on its first run** — `docs/REMEDIATION-ORDER.md` was in no cycle at all; it is cycle 7's now |
| `PR-66` | project method | ~~**Med**~~ | **Withdrawn: this is `PR-01`, raised a second time fourteen cycles later.** Cycle 14 found that three documents call the blindness fixture one of *the five shipped decks* and raised it without checking the register for the same defect. `PR-01` states it from cycle 0, in the same words and with the same consequence - *cycle 17 as written renders a fixture built to fail and reads the reds as findings*. **The one thing this sighting adds has been moved onto that row**: `tools/docs/cycles.py`, which did not exist at cycle 0, states the subject correctly in its own rule table, so the disagreement is now three documents against the tool that decides the membership rather than against `check_all.py`'s classification alone. **The id is spent and not reused**, per this register's own rule | A search of the register for the fixture's filename returns `PR-01` and this row. Both name cycle 17, [`AUDIT-METHOD.md`](AUDIT-METHOD.md) section 2 and [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 1's grade table | nothing - `PR-01` carries the remedy | none. What this row cost is a lesson rather than a fix: **a cycle checks the register for its own finding before raising one**, which forty-three cycles appending to one document makes likely rather than possible, and which nothing in the method asks for | `xs` | - see `PR-01` | **withdrawn 2026-08-23**, at cycle 16, when the next session's subject was resolved and turned out to be the row this duplicates |
| ~~`PR-67`~~ | the product | **Med** | **`figures.py` says adjacency is the whole of the binding and `bind()` pairs a command with the next fence at any distance.** The docstring reads *an unlabelled fence directly after a ```bash fence is that command's output* and argues that a list of which block goes with which command would be a second copy of what the page states by layout. What the loop does is carry `pending` forward until some later fence is reached, however much prose, however many headings lie between. Constructed and run: a `bash` fence at line 3 and an unrelated unlabelled fence at line 39, two sections later, are reported as `command` and `output` of one pair. **The page is held together by the exclusion table instead**: two `EXCLUDED_FENCES` entries give as their reason *no output is pasted under it*, which is the state true adjacency would have handled without a declaration | A synthetic page with one command fence, twelve filler paragraphs and two headings, then an unlabelled fence, returns `command line 3` and `output line 39` from `bind(fences(doc))` | `bind()` in `tools/docs/figures.py`, and the docstring beside it | Pair only when the output fence opens within a line or two of the command's close, and let anything further be `UNDECLARED`. **A hypothesis about what it uncovers**: two exclusion entries exist because their commands print no output, and under real adjacency at least one of them stops being needed - which is the way to tell the fix worked | `s` | [T-246](../tasks/T-246-two-docs-instruments-misdescribe-their-own-behaviour.md) | **closed 2026-08-29** — `bind()` measures adjacency and `GAP` is **derived, not chosen**: across every tracked document the command-to-output gaps were 2, 2, 2, 2, 2, 73, 113, so five genuine pairs sit at the minimum a closing fence and a blank line allow and there is nothing between them and the two false ones. Both false pairs are named in the docstring. **The row's own hypothesis held, and for exactly one entry**: with adjacency real, `python tools/deck/check.py examples/sort-window` no longer needs excluding and was removed, while `taskmd check` still does — measured by removing each and re-running. Guarded both ways, the far case and the near one, and both seeded red |
| `PR-68` | the product | **Low** | **`cycles.py` names a `--sizing` flag it does not implement, and runs the whole partition report for any argument it does not recognise.** The comment above cycle 30 reads *`--sizing` reports when one has drifted far enough to need cutting again*; `main()` dispatches on `--cycle`, `--list` and `--plan` and falls through to `report()` for everything else. The capability is real - `report()` prints an `OVERSIZED` line per cycle over the sizing bound - it simply has no flag. The silent fallthrough is the sharper half: a session at section 2's step 2 typing `--cycl 14` gets the full 43-cycle partition rather than a message, where `refcheck.py` and `lessons.py` both print usage and exit 1 on an argument they do not know | `python tools/docs/cycles.py --sizing` prints the default partition report. `main()`'s three branches, and `refcheck.main` / `lessons.main` rejecting an unknown first argument, are the comparison | `main()` in `tools/docs/cycles.py`, and the comment naming the flag. `figures.py` accepts an unknown argument the same way, with less consequence | Reject an argument that is not one of the three, and either implement `--sizing` or delete the name from the comment | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-69` | project method | **Low** | **Cycle 14's brief says *the four checkers that read documents* and the cycle holds five files.** `tools/docs/` gained `cycles.py` on 2026-08-23 through [T-223](../tasks/T-223-derive-the-audit-cycles-membership-instead-of-counting-it.md), the remedy this audit raised for `PR-06`; the Files and Bytes columns re-derived to 5 and 188,895 because they are printed, and the Brief column did not because it is hand-kept. So the cycle's own instruction under-counts its subject by the file the audit itself added. The two named in the brief - `figures.py`'s size and `refcheck.py`'s skip list - are still the right questions; what is missing is any question about the partition tool, and this cycle's three other findings all came from it | `python tools/docs/cycles.py --cycle 14` returns five files against a brief naming four. The Files and Bytes columns beside it are derived and agree | cycle 14's Brief cell in [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 2 | Say five, and name the partition tool. **The general hypothesis is worth more than the row**: a derived Files column beside a hand-kept Brief means every future re-cut leaves a brief describing the old membership, and `PR-06` is the same shape one column over | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| ~~`PR-70`~~ | the product | **High** | **The release gate's packaging tool has two checks with no subject at all: check 7 reads nought of the skill's eighteen documented commands, and check 5 resolves one path against the forty-eight the skill actually writes.** `check_scaffold.py` says it decides *every command a skill documents names a tool that exists, and a subcommand and flags that tool's source knows* - the check an adopting project bought with a crash at the step that closes the visual gate, where `build.md` documented a `--out` flag `render.py` did not have. Its `COMMAND_RE` requires an invocation written from `${CLAUDE_PLUGIN_ROOT}`. **Commit `2e31c20` of 2026-08-20 - *Make the gate say what failed, and the docs say what runs* - moved the whole skill onto `$HTMLDECK`**, a placeholder `SKILL.md` section 0 defines and the agent substitutes. Since that day `build.md` and `critique.md` carry eighteen invocations and check 7 matches none of them; `check_scaffold`'s eight check-7 fixtures all write `${CLAUDE_PLUGIN_ROOT}`, so the self-test stays green over a synthetic subject. **The package is not broken - all forty-eight paths resolve today - the instrument is blind**, and the run's closing line, *every `${CLAUDE_PLUGIN_ROOT}` pointer resolves*, is literally true of the single pointer left | Scanning every skill markdown file for `COMMAND_RE` returns 0 matches; scanning outside fences returns 1 `${CLAUDE_PLUGIN_ROOT}` path against 48 written as `$HTMLDECK`, and the one is `SKILL.md`'s. `git log -S` dates the placeholder change to `2e31c20`, 2026-08-20, and check 7 to 2026-08-10. Every one of the 48 resolves in this clone | `COMMAND_RE`, `check_paths` and `BARE_RE` in `tools/plugin/check_scaffold.py`, and the fixtures beside them | Teach both to read the skill's own placeholder as well as the manifest one, and add a fixture written the way the skill is written. **The hypothesis is where the placeholder is declared**: the check would have to learn `$HTMLDECK` from `SKILL.md` section 0 rather than hold a second copy of the name, or the next rename repeats this. **And the general form is worth cycle 40's attention**: an instrument whose subject can empty out silently needs the denominator printed, which is what `static_variants.py` prints for its producers and this tool does not | `s` | [T-231](../tasks/T-231-two-packaging-checks-have-no-subject-at-all.md) | **closed 2026-08-29** — the hypothesis held. The base is read from `SKILL.md` §0 and check 7 went 0 of 18 to 18 of 18, check 5 one path to 49; the denominator is printed and an undeclared base now refuses. Seeded by reproducing the 2026-08-20 state against this tree: 24 problems where it printed `OK` |
| `PR-71` | the product | ~~**Med**~~ | **Two specifications and the tool's own docstring describe a stacked area chart the deck has not carried since it was rewritten, and the tool says the deviation is recorded where it is not.** `fig_area`'s docstring states the case plainly - the stacked form rendered as two shapes because DS-020 allows one accent hue - and closes *the deviation from the reviewed specification is recorded in `portfolio-review.slides.md`*. It is not: that document still says *A stacked area chart across the full content column*, *five sectors stacked to 100%*, *Renewables is the bottom band*, and *The five bands Rise in stacking order*, with no note anywhere. `portfolio-review.foundation.md`'s Visuals row says *a stacked area (4)*, and the module docstring still names stacked area as one of the three kinds this file contributes to T-113's measurement. Only [`../docs/research/R9-embeddable-chart-library-versus-hand-authored-svg.md`](research/R9-embeddable-chart-library-versus-hand-authored-svg.md) has it right, as history | `fig_area()` emits five `<path>` and no `<rect>`, and its aria-label reads *one line each*. Searching both specifications for a deviation note returns one hit, and it is *standard deviation of quarterly valuation movements* on another slide | `fig_area`'s docstring and the module docstring in `tools/examples/portfolio_charts.py`; the Structure, Text, Visuals and Animations lines of slide 4 in `examples/portfolio-review/portfolio-review.slides.md`; the Visuals row in `examples/portfolio-review/portfolio-review.foundation.md` | Write the deviation where the tool says it is, and correct the two specifications. **The hypothesis is which direction**: a specification is a record of what was reviewed, so amending it silently is the other failure - the note belongs beside the original wording rather than instead of it, which is how the register's own struck-through rows work | `s` | [T-247](../tasks/T-247-the-portfolio-generators-documents-against-the-deck.md) | **closed 2026-08-30.** The remedy's direction was taken as written: the note goes **beside** the reviewed wording, not instead of it. Slide 4 of `portfolio-review.slides.md` keeps every line the reviewer saw and gains an *As built* item recording the deviation and its reason; the foundation's Visuals row does the same. `fig_area`'s docstring now names both homes and says plainly that it was recorded nowhere until this task, which is the half of this row that mattered. The module docstring's third chart kind is corrected with it - the three this file adds to the probe's four are a multi-series line chart, a waterfall and a scatter. |
| `PR-72` | the product | ~~**Med**~~ | **The deck carries ten charts and three places say seven.** `FIGURES` holds ten builders and the shipped deck holds ten `svg class="fig"` elements. Against that, `portfolio_charts.py`'s opening argues its own length as the honest cost of hand-authored SVG *when a deck needs seven charts instead of one*; its section banner reads *The seven figures*; and `portfolio-review.foundation.md` says *Seven charts, all hand-authored SVG* and then lists seven by slide, omitting the curve on slide 2, the limit bar on slide 3 and the tranche bars on slide 10. **The figure feeds a recorded evaluation** rather than only a sentence: T-113 costs hand-authored SVG against a chart library, and the denominator of that cost is the chart count | `len(FIGURES)` is 10 and the deck holds 10 figure elements; the three statements of seven are in the module docstring, the section banner, and the specification's Visuals row | the module docstring and the section banner in `tools/examples/portfolio_charts.py`, and the Visuals row in `examples/portfolio-review/portfolio-review.foundation.md` | Say ten and list ten. **The hypothesis is whether the count should be derived at all**: `len(FIGURES)` is printed by the self-test's own total already, and a banner stating a number the file computes is the shape `L-13` names | `xs` | [T-247](../tasks/T-247-the-portfolio-generators-documents-against-the-deck.md) | **closed 2026-08-30, and the remedy's hypothesis is refused on the measurement.** It reads *`len(FIGURES)` is printed by the self-test's own total already*; it is not. The self-test printed `%d of %d checks passed` from `len(ran)`, which is **33** - the number of checks, not the number of figures - so a reader had no printed count to rely on and the banner was the only place a number appeared. So the count is now genuinely printed, `10 figure builder(s) in FIGURES`, and the banner carries **no number at all**: one home for one quantity, which is what the hypothesis was reaching for. The module docstring's argument says ten, and the foundation's Visuals row keeps the reviewed wording and adds what shipped, listing all ten by slide. Measured: `len(FIGURES)` is 10 and the deck holds 10 `svg class="fig"`. |
| `PR-73` | project documentation | **Low** | **`check_scaffold.py`'s partition calls `chart_probe.py` a research instrument and does not say that a gate imports it.** `NOT_RUN` reads *a research instrument for T-006 - it generates the four chart types as hand-computed SVG to settle whether a charting library is needed*. True of its own entry point, and incomplete: `tools/examples/portfolio_charts.py` imports `guard_height`, `guard_label`, `label_box`, `ChartError` and `MIN_BAR_PX` from it, and that file **is** a gate - `portfolio_charts.py selftest` runs on every release. So the three guards run on every release run, through an import the table does not mention. **The table already has the vocabulary**: `density.py`, `contrast.py`, `contract.py` and `audit.py` all say *runs inside `tools/deck/check.py`, which imports it* | Scanning every tracked tool for an import of each of this cycle's nine returns exactly one edge, `portfolio_charts.py` importing `chart_probe`. `check_all.py --list` shows `portfolio_charts.py selftest` among the commands that run | the `NOT_RUN` entry for `tools/assets/chart_probe.py` in `tools/check_all.py` | Add the import to the reason, in the words the table already uses four times | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-74` | the product | **Low** | **Two of the three tracked tools that define no self-test are this cycle's, and unlike the third neither says why.** `tools/tasks/query.py` has none and states the reason in its `NOT_RUN` entry - it has no verdict to give, and the one part that could be wrong, the locator it shares with `lint.py`, is asserted in that file's self-test. `tools/assets/build_probe_deck.py` and `tools/portability/run_probes.py` state nothing. **The second is the one that matters**: `build_probes.chunk_text` is fixture-tested in both directions, including *chunk_text is not lossless*, and `run_probes.harvest_title_channel`, which reassembles those chunks by index, is asserted by nothing - while its own comment says an off-by-one there silently truncates a result | Searching every tracked tool for a self-test definition returns 3 of 50 without one: `build_probe_deck.py`, `run_probes.py` and `query.py`. Only `query.py`'s `NOT_RUN` entry explains the absence | `tools/portability/run_probes.py` and `tools/assets/build_probe_deck.py`, and their `NOT_RUN` entries in `tools/check_all.py` | A fixture for the reassembly half of the title channel, and a stated reason in the other entry. **The hypothesis is whether a stated reason is enough for the second**: a builder that reads a cache and writes a file has little to assert, and saying so is cheaper than inventing a check | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-75` | the product | **Low** | **`portfolio_charts.py` recomposes a tracked 397 KB deck for any argument that is not `selftest`.** Its entry point tests one word and falls through to `compose()`, which rewrites `examples/portfolio-review/portfolio-review.html` in place - so `selftests`, or any typo, does what the no-argument form does. The no-argument form is the documented way to compose the deck and is correct; what is missing is the refusal every other tool in this cycle makes. `measure.py` uses `argparse` with `choices`, `kb/extract.py` prints its docstring and exits 1, `run_probes.py` names the argument it does not know. **It is the third instance of the family `PR-68` names**, after `cycles.py` and `figures.py`, and the first where the fallthrough writes a tracked file | The entry point's single equality test against `selftest`, against the three refusals in the same cycle. `compose()` opens the deck for writing unconditionally | the entry point of `tools/examples/portfolio_charts.py` | Refuse an argument that is not `selftest`. **The hypothesis is whether the shipped deck should be re-derivable at all without a check**: `seed_defects.py --check` exists so the seeded deck can be proved to still derive from its parent, and this deck has no equivalent - a recompose that changed it would be caught by nothing here | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-76` | project documentation | **Low** | **`tools/kb/extract.py` implements four commands and documents three.** Its `COMMANDS` table holds `inventory`, `decks`, `libs` and `selftest`; the usage block in its own docstring lists `inventory`, `decks` and `selftest`. `libs` is the one that answers *which libraries the decks reach for, and whether they are linked or vendored* - the measurement behind T-013's whole argument about external references - and the only way to learn it exists is to read the bottom of the file. **This is `PR-62`'s question one directory over**, and here the answer is that the command is reachable from no document at all, its own included | `COMMANDS` names four keys; the docstring's usage block names three. An unknown argument prints the docstring, which is therefore also the help text that omits it | the usage block in `tools/kb/extract.py`'s module docstring | List the fourth command where the other three are listed | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-77` | the product | ~~**Med**~~ | **`--accent-ink` is four hand-chosen colours across two themes and two colour schemes, and nothing anywhere reads it.** [`THEME-CONTRACT.md`](THEME-CONTRACT.md) types it a colour primitive - *The accent as text on the ground* - `theme.py` parses that contract rather than restating it, so every theme owes it; both themes declare it light and dark (`#0C4D50` / `#7FD6DA` and `#4A3D78` / `#C4BAF0`); all five tracked decks carry it. `var(--accent-ink)` appears nowhere in the tree, and no tool reads it by name the way `audit.py` reads `--motion-kind`. **This is `PR-36` in the colour band and is not that row again**: cycle 9 searched the motion set and found `--turn-dur` and `--turn-ease`; this cycle measured all 138 tokens the shell and the two themes declare, and the colour band has one too. The theme's own claim - *every pair `contrast.py` measures clears 4.5:1* - is being maintained for a value no surface renders | Of 138 declared tokens, 7 have no `var()` reader. Four of the seven (`--motion-kind`, `--motion-long`, `--motion-subject`, `--pager`) are read by `audit.py`, `density.py`, `shell.py` or `static_variants.py` and are live. Of the three left, two are `PR-36`'s and the third is this one | the `--accent-ink` row in [`THEME-CONTRACT.md`](THEME-CONTRACT.md), and its four declarations in `themes/quarto.css` and `themes/lattice.css` | Either give it the surface the contract describes - accent-coloured text on the ground has no component today - or retire the row and the four values with it. **The hypothesis is which**, and cycle 40 has the same choice to make about `PR-36`'s pair, so the two should be decided together rather than one at a time | `xs` | [T-275](../tasks/T-275-retire-accent-ink-from-the-contract-the-themes-and-the-decks.md) | **closed 2026-08-30**, by `T-275`, in one change as its scope required. **Twelve declarations removed, not ten**: four in the two themes and **two in each of the four tracked decks**; the row's *five tracked decks* counts `reference-deck-seeded-defects.html`, which is the blindness fixture and is derived rather than tracked, and it was re-derived. The three `*-presenter.html` derivations were rebuilt too - they are gitignored, so nothing gated them and they would have carried a retired token. The contract row is gone from `THEME-CONTRACT.md` section 3.2 and its section 3.6 paragraph now records the ruling in the past tense rather than announcing it. `theme.py validate` green on both themes and `check` green on all four decks. |
| `PR-78` | the product | **Med** | **Three documents describe how variable the chrome row's tail is, and they disagree - so a legal tail produces a deck that refuses to start, and two of the three gates pass it.** `shell/shell.html` calls the region a slot *because what sits here varies by deck*. `tools/deck/shell.py` bounds it to exactly two forms, both carrying the same five ids. `shell/deck.js` guards three of those five - `if (!moreMenu) return`, `if (!rulerRing) return`, `moreBtn && moreMenu` - and dereferences the other two without a test: `toDoc.addEventListener(...)` and `motionBtn.textContent` inside `setMotion`, which runs at module scope. **Measured in real Chrome**: the reference deck with its `Read` button removed and nothing else changed reports `data-preflight="fail"`, 0 of 13 slides current, 0 ruler ticks, and the reader is shown *This deck could not start in this browser (Uncaught TypeError: Cannot read properties of n...)*. `shell.py check` calls that deck *the shipped shell unchanged*; `component.py check` passes all four of its DS-229 rows. Only the render gate sees it, as GF-2 and GF-8 | A copy of `examples/reference-deck.html` with the one `Read` button line removed: `shell.py check` exit 0, `component.py check` exit 0, `glitchfree.py` two failing conditions, and a probe reading `data-preflight` back out of a real render returns `fail` | the unguarded dereferences of `toDoc` and `motionBtn` in `shell/deck.js`; and whichever of the three statements about the tail should give way | Guard the two the way the other three are guarded, which costs two lines and makes the script agree with the shell's own description of the region. **The hypothesis is the wider one**: if the tail is really bounded to two forms then `shell.html`'s comment is what is wrong, and `shell.py check` should compare the tail against those two rather than accept any - the current arrangement is the only one where all three statements are false together | `s` | [T-245](../tasks/T-245-seven-tool-defects-in-tools-deck.md) | **closed 2026-08-29**, the narrow half; the wider one is left open on purpose. `toDoc` and `motionBtn` are guarded the way the other three ids already were, and **`toStage` with them** - the register names `toDoc`, and its twin one line below is the same dereference on the same slot, so guarding one would have left the identical crash reachable by removing the other button. `setMotion` still sets `root.dataset.motion` when the button is absent, because motion is a property of the deck and only the label is a property of the control. **Measured in real Chrome, both directions**: the deck at `HEAD` with its `Read` button removed reports `data-preflight` **fail** and **0 of 13** slides current; the same fixture on the fixed shell reports **none** and **1 of 13**, and so does the fixture with the motion button removed. **The wider hypothesis is not answered** - whether the tail is really bounded to two forms, and so whether `shell.html`'s comment or `shell.py check` should give way, is a rule question the order's section 4 does not cover. The script now agrees with the shell's own description of the region, which is the half that was a defect |
| `PR-79` | the product | **Low** | **The degraded state hardcodes one theme's palette, and its own rule explains why the values must be literal without saying whose they are.** The `:root[data-preflight]` block carries the only literal colours in the shared component block outside `@media print`, and the block states the reason plainly: *THIS BLOCK MAY USE NO CUSTOM PROPERTY... Literal values only*, because the capability it compensates for may be custom properties themselves. That is right. What it does not say is that `#23211d` is `quarto`'s `--ink` exactly, and that `#f6f1e4`, `#b9b2a4` and `#ddd6c6` are warm neutrals matching no token in either theme - so a deck on `lattice`, whose ground is a cool near-neutral and whose ink is `#1E201F`, degrades into the other theme's family. `CLAUDE.md` rule 4 wants variety to come later from a tool that generates new templates; every template it generates degrades into `quarto` too | The four hexes against both themes: `#23211d` is `quarto.css`'s `--ink`; the other three appear in neither theme. The block's literal-only rule and its reason are in the comment above it | the `:root[data-preflight]` block in `shell/components.css` | Neutral values rather than one theme's - the degraded state is a plain document and owes no palette - or a sentence in the block saying the look is fixed and why. **The hypothesis is that tokenising is the wrong fix and would break the block**, which is what makes this a note about the choice of literals rather than about their being literal | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-80` | the product | **High** | **The two content entrance motions collapse for the motion control and for reduced motion, and not for print - so what prints is the state before they run, and the marks are not on the paper.** `.dot-pop circle` animates from `transform:scale(0)` and `.arrow-pop marker path` from `scaleX(0)`, both with `animation-fill-mode:both`, and a print rendering never advances an animation - so `both` paints the FROM keyframe. `@media print` in `shell/components.css` states the general rule while fixing one selector - *Entrance animations hold their pre-animation state until played, so an unplayed slide prints blank or half-risen (DS-224)* - and then names `.rise` and nothing else. **Measured on paper, on two shipped decks**: `measure-first.html` slide 6 prints its six-dimension data quality scale with **all 30 dots gone**, the five open positions and the one filled mark on every row, under a caption reading *Two Poor, two Fair, two Good* and beside a legend whose square swatches print normally; `reference-deck.html` slide 9 prints its month-18 gate with **both branch arrowheads gone**, so a decision diagram reaches paper with undirected lines. This is DS-224 exactly, which `check.py` excuses as `[look: a person, rule 6]` - the gate is right that it cannot see it - and `printgeom.py` is green on the very pages, because `PRINT-2` and `PRINT-3` read contents-card rectangles and nothing else | `grep -n 'dot-pop' shell/components.css` prints the three collapse sites and shows which one is missing: the motion control at 738-739, `@media (prefers-reduced-motion:reduce)` at 754, and inside `@media print` from 1048 the only entrance rule is `.rise` at 1097. **Both directions were measured** (**L-05**): printing each deck through `printgeom.py`'s own path and rasterising the PDF shows the marks absent, and re-printing the same two decks through a probe carrying one extra rule - `@media print{.dot-pop circle,.arrow-pop marker path{animation:none!important;transform:none!important}}` - restores all 30 dots on the `measure-first` sheet and both arrowheads on the `reference-deck` sheet, with nothing else changed. `.pulse` is unaffected: its FROM keyframe is `scale(1)`. So is `.opening`: print hides the panels | [`../shell/components.css`](../shell/components.css) - the `@media print` block, beside its `.rise` rule | Add `.dot-pop circle,.arrow-pop marker path{animation:none!important;transform:none!important}` next to `.rise`, and re-print. **This half is measured rather than hypothesised** - the probe above is the change, and it is the declaration the other two collapse sites already carry. **The hypothesis is the wider half**: the print block collapses entrance motion one selector at a time, which is the shape that let two of three motions be missed here and, per the note at line 754, let the same two be missed once before in the reduced-motion block. Whoever fixes it decides whether the three sites become one list. Nothing detects it either way - DS-224 is a `look` | `s` | [T-232](../tasks/T-232-two-entrance-motions-do-not-collapse-for-print.md) | **closed 2026-08-29**. Both halves taken. The measured half is the declaration this row names, beside `.rise`. **The wider half was taken as a check rather than as one list**: `component.uncollapsed_motions` reads the deck's own `@keyframes` and fails any motion whose first painted frame is empty and which `@media print` does not switch off, so a motion added later carries its own obligation and nobody maintains a list. **The first form of that check compared the three collapse sites, and its own measurement refused it** - nine selectors reported, two real, because print hides the whole chrome and a looping dash prints its first frame correctly (**L-142**). **And this row's `.opening` is unaffected is wrong**: the print block sets `.slide .sources-box{display:block!important}`, so a box the reader had opened printed as an empty space - a third instance, found by the new check on its first run and fixed with the other two. `DS-224` is no longer only a `look`: its excusal in `check.py` is closed, the mechanical half is a verdict, and the paper half is two rows in [`OWED-LOOKS.md`](OWED-LOOKS.md), owed after B12 rebuilds the decks |
| `PR-81` | the product | ~~**Med**~~ | **The reference deck's claim slide spends $5.6M on a package its own ask prices at $4.1M, and holds $1.5M out of the same grant.** Slide 1's bottom line reads *Spend the $5.6M grant on bus frequency, and hold $1.5M for bike-share until month 18*, and the contents sheet repeats it. Slide 5 states frequency's capital as **$4.1M - leaves $1.5M**, and gives $5.6M to bike-share as *the whole grant*. Slide 12, the ask, is right: *$4.1M of the grant, $6.8M a year from the general fund, $1.5M held for the gate*. So the deck's most important sentence asserts 5.6 spent plus 1.5 held out of a 5.6 grant. **A second face, wider than one line**: slide 10 lists as a cost *The city takes $1.5M less from the state than it could have taken*, while slides 9 and 12 spend that same $1.5M at month 18 on 16 Old Quarter stations - money forgone at the state cannot be a reserve eighteen months later. That half is carried faithfully from `examples/sources/cost-model.md`, which says the grant *lapses if it is not committed*, then calls the $1.5M both *uncommitted* and *held for bike-share*. **This is D1 Spine and D4 Consistency, two of the five dimensions no instrument here reaches** (**L-05**, DS-191) - and it reaches past the copy: `seed_defects.py` derives the blindness fixture from this deck and seeds exactly one D4 defect, *slide 9 reserve contradicts slide 5's $1.5M*, so a baseline already contradicting itself on D4 confounds what the fixture is built to prove | The figures are on the deck's own slides and on its printed pages: `python tools/deck/render.py shots examples/reference-deck.html 1,5,9,10,12`. `grep -n '1\.5M' examples/sources/cost-model.md` prints the source's three statements about the same $1.5M. Every instrument is green on this deck - `check.py`, `audit.py`, `glitchfree.py` and `printgeom.py` - which is this finding's own point | [`../examples/reference-deck.html`](../examples/reference-deck.html), slide 1's bottom line and its contents entry - and `examples/sources/cost-model.md`, **cycle 18's file**, named here so cycle 18 does not raise the same thing a second time (`AM-12`) | **A hypothesis.** The narrow half is one line: make slide 1 say what slide 12 says. The wide half is a decision the copy cannot take for itself - whether the $1.5M is grant money committed now and drawn at month 18, or grant money forgone. Only one can be true, and slide 10's cost line and slide 9's reserve each need the other answer. Whoever fixes it settles that in the source first, rewrites both, then re-derives the fixture | `s` | [T-248](../tasks/T-248-four-content-errors-in-three-shipped-decks.md) | **closed 2026-08-30**, and the wide half was settled in the source first as the remedy requires. **The $1.5M is committed at approval and drawn at the month-18 gate**, not forgone. Decided on the deck's own weight of evidence rather than by preference: of the seven places the deck states it, six already read *held*, *reserve* or *held for the gate* - slides 1, 5, 9 (three times) and 12 - and only slide 10's cost line read *forgone*. The source settles it outright: the grant *lapses if it is not committed*, so money forgone at the state cannot be spent on 16 stations eighteen months later. `cost-model.md`'s *leaves $1.5M of the grant uncommitted* now says committed-and-drawn-later, and its *What the city forgoes* section says what is actually forgone - **46 stations and eighteen months**, since $1.5M buys 16 at the vendor's per-station price against the 62 the bike-share proposal would have built at once, and none of the $5.6M goes back to the state. The narrow half is one line: slide 1 now reads *Spend $4.1M of the $5.6M grant on bus frequency, and hold $1.5M for bike-share until month 18*, which is what slide 12 says. The contents sheet derives from it. The fixture was re-derived after both. |
| `PR-82` | project method | **Low** | **[`AUDIT-METHOD.md`](AUDIT-METHOD.md) section 2 says cycle 17 reads the decks' specifications, and `cycles.py` gives them to cycle 18.** The bullet is about cycle 17 throughout - it names the five files, the two instruments and their byte figures - and ends *The decks are rendered, looked at offline, and measured with the tools in `tools/deck/`, and their specifications are read.* Those specifications and sources are cycle 18's twenty-five files. Read as a statement about cycle 17 it is wrong; read as an account of the audit's total coverage it is true and silent about when. **The ambiguity is not free**: this cycle read two of cycle 18's files on the strength of it | `python tools/docs/cycles.py --cycle 18` lists the three `examples/sources/*.md` with every `.foundation.md` and `.slides.md`, 25 files and 301,861 bytes, and `--cycle 17` names five `.html` files and nothing else. `git show 5463021 -- docs/AUDIT-METHOD.md` shows the clause moving through that day's rewrite of the same bullet rather than entering with it, so it is long-standing wording. T-219 section 2's cycle 17 brief names four tools and a print, and no specification | [`AUDIT-METHOD.md`](AUDIT-METHOD.md) section 2 - **cycle 7's file, and cycle 7 is closed**, so recording it here is the only place it lands. The same reason `PR-21` was recorded at cycle 5 | Say when: *and their specifications are read at cycle 18*. **A hypothesis in one respect**: the bullet exists to justify grade C, and naming a second cycle inside it may read as scope creep - whoever fixes it decides whether the sentence instead drops the clause and leaves T-219 section 2's table to carry it | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-83` | the product | ~~**High**~~ | **A shipped deck carries eleven copies of one source, ten of them unreachable, and the verb that repairs a drifted quick view writes the copy nobody reads.** `quickview.py`'s `wire()` states the contract in its own docstring - *the control goes on every mark, the `<template>` goes on the first*, and *six copies of one document would be the size cost this feature has to justify, spent on nothing* - and [`../shell/deck.js`](../shell/deck.js) repeats it verbatim above the lookup that implements it. `examples/portfolio-review/portfolio-review.html` carries one template per slide: **12 templates, 2 distinct payloads**, `Portfolio model` eleven times at 8,451 bytes each. `deck.js` keys `qvSrc` by `data-qv`, so the **last** template wins and the other ten are dead weight - **84,510 bytes, 21.3% of the deck**. **The second half is worse than the waste**: `rewire()` substitutes with `count=1`, so `refresh --write` updates the **first** copy while the reader opens the **last**, and `check_all.py`'s `_quickview_args` dedupes by title, so the gate compares one of eleven and exits 0. [T-181](../tasks/T-181-nothing-detects-that-a-decks-embedded-quick-view-has-drifted-from-its-source.md) shipped `check` to make quick-view drift detectable; this deck's shape defeats it | `python tools/deck/quickview.py list examples/portfolio-review/portfolio-review.html` prints 12 quick views for 2 sources. The same tool's `check`, given both sources, prints `compared 2 of 12 carried: 2 match, 0 drifted` and exits 0. **Both directions measured on scratch copies** (**L-05**): appending one heading to a copy of `portfolio-model.md` and running `refresh --write` reports `1 changed, 0 unchanged`; the edit lands in slide 1's template, slides 3 to 12 keep the old rendering, the last template in the DOM - the one `deck.js` keys - does **not** carry it, and `check` on that same file still prints `match` and exits 0. `grep -n 'qvSrc' shell/deck.js` is the last-wins line and `grep -n 'count=1' tools/deck/quickview.py` the first-only one. The other three decks are one template per source and clean - sort-window 3 of 3 match, measure-first 5 of 5 | [`../examples/portfolio-review/portfolio-review.html`](../examples/portfolio-review/portfolio-review.html) for the ten copies; [`../tools/deck/quickview.py`](../tools/deck/quickview.py) - `wire()` for how they arrived and `rewire()`'s `count=1`; [`../tools/check_all.py`](../tools/check_all.py) - `_quickview_args`'s `seen` set | **Measured in one half, a hypothesis in the other.** Measured: the ten copies are byte-identical to the eleventh, so removing them is a deletion and nothing renders differently. Hypothesis: which of the three sites is the defect. `wire()` is correct over a whole deck and this deck was wired a slide at a time, so the cause may sit in whatever built it rather than in the tool; `rewire`'s `count=1` and the gate's dedupe are each right on a deck holding one copy per source and each wrong on this one. Whoever fixes it decides whether `check` should **fail** on a repeated `data-qv` rather than silently compare the first - one rule that would have caught all of it | `s` | [T-233](../tasks/T-233-a-shipped-deck-carries-eleven-copies-of-one-source.md) | **closed 2026-08-30**, and the row's open question - **which of the three sites is the defect** - is answered by reading each against what it is for. **`wire()` is correct**: its `first[0]` guard puts the template on the first mark and a control on every one, exactly as its docstring states, and its self-test proves it. **`deck.js` is correct**: keying `qvSrc` by `data-qv` implements the stated contract. **The deck is the defect**, and so is `rewire()`'s `count=1` - which is right on a deck holding one copy and wrong on this one, because it repaired the **first** template while `deck.js` renders the **last**. `rewire` now replaces every copy and returns how many, and `refresh` says so out loud when it repairs more than one. **A fourth site the row did not name, found by seeding the drift into the copy the deck renders**: `check`'s comparison also read the first template, so on the eleven-copy deck a real drift in the rendered copy reported `match` and exited 0 - the detection T-181 shipped, defeated in a second way. So the row's last question is answered **yes**: `check` now names a repeated `data-qv` and **fails** on it, which is the one rule that catches all of it. Measured: eleven byte-identical copies, deck **407,546 -> 322,796 bytes, -84,750, -20.8%**; twelve controls kept, two templates left; `compared 2 of 12` became `compared 2 of 2`; and the drift-and-repair was run on both deck shapes. **A fifth site surfaced when the batch's gate ran, and it is the most expensive of them**: `spec.py`'s `SPEC-5` searches a slide's own `<section>` for a ledger value, and a quick view's `<template>` sits inside that section - so the *source document* was answering for the slide. Isolated to one variable: with this deck at `HEAD` and nothing changed but the deduplication, `SPEC-5` fails on exactly two ledger rows. `slide_text` now strips inert templates, matched on the exact form `quickview.py` writes - the loose `<template\b.*?</template>` was tried first and was worse than the defect, because this deck holds **eight** `<template` openings against two closings and the pattern swallowed slide 1's section opening. **What the rule then found is a real defect**: `65%` appears nowhere a reader can see in this deck, while the slides specification, the foundation's opening paragraph and the source all call it half the deck's argument - [T-282](../tasks/T-282-the-opening-slide-carries-one-of-the-two-figures-its-specification-names.md). |
| `PR-84` | the product | ~~**Med**~~ | **A shipped deck prints a sort finish its own stated rate contradicts by two hours, and the tripwire that rests on it is already breached at every volume the deck models.** `examples/sort-window/sources/throughput-model.md` rates the single sorter line at **3,100 parcels an hour** and states the method one line under its own table - *the finish time is arrival plus sort time and nothing compresses it*. The second trunk obeys it: 7,200 over 3,100 is 2h 19m, and 23:40 plus 2h 19m is **01:59**, which slide 5's derivation panel shows as its working. The first trunk does not: 20,400 over 3,100 is **6h 35m**, so 19:40 plus that is **02:15**, and the source table and the deck both print **00:14**. The foundation's ledger carries `First trunk sort finishes - 00:14 - throughput-model - 5` as a fact. **The consequence reaches a tripwire.** Slide 10 reads *Above 31,900 in a day, and the first trunk's sort runs into the second's*, but only 12,400 parcels fit between 19:40 and the second trunk's 23:40, so the overlap begins once the first trunk passes 12,400 - about **16,800 parcels a day**, below the October baseline of 18,400 and far below the 27,600 peak the deck is about. The condition the deck names as a future limit holds on every day in its own model | `grep -n '3,100' examples/sort-window/sources/throughput-model.md` prints the rate and the tolerance it is held to. `python -c "print(20400/3100, 7200/3100)"` returns 6.58 and 2.32 hours: the same division reproduces the deck's own 2h 19m exactly and reaches 00:14 from no start time at all - 00:14 from 19:40 is 4h 34m, which is 14,157 parcels at the stated rate. A targeted search of the deck returns `First trunk 20,400 parcels 19:40 sort at 3,100/hr clears 00:14` and, separately, `Where 01:59 comes from ... 7,200 divided by 3,100 is 2h 19m`. `python tools/deck/spec.py` is green on this pair and `check.py`, `audit.py`, `glitchfree.py` and `printgeom.py` are green on the deck, which is this row's point: it is **D1 Spine and D4 Consistency**, the dimensions **L-05** and DS-191 place outside every instrument's reach - the same class as `PR-81`, on a second deck | [`../examples/sort-window/sources/throughput-model.md`](../examples/sort-window/sources/throughput-model.md) - the first trunk's row and the 31,900 tripwire; then the ledger row in `sort-window.foundation.md` and slides 5 and 10 | **A hypothesis, and the arithmetic is its easy half.** Either the first trunk's parcel count is wrong, or its finish time is, or the depot has more capacity than *one sorter line* - and the three answers give three different decks. Under none of them does 20,400 at 3,100 an hour finish before the second trunk lands, so the fix reaches slide 5's night-flow diagram and slide 10's tripwire rather than one cell. `service-calendar.md`'s *sorting runs 20:00 to 01:00* separately caps a night at 15,500 parcels against a 27,600 peak, so whoever fixes this settles the capacity story once, in the source, and re-derives the ledger from it | `s` | [T-281](../tasks/T-281-the-sort-windows-capacity-story-cannot-hold-its-own-failure-table.md) | **closed 2026-08-30.** Deferred earlier the same day with all three candidate answers refused as number fixes, then **ruled by the owner: the depot has two sorter lines**, both on the evening shift to 23:00 and one through the night. The deferral's own arithmetic is what the ruling was taken on, and the answer repairs the deck without replacing anything it already showed: the second trunk's **01:59** is **unchanged**, because 7,200 at 3,100 an hour was always the night line's working and slide 5's derivation panel already printed it. What moves is the first trunk - 20,400 on both lines at 6,200 an hour is 3h 17m, so **00:14 becomes 22:57**, clear of the second trunk by 43 minutes - and the tripwire, which was the observed busiest day rather than a derived threshold: both lines sort **24,800** in the four hours before the second trunk lands, so the overlap starts at about **33,500** a day and 31,900 stays in the sentence as the comparison. **The failure table is now reachable**, which it was not at any constant rate: 3,067 of the second trunk's 7,200 are still on the belt at the cut-off, against the 2,875 that 84% of a 12.4% peak miss implies - a modelled peak night against a 31-night mean, agreeing to within 7%. The source states the capacity once and every figure is derived from it; the specification's four affected lines are amended and dated rather than annotated, because what was wrong was the model under the wording. One look owed on the rebuilt lane, whose bar geometry was recomputed rather than rendered. |
| `PR-85` | the product | ~~**Med**~~ | **A shipped deck attributes a transmission asset's $29M to renewables, on the slide that is its concentration argument.** `examples/portfolio-review/sources/portfolio-model.md` says *Unrealised revaluation of $172M is the largest single line and $131M of it sits in renewables*, then itemises that $131M as Calder wind $58M, Norbeck solar $44M and **Aldis transmission $29M**. The same document's top-three table calls Aldis *Transmission. The only one of the three outside renewables.* Renewables' share is **$102M**, so the stated figure overstates it by 28%. The deck carries both halves: slide 6's bottom line is *Unrealised revaluation is $172M of the movement, and $131M of it is renewables*, its disclosure is specified as *the three renewable assets carrying the revaluation*, and slide 8 prints Aldis as the one outside renewables. The ledger row reads `Revaluation sitting in renewables - $131M`. **The parts sum and the label does not**, which is why the foundation's own added bar - *every total that is stated is checkable against its parts on the same slide* - passes on the arithmetic and cannot see this | 58 + 44 + 29 = 131; 58 + 44 = 102. `grep -n '131M' examples/portfolio-review/sources/portfolio-model.md` and the same search for `Aldis` prints the attribution and the sector label. A targeted search of the deck returns `$131M of the revaluation sits in renewables. What the $131M is Calder wind $58M ... Aldis transmission $29M` and, on another slide, `Aldis transmission 10% of NAV. Transmission. The only one of the three outside renewables.` **Every other total in the source reconciles** and was checked: the five allocation columns each sum to 100, the sector contributions sum to +12.4, the waterfall closes on 2,400, 7.7 + 14.8 = 22.5 at 0.9% of NAV, 11.9 + 29.6 = 41.5 at 1.7%, and the three tranches sum to the $170M the rebalancing moves - so this is one wrong attribution and not a document that fails to add up | [`../examples/portfolio-review/sources/portfolio-model.md`](../examples/portfolio-review/sources/portfolio-model.md) - the sentence introducing the $131M and the asset table under it; then slide 6 and the ledger row in `portfolio-review.foundation.md` | **A hypothesis about which number is the wrong one.** Either renewables is $102M and slide 6's bottom line, its annotation and the ledger row all move, or Aldis does not belong in that table and the total becomes $102M across two assets. The deck's argument is marginally weaker under both, which is the reason to settle it in the source before touching a slide. A third possibility is that a fourth renewable asset carrying $29M was meant and lost | `s` | [T-248](../tasks/T-248-four-content-errors-in-three-shipped-decks.md) | **closed 2026-08-30.** Of the row's three candidate answers the first was taken: **renewables is $102M**. Decided from the source's own words rather than by preference - its top-three table already calls Aldis *Transmission. The only one of the three outside renewables*, so the label was wrong and the parts were right; the third possibility, a lost fourth renewable asset, would invent data this document does not have. The three-asset total stays **$131M** and is now labelled as *the three largest revaluation lines*, with an *of which renewables $102M* row under it, so the sum a reader checks still closes. Slide 3's sector line, slide 6's bottom line, its disclosure heading (*What the $131M is* became *The three largest lines*) and the ledger row all moved; the embedded quick view was refreshed from the corrected source rather than edited. |
| `PR-86` | product documentation | ~~**Med**~~ | **The adopter deck's sanitisation left the source project's own names in the documents beside it, the removed `D6` filename among them.** [T-128](../tasks/T-128-publish-the-adopter-deck-as-a-worked-example.md)'s seventh class - identifiers and paths from the source project's namespaces, *checked by resolving each against this repository* - was recorded as `foreign paths 0` on 2026-08-16. Resolving every path-like and id-shaped token in cycle 18's thirteen `measure-first` files against the tree returns five surviving paths and one dangling id. `measure-first.slides.md` line 3 opens *Expanded from the outline in `D6-executive-board-presentation.foundation.md`* - a file that exists nowhere here, carrying the exact `D6` name T-128 ruled goes. `measure-first.foundation.md` line 57 says the sources are read *from `deliverables/`*, where this repository has `sources/`. Both diagram YAMLs name `deliverables/D1-...md` and `deliverables/D3-...md` as their source, `deliverables/diagrams` as their output and `python tools/bpmn/bpmnlay.py` as the rebuild command - a tool this repository does not contain - under a header reading *Never hand-edit the .bpmn or .svg - change this file and rebuild*. **And `D-004` dangles seven times**, across the to-be YAML and D3, D4 and D5, with no register defining it anywhere in the set: the same shape as the `[D-006]` and `[D-007]` T-128 names and strips | `grep -rn 'D6' examples/measure-first/` and the same sweep for `deliverables/` prints the slides line and the five path references; `git ls-files examples/measure-first/` lists no such file and `ls tools/` has no `bpmn`. `grep -rn 'D-00[0-9]' examples/measure-first/` prints seven `D-004` and no definition. Resolving every id-shaped token in the thirteen files against `docs/DESIGN-SYSTEM.md`, `tasks/` and `docs/lessons/` leaves `D-004` as the only one that dangles - `A-03` to `A-14`, `DS-085`, `DS-105` and `X-04` all resolve and are correctly kept. **The rest of the sanitisation holds and was measured the same way**: case identity, place names, training context and local-system paths are zero across every file type including the XML - both BPMN `targetNamespace` values read `http://larkfield/bpmn` and every `name=` attribute carries the sanitised identity, which is **L-111**'s repair still working - and the twelve `http://` hits are BPMN and SVG namespace URIs rather than loads | `examples/measure-first/measure-first.slides.md` line 3, `measure-first.foundation.md` line 57, both `examples/measure-first/diagrams/*.yaml` headers, and the seven `D-004` marks | **A hypothesis only about `D-004`.** The path repairs are mechanical: the slides line names `measure-first.foundation.md`, `deliverables/` becomes `sources/`, and the YAML *Generated with* line either names a tool an adopter has or says plainly that the diagrams were built elsewhere. `D-004` is a judgement - it is load-bearing four times as *analytics recommends, a person approves*, so deleting the mark loses the citation and keeping it leaves a reference to nothing. **The wider half is how the class was cleared**: T-128 enumerated the ids it had found and removed those, and the one nobody enumerated survived seven times - the failure `PR-06`, [T-222](../tasks/T-222-derive-the-reconcile-sweeps-membership-instead-of-enumerating-it.md) and [T-223](../tasks/T-223-derive-the-audit-cycles-membership-instead-of-counting-it.md) each already cost, in a check whose own criterion said *resolve* and never *list* | `s` | [T-248](../tasks/T-248-four-content-errors-in-three-shipped-decks.md) | **closed 2026-08-30.** The five paths were mechanical and are repaired: the slides line names `measure-first.foundation.md`, `deliverables/` became `sources/` in the foundation and in both diagram YAMLs, and the *Generated with* line now says plainly that the `.bpmn` and `.svg` were laid out by a tool this repository does not ship - the row's second option, because naming a tool an adopter has would be inventing one. **`D-004` was given a home rather than deleted**: the row is right that deleting the mark loses a citation that is load-bearing four times, so it is now defined in `D5-management-decision-matrix.md` - the document whose subject is decisions - in a *Decisions referenced by mark* table under its Terms section. All seven marks resolve inside the set, which is what T-128's seventh class asks for, and nothing was invented: the definition is the sentence D3 and D4 already state beside every mark. The embedded quick view of D5 was refreshed from the corrected source. |
| `PR-87` | product documentation | **Low** | **The adopter deck's foundation states two of its own counts wrong, in a file the figure watcher does not read.** `measure-first.foundation.md`'s *Quality bar - additions only* opens **Three**, *and each comes from something this project has already paid for*, then lists **four** numbered items. Its figure ledger says *Thirteen of the **forty** rows are in the second block*; the second block is thirteen and the ledger is **38** rows. `tools/docs/figures.py` names `examples/measure-first/measure-first.html` and no specification beside it, so both figures sit outside the one instrument built to catch a pasted number going stale - the same shape as `PR-04` | Counting the numbered items under *Quality bar* returns 4 against the stated *Three*; counting the ledger's two blocks returns 25 + 13 = 38 against the stated *forty*. `grep -n 'measure-first' tools/docs/figures.py` prints one path and it is the `.html`; `python tools/docs/figures.py` exits 0 with `0 stale figure(s)` on this tree | `examples/measure-first/measure-first.foundation.md` - the *Quality bar* opening line and the *Figure ledger* introduction | Say four, and say thirty-eight. **A hypothesis about the second**: a ledger row count moves whenever a row is added, so the honest fix may be to drop the total and keep *thirteen of them reach a slide through a disclosure or a diagram label*, which is the sentence's actual point and cannot go stale | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-88` | product documentation | **Low** | **Two of the five source documents the adopter deck carries inside itself contradict the third, and one contradiction is the deck's own stated quality bar.** `D1` section 1.1 gives the monthly meeting **step 6** and gives step 5 to a decision, having explained one line above that *in BPMN a decision only routes the flow. It does no work.* Its own section 2 then reads *Step 5 can change any number without a recorded reason. The meeting minutes hold the new figure*, and section 5.1 cites *the monthly meeting overrides the calculation with no recorded reason (step 5)* - so a reader following either citation lands on the gateway *More than 15% away from the draft?*, which by that document's own rule changes nothing. Section 1.1's loop sentence gets it right and says *the meeting at step 6*. **Separately**, `measure-first.foundation.md`'s second quality-bar addition is *Delivery punctuality is stated as on time, never as OTIF ... the industry term claims a measurement that does not exist (D1 section 3)* - and D1 section 3 says exactly that. `D2` section 3 nevertheless calls the same figure *the 87% OTIF figure*, and `D4` section 3 heads a KPI row **Delivery punctuality (OTIF) - 87%**. The bar holds on every slide and is defeated inside the five quick views the deck opens from its own provenance marks | `grep -n 'step 5' examples/measure-first/sources/D1-current-business-process-analysis.md` and the same search for `step 6` prints four references - one correct at line 78, two wrong at 119 and 187, and the *steps 5 and 8 have their own rows* rule at 70. `grep -rn 'OTIF' examples/measure-first/sources/*.md` prints D1's caveated definition and D2's and D4's uncaveated uses. Extracting the deck's text with `<template>` blocks removed returns **no** OTIF; leaving them in returns six, every one inside a carried source | `examples/measure-first/sources/D1-current-business-process-analysis.md` sections 2 and 5.1, and the OTIF cells in `D2-predictive-analytics-assessment.md` section 3 and `D4-implementation-concept.md` section 3 | D1's two citations become *step 6*. **A hypothesis for the OTIF half**: the bar is about what the deck says, and a copied source is evidence rather than deck copy - so the question is whether a quality bar reaches the documents a deck carries. If it does, D2 and D4 gain D1's caveat; if it does not, the bar should say so, because a reader who opens the mark meets the term the bar exists to avoid | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-89` | product documentation | **Low** | **A slide's `Sources` field sits outside its slide, below the closing rule and a different section, and `SPEC-1` passes because the last section runs to the end of the file.** `sort-window.slides.md` states *Nine fields per slide*. Slide 12's block ends at *Interactive elements. None.*, then a horizontal rule, then `## Open - needs a decision` and its table, and the document's final line is `- **Sources.** fleet-and-cost-model, service-calendar`. Read as written it attaches the sources to the Open table. `spec.py`'s SPEC-1 finds it because slide 12's section has no closing boundary, so the whole rule is satisfied by a field that has drifted out of the slide it describes | `tail -4 examples/*/*.slides.md` shows the trailing `- **Sources.**` on `sort-window` alone; the other two specifications end on their own closing sections. `python tools/deck/spec.py examples/sort-window/sort-window.foundation.md examples/sort-window/sort-window.slides.md examples/sort-window/sort-window.html` returns SPEC-1 `pass`. The declared sources are right on their merits - slide 12 cites $310k, $120k, the 21:50 slot and the 02:30 cut-off from the fleet model and 19 September from the calendar, all of which the ledger's `Used on` column already gives to slide 12 | `examples/sort-window/sort-window.slides.md` - the final line, and slide 12's block | Move the line into slide 12's block, above the rule. **A hypothesis about the gate**: SPEC-1 cannot tell a field inside the last slide from a field after it, because nothing closes the final section - whoever fixes the document decides whether the rule is worth teaching that boundary, which would cost a terminator convention the other two specifications do not need | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-90` | project documentation | **Med** | **The ruleset audit is titled *Every rule, with its verdict* and holds 165 of the ruleset's 177.** [`RULESET-AUDIT.md`](RULESET-AUDIT.md) opens *Every rule in `DESIGN-SYSTEM.md` tested against one question* and §1 reports *165 rules examined*; the ruleset carries **177** today. **`DS-233` to `DS-244` have no row**, and nothing in the document says a rule might be missing. Its own caveat covers the other direction only - *a rule amended after 2026-08-17 has a verdict about the wording it used to carry* - which is drift inside a row, not a row that was never written. The document's stated purpose is tier 3, *read when someone asks whether a rule was ever examined*, and for twelve rules that question now returns a silence indistinguishable from a failed search. **This is not `PR-12`**: that row is a stale *count* in [`BRIEF.md`](BRIEF.md) and [`EVALUATION.md`](EVALUATION.md); this is a missing *membership* in the examination itself | Parsing §5's table returns **165** ids and parsing the ruleset's rule tables returns **177**; the difference is exactly `DS-233 DS-234 DS-235 DS-236 DS-237 DS-238 DS-239 DS-240 DS-241 DS-242 DS-243 DS-244`, and no id in the audit table is absent from the ruleset. `git log -1 --format=%ad --date=short -S DS-233 -- docs/DESIGN-SYSTEM.md` prints 2026-08-18 and the same for `DS-244` prints 2026-08-21, both after the 2026-08-17 run. **A pass came with it**: all 165 surviving rows' `Check/Reach` pairs still agree with `DESIGN-SYSTEM.md` exactly, so the one column the document promises stays true has | [`RULESET-AUDIT.md`](RULESET-AUDIT.md) §1 and §5 | Either twelve rows, or one sentence stating the denominator. **A hypothesis about which**: the document is honest about being a snapshot everywhere except its membership, so the cheap fix is a stated denominator - *165 of the 177 rules as of 2026-08-17* - plus the excluded list, which is derivable rather than typed. Running the three tests on twelve rules is a second audit and belongs to whoever asks for one | `s` | [T-249](../tasks/T-249-the-two-prior-audits-registers.md) | open |
| `PR-91` | project documentation | **Med** | **The context audit says the screening partition is *checked by nobody* and that *there is no command that counts these rows*, and names the task that built one.** [`CONTEXT-AUDIT.md`](CONTEXT-AUDIT.md) §4.1's closing paragraph reports the *12 adopted, 10 rejected, 13 deferred, summing to 35* claim as a part-of-whole in prose no gate reads, and ends *That is [T-156], raised rather than fixed here because building it is not re-running research*. **[T-156](../tasks/T-156-make-the-screening-partition-a-figure-a-checker-can-count.md) is `done`, `shipped_in: 0.3.0`**, and `tools/docs/screening.py` counts exactly those rows in exactly those two documents. **The same document handles the same shape correctly one section later**: §10.4's *the one number in this repository that governs what every session pays is checked by nobody* carries *Closed 2026-08-15 by [T-158](../tasks/T-158-measure-the-tier-1-bound-instead-of-remembering-it.md), and the paragraph above is kept as the state it closed from.* §4.1 has no such note, so the two paragraphs teach opposite things about how this document records a closure | `python tools/docs/screening.py` prints the three counts, the sentence, the catalogue size and `0 disagreements`, binding `docs/CONTEXT-AUDIT.md` §4 to `docs/research/R8-context-economy-for-coding-agents.md` §7.2. `grep -n '^status:' tasks/T-156-*.md` prints `status: done`. Cycle 14 read `screening.py` and recorded its two anchors failing loudly rather than quietly; nothing there could see that the document it checks still says it does not exist | [`CONTEXT-AUDIT.md`](CONTEXT-AUDIT.md) §4.1's final paragraph | One closure note in §10.4's form. **A hypothesis worth more than the note**: this is the second live *nobody checks this* claim in the document and the first was found the same way, so the class is *a stated gap whose remedy shipped* - cheaper to sweep every such sentence in both audit registers once than to catch them one cycle at a time | `xs` | [T-249](../tasks/T-249-the-two-prior-audits-registers.md) | open |
| `PR-92` | project method | **Med** | **The ruleset audit's ceiling on its own test 2 rests on a fixture that has not had that shape since [T-224](../tasks/T-224-give-the-blindness-fixture-its-own-instrument-in-cycle-17.md).** [`RULESET-AUDIT.md`](RULESET-AUDIT.md) §3 states *`examples/reference-deck-seeded-defects.html` seeds **five** defects - DS-035, DS-075, DS-141, DS-142, DS-229 - so it proves five rules can fail and is silent on the other 110 the gate owns.* The fixture's generator names **none** of those five ids; it seeds one defect per **evaluation dimension**, ten of them, `D1`-`D4` and `S1`-`S6`, and asserts that in its own instrument. The gate owns **122** rules, not the 115 that sentence's two halves imply. So the paragraph telling a reader how far test 2's verdicts can be trusted describes a fixture, a numerator and a denominator that are all wrong - while the ceiling it argues for is still correct | `grep -c` for the five ids over `tools/examples/seed_defects.py` prints `0`. `python tools/examples/seed_defects.py --check` ends *dimensions carrying a seeded defect: D1, D2, D3, D4, S1, S2, S3, S4, S5, S6 (10)*. `python tools/deck/ruleset.py` prints *177 rules, 122 owned by a gate*. Cycle 17's ledger row records the same ten dimensions and `PR-01` records the fixture's instrument changing hands; both are about the fixture, and neither reaches this sentence | [`RULESET-AUDIT.md`](RULESET-AUDIT.md) §3, the test-2 ceiling paragraph | Restate the ceiling in the fixture's current terms. **A hypothesis about why it went wrong quietly**: the sentence pins the fixture by listing rule ids rather than by naming its instrument, so when the fixture was rebuilt around dimensions there was nothing for the sentence to fail against - the same shape as `PR-01`, one document further out | `xs` | [T-249](../tasks/T-249-the-two-prior-audits-registers.md) | open |
| `PR-93` | the product | **Low** | **The deck gate prints a traceback for any deck path it cannot read, and the byproduct register names only half the case.** `check.py` given a directory raises `PermissionError` and given a missing file raises `FileNotFoundError`, both as a raw traceback out of `gather()`'s `open()`; given no argument at all it prints its usage correctly, so the readable refusal exists and only this branch is missing. [`CONTEXT-AUDIT.md`](CONTEXT-AUDIT.md) §8's `BP-1` records the directory case, reproduced 2026-08-13 and **still exact today** - the row is right and the defect is unfixed - but the missing-file case it does not name is the one a mistyped deck name reaches | `python tools/deck/check.py examples/sort-window` ends `PermissionError: [Errno 13]` at `check.py:532`; `python tools/deck/check.py examples/nope.html` ends `FileNotFoundError: [Errno 2]` at the same line; `python tools/deck/check.py` with no argument prints the usage. Searched before raising (`AM-12`): `PR-42` to `PR-47` are cycle 10's rows on `check.py` and none of them is this, and `PR-56` is a different loud failure, in `render.py` | [`../tools/deck/check.py`](../tools/deck/check.py)'s `gather()`, at the deck `open()` | One readable refusal for both, naming the path and what was expected. **Recorded under `AM-09`**: the file is **cycle 10's** and the finding was reached from cycle 19's `BP-1`, so cycle 10 does not own a row it never saw. **A hypothesis**: the no-argument path already prints usage, so the defect is a missing readable-path test rather than missing machinery - which is why a whole cycle reading this file did not surface it | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-94` | project method | **Low** | **The ruleset audit's own `unverified` total disagrees with its two parts, and the parts are right.** [`RULESET-AUDIT.md`](RULESET-AUDIT.md) §3 reads *29 rules are recorded `unverified`* ... *27 of them the gate owns and excuses on every deck built here* ... *the rest are `off-gate` in the ruleset itself - DS-072, DS-210 and DS-211*. 27 + 3 is 30, and §5's table holds **30** rows whose test-2 verdict is `unverified`. The sentence needs no reading of the tree to fail: it contradicts itself, in the paragraph that states the ceiling on the audit's central test | Counting §5's rows by their test-2 cell returns 30 `unverified`, of which the three carrying `off-gate` in `Check/Reach` are exactly `DS-072`, `DS-210` and `DS-211`, leaving 27. `python tools/deck/ruleset.py` independently prints *excused in the ruleset 3 DS-072 DS-210 DS-211* | [`RULESET-AUDIT.md`](RULESET-AUDIT.md) §3's third paragraph | `29` becomes `30`. **A hypothesis about the class rather than the digit**: the figure is derivable from the document's own table and is typed instead, which is the shape `figures.py` exists for and cannot reach here because no command prints it - the same gap `PR-91` reports already closed for the screening partition | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-95` | project documentation | **Low** | **The ruleset audit's proof that its dead-legislation claim failed points at two lines that have moved, and counts one deck too few.** §2 cites `shell/deck.js:410` as carrying `DS-146` by name and `shell/deck.js:825` as `DS-147`'s `countUp()`; today those are lines **471** and **952** of a 997-line file, and 410 and 825 are ordinary code. The same paragraph says *both ship in all three decks* - **four** decks ship and both mechanisms are in all four. The argument is untouched and every pointer in it is wrong, which is the shape `PR-02`, `PR-03` and `PR-11` have already found for the deck count in three other documents | `grep -n DS-146 shell/deck.js` prints 471 and `grep -n countUp shell/deck.js` prints 952; `(Get-Content shell/deck.js).Count` is 997. `grep -c countUp` over the four gated decks prints 2 each. **The claims that hold were re-run rather than assumed**: `grep -c -i chart shell/components.css` is still `0`, and `examples/reference-deck.html` still holds `<polyline>` exactly twice, so `DS-122`'s hand-authored chart is still the subject §2 says it is | [`RULESET-AUDIT.md`](RULESET-AUDIT.md) §2, and the `DS-146` row in §5 | Cite the symbol, not the line. **A hypothesis worth more than these two edits**: `refcheck.py` reads the pointers a reader follows and a `file:line` is one it cannot resolve, so a line citation anywhere in the record is unwatched by construction - the rule is *do not cite a line number*, and it is checkable | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-96` | project method | ~~**Low**~~ | **Two documents say the cycle-membership command reports the partition's verdict before it answers, and it prints it after.** [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) §2 step 2 reads *It reports the whole partition's verdict before it answers, so a tracked file that has fallen outside every cycle stops the reading rather than surviving it*, and `cycles.py`'s `complaint()` docstring reads *ask this file which files a cycle reads, and it answers only after saying whether the partition still holds*. In `--cycle` mode the file list, the byte total and a blank line print first and `complaint()` runs last; **on a holding partition it prints nothing at all**, in every mode but the default. The exit code still carries the verdict so the guard works - the order both documents promise is the reverse of the code's, and a green `--cycle` run cannot be told from a mode that does not check | `tools/docs/cycles.py`'s `--cycle` branch prints the header, the rows and the total, then `return complaint(...)`; `complaint()` emits its `UNASSIGNED` / `STALE` / `UNSTATED` lines and its summary only `if unassigned or stale or empty`. `python tools/docs/cycles.py --cycle 19` prints two files and no verdict line, where the default mode ends on *Every tracked file belongs to exactly one cycle, and every rule earns its place* | [`../tools/docs/cycles.py`](../tools/docs/cycles.py)'s `--cycle` / `--list` / `--plan` branches, or the two sentences | Move the call above the answer, or say *after*. **Recorded under `AM-09`**: `cycles.py` is **cycle 14's** file and `T-219`'s record is **cycle 7's**, and both cycles are closed. **A hypothesis about which way to fix it**: printing the one-line green verdict in all four modes costs nothing, makes the promised order true, and removes the silence that currently reads the same as an unchecked mode - which is `AM-11` on this run's own instrument | `xs` | - withdrawn, no task | **withdrawn 2026-08-29**, at cycle 40, as a second raising of `PR-65` - the same four documents and the same code path, found by a later cycle that did not search the register first. Its added observation is on `PR-65`'s row (`AM-12`) |
| `PR-97` | product documentation | **Med** | **The rationale records two conflicts as unresolved and one of them as unowned; both were closed by shipped work, and one of the two closing tasks says in its own record that it was closing it.** [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md) §2.2 is headed *The two real use found - U-01 and U-02, and **neither is resolved***, and both rows read **Open**. `U-01` - *a `file://` link cannot be shown live vs a source line a reader can open* - is marked *Owned by [T-070](../tasks/T-070-the-quick-view-for-a-source-document.md), whose overlay is the other way to satisfy the same need*, and **T-070 §1 reads *U-01 is this task's to close***; it is `done`, `shipped_in: 0.2.1`. `U-02` - *five sources cannot carry titles either way, because the mark is one `<p>`* - is marked *Owned by nobody yet; it arrives with [T-092](../tasks/T-092-product-feedback-from-the-first-external-deck.md)*, and **`DS-105` no longer puts multiple sources in a `<p>` at all**: it routes them into a `.sources` control, one line per source, amended by [T-069](../tasks/T-069-extend-the-provenance-mark-to-multiple-sources.md) in `0.1.4`. **The section states its own purpose as *so that the next person to open DS-092 or DS-105 sees that the rule has a known cost*** - and that reader is now told the cost is unpaid by a rule whose own text carries the remedy | `python tools/deck/quickview.py list` and a grep for `sources-item` over the four gated decks: `sort-window` 17 source lines and 5 quick views, `measure-first` 26 and 8, `portfolio-review` 18 and 15, `reference-deck` 17 and 0. The markup is `<button class="sources-open" data-qv="Service calendar" data-file="service-calendar.md">Service calendar` - a **titled** source the reader **opens**, with no link, which is what both rows say is impossible. `DS-105`'s current text names `.sources` and `COMPONENT-CONTRACT.md` §3.2. `grep -n '^status:' tasks/T-069-*.md tasks/T-070-*.md tasks/T-092-*.md` prints `done` three times, at `0.1.4`, `0.2.1` and `0.2.1`. **All 21 tasks this document names are closed**, so nothing else here is stale in that direction | [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md) §2.2 - the heading and both `State` cells | Two resolutions and a heading. **A hypothesis about why this one survived where the rest of the document did not**: §2.2 is the only table here whose column is `State` rather than `Resolution`, and a `State` cell has no shape that looks unfinished - every other section records a closure by amending the sentence it invalidates, which is a habit that cannot reach a cell already designed to say *open* | `s` | [T-241](../tasks/T-241-the-design-system-and-the-rationale-against-what-shipped.md) | open |
| `PR-98` | product documentation | **Med** | **Four sections state a coverage count, three carry the same re-derive caveat, and the fourth - the one with no caveat - is the one that has gone stale.** §5.6 writes *105 to 107 ... derived from the ruleset, as that task requires, and re-derivable rather than to be trusted from this line*; §5.9 and §5.10 repeat that clause almost verbatim. §5.8 states the split with no caveat and in the present tense: *Eighty-five are `auto` or `render` and the mechanical gate owns them; four have `Check: —` ... The remaining twenty-five were `judge`*. Today the `hard` rules split **96** `auto` or `render`, **5** with `Check: —`, **29** `judge`. Two of the three are wrong by 11 and 4; the third is right, because the paragraph's own *twenty-four after DS-107 moved* already resolves four to five. **The `judge` figure is `PR-30`'s subject in a fourth document and is not raised again** (`AM-12`); what is raised here is that this section, alone among its siblings, tells a reader to trust it | Counting the ruleset's rows whose label is `hard` returns 96 `auto`/`render`, 29 `judge`, and 5 with `Check: —` - `DS-107`, `DS-190`, `DS-191`, `DS-220`, `DS-221`, exactly the five the paragraph's own correction implies. `python tools/deck/ruleset.py` prints *177 rules, 122 owned by a gate ... owned and hard 96*. `grep -n 're-derivable rather than' docs/DESIGN-RATIONALE.md` returns §5.6, §5.9 and §5.10 and not §5.8 | [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md) §5.8's opening paragraph | The caveat the other three carry, or the current numbers. **A hypothesis about which**: the numbers are load-bearing for the paragraph's argument - *the remaining twenty-five were judge and nothing produced a verdict for any of them* is a historical claim that needs its historical figure - so the fix is the caveat plus a date, not a re-derivation that would destroy the argument. The general form is `L-95`: a count inside a narrative is a different object from a count inside a claim, and only the second may be refreshed in place | `xs` | [T-241](../tasks/T-241-the-design-system-and-the-rationale-against-what-shipped.md) | open |
| `PR-99` | product documentation | **Low** | **A six-term breakdown of thirteen findings sums to thirteen with two of its terms wrong in compensating directions.** [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md) §2.1 opens *Four conflicts between two `hard` rules, three rules that could not be built as written, **two silences** with a computable answer, one check that was impossible as specified, **two measurement traps**, and one loop rule that could not coexist with its own cost control* - 4+3+2+1+2+1 = 13, which is the row count, so the sentence checks out. Its own `Class` column reads **three** `silence` rows (`F-03`, `F-06`, `F-09`) and **one** `measurement` row (`F-12`). The true partition is 4 conflicts, 3 unbuildable, **3** silences, 1 check impossible, **1** measurement and 1 loop rule - also 13. **The error hides because `F-12` really does hold two traps**, `(a)` the clamped `1fr` track and `(b)` the never-quiescing render, so the sentence silently mixes rows with instances and loses a silence to pay for it | Parsing §2.1's table by its `Class` cell returns conflict 5 (`F-01`, `F-04`, `F-05`, `F-10`, `F-13`), unbuildable 3, silence 3, check impossible 1, measurement 1. **The neighbouring claim was tested and holds**: §2's *four of those are conflicts between two rules both labelled `hard`* is exact once `F-13` is read as the separately-counted loop rule, because [`DESIGN-SYSTEM.md`](DESIGN-SYSTEM.md) §7 opens *WCAG 2.2 Level AA is the floor. **Every row `hard`***, so `F-10`'s `1.4.3 × 1.4.11` is two `hard` rules | [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md) §2.1's opening paragraph | *two silences* becomes *three*, *two measurement traps* becomes *one, holding two traps*. **A hypothesis about the class**: this is the third part-of-whole sentence in the record found summing correctly over wrong parts, after `CONTEXT-AUDIT.md` §4's screening partition and this register's own coverage ledger - and the first two were both fixed by a command that counts the rows. There is no command here | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-100` | product documentation | **Low** | **The rationale names five classes as the live instances of a contract state that has held none since 2026-08-12, and the contract says so on its own page.** §5.9's *What `vocabulary` is for* reads *Five rows are styled, emittable and unused by this deck - the figure's three role classes, `.t-ink` and `.mono`*, and argues that the check reads the state backwards so *the count of them is a number a reader can watch*. [`COMPONENT-CONTRACT.md`](COMPONENT-CONTRACT.md) §2 states the opposite in its own preamble - *No row below is `vocabulary` today, and that is the correct number rather than a gap* - and records that the five moved to `author` on 2026-08-12 when the deck they were waiting for arrived. The mechanism the paragraph defends is intact and its illustration is not, which is the cheap half of the fix and the reason it is `Low` rather than higher | `python tools/deck/component.py check examples/reference-deck.html` prints *every `vocabulary` row is still unused: **0 declared**, 0 now in the deck*. `grep -n vocabulary docs/COMPONENT-CONTRACT.md` returns the preamble at §2 and the two dated notes at the `.t-ink` and `.mono` rows, both reading *It was `vocabulary` until 2026-08-12*. `PR-34` and `PR-41` are cycle 9's and cycle 10's rows on this contract and neither is this | [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md) §5.9's `vocabulary` paragraph | Name the five as the rows that *were* `vocabulary`, and say the count is zero. **A hypothesis worth more than the edit**: the paragraph's own argument predicts this - *a rule that matches nothing looks exactly like a rule that passed* is what it says about the deck, and a check reporting `0 declared, 0 in the deck` is that sentence turned on the check itself. Whether a state with no members should still be checked is the question the edit should answer | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-101` | project method | **Med** | **The cycle plan's Files and Bytes cells have drifted from the command that emits them, in six of thirty-seven sized rows, and one row is wrong by 186,593 bytes.** [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 2 states the rule in its own words - *The Files and Bytes columns are printed, not typed. `python tools/docs/cycles.py --plan` emits them* - and nothing compares the two. Cycles 5, 6, 7, 21, 22 and 24 disagree today; cycle 7 by **+1 file and +186,593 bytes** (6 / 132,120 in the table against 7 / 318,713 from the command) and cycle 22 by +1 file. **This is the drift `PR-68`'s missing flag was named to report** - that row records `--sizing` as documented and unimplemented, and its comment reads *reports when one has drifted far enough to need cutting again*. It is also **L-52** inside the audit's own plan: the bytes did not change, the tree did. **The drift has a measurable rate.** [T-223](../tasks/T-223-derive-the-audit-cycles-membership-instead-of-counting-it.md)'s *Outputs produced* records both tables as regenerated from the command on 2026-08-23, and six rows were wrong five days later - so the reprint bought five days. That task also notes *cycle 22 gained `L-134` and `L-135`* by hand, and the row it left says 59 files against the command's 60, which is the same fact recorded in prose and not in the cell | Parse section 2's sized rows out of the task record and compare each against `python tools/docs/cycles.py --plan`; it prints the six disagreeing rows and `6 of 37 sized rows disagree`. Cycle 21's own step 2 is how this surfaced - the command answered **167,770** against the table's 167,650 | [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 2's cycle programme | **A hypothesis with a cheaper half.** Reprinting the two columns fixes today and restores exactly the state that decayed, which is `PR-12`'s argument against the same move. The half that lasts is making the comparison a command - `cycles.py --plan --check`, or implementing `--sizing` as `PR-68` says it is already documented to behave - so the table is a derived copy that fails loudly rather than one nobody diffs (**L-74**). The two rows are not one fix: reprinting the cells does not read the file `PR-102` names | `s` | [T-239](../tasks/T-239-the-audits-own-record-against-what-it-did.md) | open |
| `PR-102` | project method | **Med** | **A tracked file is in none of the coverage partition's three states, because the cycle that owns it closed before the file existed - and that cycle's subject is the run's own record, so it keeps growing.** Cycle 7's ledger row records **6 files, 123,250 bytes**, read at commit `7678ae0` on 2026-08-23. The cycle holds **7 files, 318,713 bytes** today. The newcomer is [T-224](../tasks/T-224-give-the-blindness-fixture-its-own-instrument-in-cycle-17.md), created at cycle 17 on 2026-08-24 as `PR-01`'s remedy: it did not exist when cycle 7 ran, and no later cycle owns it. [`AUDIT-METHOD.md`](AUDIT-METHOD.md) section 2 says a file in none of the three *is a gap in the audit*, so this is that gap, stated by the run's own rule. **The transferable half is structural rather than clerical**: cycle 7's subject is `AUDIT-METHOD.md`, this register and the unreleased task records - all three of which this run writes into - so the cycle is complete only against a tree that stopped moving. The register alone was **55,879** bytes when cycle 7 read it and is **216,712** now, so 160,833 bytes of its own subject postdate its reading | `git ls-tree -r --name-only 7678ae0 -- tasks/` returns four task records in cycle 7's range and no `T-224`; `git cat-file -s 7678ae0:docs/PRE-RELEASE-AUDIT.md` returns `55879`. `python tools/docs/cycles.py --cycle 7` returns seven files and 318,713 bytes today | [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 2's cycle 41 row, and cycle 7's ledger row in section 2 above | **A hypothesis.** Cycle 41 already re-reads *cycles 1, 3 and 5, plus every cycle a remedy touched*, and cycle 7 is neither - so the smallest fix is naming it there, on the stated ground that a cycle whose subject includes the run's own record must be read last. The wider question is whether the register and `AUDIT-METHOD.md` belong to a numbered cycle at all while the run is live, or to a closing step. That half is the method's rather than this project's | `s` | [T-239](../tasks/T-239-the-audits-own-record-against-what-it-did.md) | open |
| `PR-103` | project documentation | **Med** | **Two lessons state a figure about the tree in the present tense and both have drifted, in the folder whose own L-52 is the lesson about exactly that.** [`lessons/L-39.md`](lessons/L-39.md) reads *`DESIGN-SYSTEM.md` has ended at **section 9 in every one of its 13 commits***; the file has **54** commits and still ends at section 9, so the claim's substance holds and its number does not. [`lessons/L-43.md`](lessons/L-43.md) reads *`ruleset.py --gates` partitions all **114** `hard` rules across their gates* - an instruction about what the tool does now - and the tool prints **130**. **Neither is covered by an existing row**: `PR-12` names [`BRIEF.md`](BRIEF.md) and `PR-32` names [`EVALUATION.md`](EVALUATION.md) section 1, whose own cell says **117**, so one population is stated three ways in three documents and no two agree. **L-52**'s remedy - *a document that pastes derived figures owes a re-derivation step, written down in the document's own rule* - is [`PUBLISHING.md`](PUBLISHING.md) section 6, and `docs/lessons/` is outside it | `git rev-list --count HEAD -- docs/DESIGN-SYSTEM.md` returns `54`, and the claim is the only `13 commits` in the tree. `python tools/deck/ruleset.py --gates` prints `hard rules 130` and `130 = hard, so every hard rule has an owner` | [`lessons/L-39.md`](lessons/L-39.md) and [`lessons/L-43.md`](lessons/L-43.md) | **A hypothesis, and the lesson beside them argues for the harder half.** Correcting `13` to `54` and `114` to `130` restores the state that decayed and buys one week. Neither number is load-bearing: L-39's point is that a cited section never existed, and L-43's is that a partition must cover the whole population - both survive the count being deleted. So the candidate remedy is **removing the figures rather than refreshing them**, which is what `PR-12` proposes for the two documents carrying the same population | `xs` | [T-250](../tasks/T-250-two-lessons-state-a-figure-and-two-link-to-a-dead-anchor.md) | open |
| `PR-104` | project documentation | **Med** | **Two lessons link to an in-page anchor the one-file-per-lesson split removed, and every gate reports the tree clean.** [`lessons/L-38.md`](lessons/L-38.md) line 11 and [`lessons/L-42.md`](lessons/L-42.md) line 36 both link `L-36` to an anchor beginning `#l-36--a-stated-tolerance`. That anchor was a heading in `LESSONS.md` when every lesson lived in one file; [T-146](../tasks/T-146-one-file-per-lesson-with-a-generated-index.md) gave each its own file, and neither file now contains that heading, so both links resolve to nothing. **Neither instrument can see it.** `lessons.py` matches the id pattern case-sensitively, so the lowercase anchor is invisible while the link *label* resolves correctly; `refcheck.py` returns *3860 document pointer(s) checked, 0 broken*. **These are the only two broken in-page anchors in the tree** - the third, in [T-056](../tasks/T-056-humanize-the-human-facing-documents-before-publishing.md), resolves to that file's own heading. **L-09** calls a dangling pointer a defect, and this repository publishes | Listing every tracked `.md` carrying a `](#` link returns exactly three files; the two lesson files carry the `#l-36--` anchor and neither holds a matching heading. `python tools/docs/lessons.py` and `python tools/docs/refcheck.py` both exit 0 | [`lessons/L-38.md`](lessons/L-38.md), [`lessons/L-42.md`](lessons/L-42.md), and whichever gate takes the class | **A hypothesis in two parts.** The links are `xs` - point both at `L-36.md`, which is what every other cross-lesson reference does (L-63 links `L-97.md` twice). The part worth deciding is whether `refcheck.py` should resolve an in-page anchor against its own file's headings: three instances tree-wide is a thin population for a new check (**L-75**), and against that, this class was created wholesale by one split and nothing would have reported it | `s` | [T-250](../tasks/T-250-two-lessons-state-a-figure-and-two-link-to-a-dead-anchor.md) | open |
| `PR-105` | project documentation | **Low** | **The lesson that says one document owns each fact claims to be that document, and after the split it owns one lesson.** [`lessons/L-13.md`](lessons/L-13.md) ends *`CLAUDE.md` owns the rules, `BRIEF.md` owns what to build, `TASK-WORKFLOW.md` owns the task mechanics, and **this file owns the lessons***. Written when `LESSONS.md` held all of them the sentence was true; [T-146](../tasks/T-146-one-file-per-lesson-with-a-generated-index.md) made *this file* `docs/lessons/L-13.md`, which owns `L-13`. The other three clauses still resolve, which is what makes the fourth read as maintained. **The instrument cannot reach it**: `lessons.py` checks the id, the title, the category and the citations, never a sentence | The clause is the file's last paragraph; the file is one of 137 under `docs/lessons/`, and `python tools/docs/lessons.py` reports `137 lesson(s), 137 row(s) in the index` | [`lessons/L-13.md`](lessons/L-13.md) | **A hypothesis.** *This file* becomes [`LESSONS.md`](LESSONS.md) and its folder, which is what now owns them. Worth checking the same clause elsewhere in the split set before fixing one - a self-reference is the class of sentence a file move breaks silently, and this move took 137 files at once | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-106` | project documentation | **Low** | **Twenty-one of 137 lesson files end with a horizontal rule that separated entries in a file that no longer exists.** `L-46`, `L-47`, `L-48`, `L-49`, `L-50`, `L-55`, `L-61`, `L-72`, `L-73`, `L-74`, `L-75`, `L-76`, `L-81` to `L-87`, `L-98` and `L-99` each end on a `---` rule with nothing after it, so each renders a line under its last paragraph. In `LESSONS.md` that mark separated one lesson from the next; [T-146](../tasks/T-146-one-file-per-lesson-with-a-generated-index.md) cut the file at those marks and 21 of them went with the lesson above. **The set is inconsistent rather than uniformly wrong**, which is the tell that it is residue and not a house style - the other 116 files end on prose. Twelve are cycle 21's and nine are cycle 22's; the class is one fix and is recorded here whole (`AM-09`) | Read each file's last non-blank line: 21 of 137 are `---`. The generated index is unaffected - `python tools/docs/lessons.py` exits 0 either way, since a trailing rule is not a heading, a category or a citation | `docs/lessons/`, the 21 files named | **A hypothesis.** Delete the trailing rule in all 21 in one change, and say in [`LESSONS.md`](LESSONS.md)'s *To add a lesson* instruction that a lesson file ends on its last paragraph - otherwise the next hand-written lesson copies whichever neighbour it was read beside. Cheap enough that the cost is deciding whether it is worth doing at all | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-107` | project method | **Low** | **Five cycles ran and wrote no log row, and the three that did were prepended into a table the other twenty-three keep in chronological order.** [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md)'s `## Log` carries a row for cycles 0 to 12 and for 18, 19 and 20, and **none for 13, 14, 15, 16 or 17** - the five that raised `PR-58` to `PR-89`, a third of the run's findings. Section 2 step 4 says *set the cycle's status here, commit, stop* and names only the status cell, so the log row is a convention the record kept for thirteen consecutive cycles and then stopped keeping, with nothing to notice. **The three that came back arrived at the top**, newest first, above a block that runs oldest first from `→ proposed`, so the table now reads in two directions. The coverage ledger in this register is unaffected and complete for all 21 cycles, which is why the gap is `Low` rather than a coverage claim | The log holds 26 dated rows; extracting the cycle each names gives 0 to 12, 18, 19, 20 and no 13 to 17. Section 2's status column marks all of 13 to 17 `done`, and this register's section 2 carries a ledger row for each | [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md)'s `## Log`, and section 2 step 4 | **A hypothesis.** The five rows cannot be written now without inventing them - what each cycle found is in the ledger and in the finding rows, and a log row reconstructed from those is a summary of a summary rather than the account the cycle wrote. So the candidate remedy is the forward half only: **step 4 names the log row as well as the status cell**, and the table states its own direction. The backward half is a decision for cycle 40 - either accept the five as unrecoverable with a dated note, or accept that the ledger is the record and the log is commentary, in which case cycles 18 to 21 are the anomaly and not 13 to 17 | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-108` | project documentation | **Low** | **The two dead source-line citations `PR-95` found in the ruleset audit have a second home it did not name, and the same paragraph repeats its deck count too.** [`lessons/L-115.md`](lessons/L-115.md) reads *DS-146 is `shell/deck.js:410` and DS-147 is `countUp()` at `shell/deck.js:825`, each carrying its rule id in a comment, and **both ship in all three decks***. Today DS-146 is line **471**, `countUp()` is line **952**, line 410 is the quick-view title assignment and line 825 is **blank**; **four** decks ship. That is `PR-95`'s row word for word, in a different file - and `PR-95`'s subject cell names [`RULESET-AUDIT.md`](RULESET-AUDIT.md) §2 and its §5 `DS-146` row and nothing else. **The lesson's own argument is untouched**: both rules are still alive, `grep -c -i chart shell/components.css` is still `0`, and the reference deck still draws its chart by hand - so what decayed is only the proof offered for it. **This is the second half `PR-95` predicted and did not sweep for.** Its remedy already reads *a line citation anywhere in the record is unwatched by construction*, and *anywhere* turned out to include the lesson written from the same finding - which is **L-133**'s second half exactly: the repair was scoped to the document that hurt rather than to the habit | `grep -n DS-146 shell/deck.js` prints 471 and `grep -n countUp shell/deck.js` prints 952 and 972; `sed -n '410p;825p' shell/deck.js` prints the quick-view title line and an empty line. `git log --oneline --since=2026-08-17 -- shell/deck.js` lists three commits, so the file moved three times after the lesson was written on 2026-08-17. Searching every tracked `.md` for a `file.ext:NNN` citation returns **16 files**; the only two live documents carrying this pair are `RULESET-AUDIT.md` and this lesson, and the rest are closed task records, which are history and stay true | [`lessons/L-115.md`](lessons/L-115.md), and whichever gate takes the class | Cite the symbol, not the line - the same one-line fix as `PR-95`, in a file that fix would have missed. **The hypothesis is `PR-95`'s and this row raises its price**: a *do not cite a line number* rule is checkable and is now backed by two live instances rather than one, and the sweep that finds them has to run over the tree rather than over the document being repaired | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-109` | project documentation | **Low** | **A lesson about checking a self-quoting page quotes that page's live sentence, and both of its numbers have since moved.** [`lessons/L-104.md`](lessons/L-104.md) is built on the two sentences in [`../CLAUDE.md`](../CLAUDE.md)'s tier-1 bound and distinguishes them by name: *The **live** sentence reads "15,208 bytes against `tasks/TASK-WORKFLOW.md`'s 11,925"* against *The **record** sentence beside it reads "it read 18,807 against `.taskmd/config.md`'s 14,087"*. The live sentence now reads **15,572 against 13,324**. **The half the lesson calls a record still matches and the half it calls live does not** - which inverts the very distinction the lesson exists to draw, in the lesson that draws it. **The mechanism is intact**: today's sentence still never writes `CLAUDE.md` as a label, so the first term still binds nothing, which is the lesson's actual claim. It is the class `PR-103` names one file over, and the third document this audit has found stating tier 1's bound at a value the file no longer carries - cycle 3's ledger row re-measured it to 15,572 against 13,324 with a stated debt of 2,248, and `PR-14`'s ruling has since moved the floor again | `grep -n 'still over its own bound' CLAUDE.md` prints *15,572 bytes against `tasks/TASK-WORKFLOW.md`'s 13,324, measured 2026-08-22*, and the two stale numerals are lines 13 and 15 of the lesson. `grep -n 18,807 CLAUDE.md` still returns the record sentence the lesson's other half quotes, unchanged. `python tools/docs/figures.py` reports `0 stale figure(s)`, and `python tools/docs/lessons.py` exits 0 - neither reads a quotation inside a lesson body | [`lessons/L-104.md`](lessons/L-104.md) | **A hypothesis, and the lesson argues against the obvious fix in its own text.** Refreshing the two numerals restores a state that decays on the next edit to either file, and `PR-103` proposes deleting such figures rather than refreshing them. The form that survives is to quote the sentence's **shape** and not its values - *the live sentence names no label for its own file* - which is the lesson's point and needs no number at all. Worth deciding once for the class: `PR-103`, `PR-105` and this row are three lessons quoting a tree that moved under them | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-110` | project method | **Low** | **The audit's own severity table undercounts its own findings, and it was already wrong before the cycle that last reconciled it.** [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 3 states **High 8, Medium 63, Low 34** - 105 findings. Section 3 of this register holds **107** rows with no gap and no reuse: **High 8, Medium 64, Low 35**. **Cycle 21 added four Medium and three Low correctly and carried an existing error forward**, so the table was short by one of each before it, and adding to it accurately is exactly what preserved the defect. That is **L-136** in the run's own record - *a count with no membership cannot be audited, and its errors cancel instead of showing* - written about this audit's coverage table and now true of its findings table. `PR-107` is the neighbouring row and does not reach this: it names the missing **log** rows and states that *the coverage ledger in this register is unaffected and complete*, which is true and says nothing about the severity counts | Parse section 3's rows out of this register - every line beginning with a `PR-nn` cell - and count the third cell: it prints `107 rows, 107 unique, High 8 Medium 64 Low 35`. The last id is `PR-107` and the ids are consecutive from `PR-01`, so nothing is missing from the rows themselves. **The cycle corrected the cells rather than only raising them**, per cycle 7's precedent that a document this run writes into is this run's to keep true, so the table now states the derived values including this cycle's three | [T-219](../tasks/T-219-pre-release-audit-of-the-whole-repository.md) section 3's counts table | **A hypothesis.** Correcting the cells buys one cycle, which is what just happened and is why the row exists. The half that lasts is a command - the count is a fold over rows this register already holds, so `findings.py` or a flag on it can print the table and fail when the task record disagrees, which is `PR-101`'s remedy pointed at the other hand-kept table in the same document. Whoever implements it decides whether a struck-through rank still counts as raised, because that is the one judgement a script cannot take | `s` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-111` | project documentation | **Low** | **The rules register's verdict summary disagrees with its own tables in two different ways, and one of the errors was written by a later edit that changed a single line.** [`research/R1-rules-candidate.md`](research/R1-rules-candidate.md)'s summary box states **110 keep, 17 amend, 1 drop, 26 defer - 154**. The tables hold **111 keep and 16 amend**; the drop, the defer and the total are exact. **The mover is `A10`**, whose cell now reads *keep* followed by an italic note saying it *read `amend` from the day it was written* - changed on 2026-08-22 by [T-211](../tasks/T-211-scope-speaker-notes-and-decide-what-ds-088-becomes.md), in a commit whose stat line is `1 file changed, 1 insertion(+), 1 deletion(-)`. The row records its own history and the count four screens above it was never touched. **The second error was wrong when written and has nothing to do with A10**: the same box breaks the 26 defers down as *11 process rules to T-020, 5 check mechanics to T-005, 7 critique-format rules to T-004, and 4 pipeline rules from group A*, which sums to **27**. Group A-prime carries **10** defers, not 11 - its eleventh rule is a keep - so the component was taken from the group's size rather than from its verdicts. **The pair is in a second live document**: [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md) line 19 restates *154 corpus rules were resolved into this ruleset: 110 keep, 17 amend, 1 drop, 26 defer*, which is cycle 20's file and is recorded here rather than raised twice (`AM-09`) | Parse the register's rule rows - every line whose first cell is a rule id - and count the bolded verdict: it prints `154 rows, keep 111, amend 16, defer 26, drop 1`. Counting the defer targets by group prints `A 4, A-prime 10, K 5, M 7`. `git show 4a2b296 -- docs/research/R1-rules-candidate.md` is the one-line change, and its own message says *R1's candidate rule A10 reads keep instead of amend*. `grep -n '110 keep' docs/DESIGN-RATIONALE.md` returns line 19 | [`research/R1-rules-candidate.md`](research/R1-rules-candidate.md)'s summary box, and [`DESIGN-RATIONALE.md`](DESIGN-RATIONALE.md) line 19 | **A hypothesis, and the two halves want different fixes.** The verdict tally is a fold over rows in the same file and can be printed rather than typed, which is the durable half and is `PR-110`'s remedy pointed at a second table. The defer breakdown is prose that was never derived at all, so correcting 11 to 10 restores a sentence nothing will re-check - the form that lasts is naming the groups and letting the count come from them. Worth deciding once: this is the third hand-kept count this audit has found in a document that also contains the rows it counts | `s` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-112` | project method | **Med** | **Tier 1 states as fact the two figures the specification records as measured false, in the rule the file calls one of *the rules that must survive*.** [`../CLAUDE.md`](../CLAUDE.md) rule 1 reads *Every deck in the source corpus failed this - **2-7 external references each**, mostly web fonts*. [`BRIEF.md`](BRIEF.md) carries a banner on the section that claim came from: ***Superseded 2026-08-06 by R1**... two of its claims are wrong: external references range **0-21, not 2-7**, and **three decks are already fully self-contained**... **Use R1's numbers, not the table above.*** So *every deck failed* is false by the project's own measurement - three passed - and *2-7* is the range R1 replaced. **The tell is that the paragraph was edited on the day the correction landed.** Rule 1's second half opens *Measured and settled 2026-08-06* and carries R5's 192 KB result, so someone updated the conclusion and left the two corrected figures in the sentence above it - **L-96**'s *the copy in the document under the knife is the one a survey most reliably misses*. **This is tier 1**, which every session of this project loads before its first tool call, and `CLAUDE.md` itself says *Read the brief first - its Decisions taken section overrides anything older in it* | `grep -n 'external reference' CLAUDE.md` returns line 110 with the claim and line 114 with the dated amendment below it; `sed -n '59,64p' docs/BRIEF.md` returns the superseded banner naming both corrections. The register carries no row on it: searching this file for `2-7 external`, `0-21` and `source corpus failed` returns zero. Cycle 3 read both files together and checked four other claims in them, none of which was this one | [`../CLAUDE.md`](../CLAUDE.md) rule 1, and cycle 3's ledger row above | **A hypothesis, and the cheap fix is not the right one.** Replacing 2-7 with 0-21 keeps a corpus figure in tier 1 that no gate reads and that only R1 can restate. Rule 1's argument does not need either number - *self-contained or it does not ship* stands on the measurement already beside it - so the candidate remedy is **deleting the corpus figure and pointing at R1**, which is `PR-103`'s and `PR-12`'s proposal applied to the file that is paid for on every turn. It also removes bytes from a document that is over its own bound, which is the one place in this repository where a deletion has a second reason | `xs` | [T-236](../tasks/T-236-tier-1-and-the-brief-against-what-they-measure.md) | open |
| `PR-113` | project documentation | **Low** | **The external-principles note's evidence grade is its own thesis, and the population it grades is stated three ways that no two agree on.** [`research/R2-external-principles.md`](research/R2-external-principles.md) section 2 says *Counted across sections 4 to 11: **7 principles at E1, 8 at E2, 9 at E3, 2 at E4*** - which sums to **26**, while those sections carry **28** principles, `P-01` to `P-28`. **`E4` appears exactly once in the whole range**, on `P-10`, which is itself marked *rejected as stated*. And section 14 hands the next task *the two E4 principles (`P-09`'s exact range, `P-12`'s scope)* - **section 6 grades both of them `E3`**. Section 2's own gloss disagrees with all of it: *the two E4s are both about visual pacing*, where `P-09` is line length and `P-12` is sans-serif legibility at distance, both typography. **The grade is not decoration in this document**: section 2 states that *where an E4 rule conflicts with an E1 or E2 one, it loses without discussion*, so the E4 set is the set that loses arguments, and section 14 flags it as the set that is *cheap to overturn*. Naming the wrong two hands that licence to two principles the document graded higher | Extracting each `P-nn` heading's bracketed grade from sections 4 to 11 returns **28** principles and `E1` 11, `E2` 5, `E3` 16, `E4` 1, counting every grade a heading mentions; `P-09` and `P-12` both read `[E3]`. `python tools/docs/figures.py` reports `0 stale figure(s)` and no research document is in its artifact manifest, so nothing watches any figure in this file | [`research/R2-external-principles.md`](research/R2-external-principles.md) section 2's tally and section 14's *two open items* | **A hypothesis with a question inside it.** The tally can be derived from the headings, which is the same fix as `PR-110` and `PR-111` and would settle the count. It does not settle section 14, because a heading carrying `[E2/E3]` or `[E1 via P-06]` has no single grade - so deriving the tally first forces the prior question of **whether a principle has one grade or a set**, which is what made three different counts possible. Answer that before printing anything | `s` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-114` | project documentation | **Med** | **R9 describes DS-122's rule and its check as they were before the change R9 itself proposed, which shipped in the current release.** Section 1 states *DS-122 is `hard`, `auto` and owned, and its check is a list of five vendor names*, and prints a probe table showing `uPlot`, `tanstack charts`, `apexcharts` and `frappe-charts` all passing a rule reading *no chart library*. Section 8 opens *The rule today is a flat ban with a name-matching check*, proposes the threshold and the declaration block, and closes **That change is not made here. It needs a child task**. [T-202](../tasks/T-202-amend-ds-122-into-a-threshold-and-bind-its-check-on-structure.md) **is that child task, it is `done`, and it shipped in `0.6.0`** - the release this audit is being run after. `DS-122`'s rule text today is section 8's proposal almost verbatim, and the check binds on three run-time mark mechanisms rather than on five names. **The document was edited on 2026-08-21 and not for this**: five dated corrections from [T-205](../tasks/T-205-correct-r9s-gate-7-scoring-and-its-account-of-looking.md) sit in sections 3, 6, 7 and 9, every one of them about gate 7 or about looking, so the pass that brought the note current went through the sections beside this one - **L-96**'s *the copy in the document under the knife is the one a survey most reliably misses* | `grep -n 'DS-122' docs/DESIGN-SYSTEM.md` returns the threshold text, *a deck whose charts the reader is expected to interrogate may carry a chart engine - declared, and its output must be SVG*. `sed -n '694,700p' tools/deck/audit.py` carries the comment opening *It was a substring blocklist of five vendor names*, with the calibration across all five shipped decks beneath it. `grep '^status:' tasks/T-202-*.md` prints `done` and `shipped_in` prints `0.6.0`. Searching this register for `DS-122` returns four hits and none is this; `PR-71` cites R9 as the one document that has the stacked area right, which is a different subject | [`research/R9-embeddable-chart-library-versus-hand-authored-svg.md`](research/R9-embeddable-chart-library-versus-hand-authored-svg.md) sections 1 and 8 | **A hypothesis, and the cheap fix is not obviously the right one.** The document already has the form: section 9's fourth item is struck through and marked *Settled 2026-08-21 by T-205*, so a dated closure on section 8 naming T-202 is one line in a shape the file already uses. Section 1's probe table is the harder half - it is evidence about the check that was replaced, and `audit.py`'s new comment still cites that evidence as its own reason, so deleting the table loses the **L-125** case rather than correcting it. Whoever implements this decides whether the table is restated as history or re-probed against the structural check | `xs` | [T-251](../tasks/T-251-a-research-note-and-a-memory-entry-describe-a-state-that-changed.md) | open |
| `PR-115` | project method | **Med** | **The document that binds this project's audits to their methods names a method for one of the three, and at least one of the other two has one.** [`AUDIT-METHOD.md`](AUDIT-METHOD.md) opens *The method is not here* and then names taskmd's three files for the pre-release audit, with the shipped version and the byte count. Section 1's table lists **Context economy** with a trigger - *request, or a change to tier 1* - and a register, and no method at all; the **Ruleset** row is the same. **The context-economy method exists and it is not R8.** [`research/R8-context-economy-for-coding-agents.md`](research/R8-context-economy-for-coding-agents.md) opens with a banner reading *the operative method is now the `ecoctx` skill*, packaged into its own repository by [T-137](../tasks/T-137-package-the-context-economy-method-as-a-skill.md) and shipped in `0.3.0`, and it says of itself that *where the two differ, the skill is the method and this document is a record of what it was on the day it was packaged*. **Both local documents that could route a session send it to the record instead.** [`CONTEXT-AUDIT.md`](CONTEXT-AUDIT.md) names R8 section 5 and section 2.1 as what it was run against, and `AUDIT-METHOD.md` names nothing - so a session triggered by a change to tier 1, which is a live trigger, reads a snapshot that warns it may be out of date | `git grep -c ecoctx` returns five tracked files - `docs/RELEASE-PHASES.md`, R8 itself and three task records - and neither `docs/AUDIT-METHOD.md` nor `docs/CONTEXT-AUDIT.md` is among them. `grep '^status:' tasks/T-137-*.md` prints `done` and `shipped_in` prints `0.3.0`. Searching this register for `ecoctx` and for `T-137` returns zero. The pre-release row is the control: it names three upstream files, a version and 11,371 bytes, and that figure is exact today | [`AUDIT-METHOD.md`](AUDIT-METHOD.md) section 1's table and the sentence above it | **A hypothesis.** A Method column on section 1's table, or one clause extending the opening sentence, and the file is 4,430 bytes so neither is a budget question. **The prior question is whether the third row can be filled**: the Ruleset audit names `RULESET-AUDIT.md` and [T-119](../tasks/T-119-audit-the-ruleset-for-rules-that-cost-more-than-they-return.md) and no method either, so a column fillable twice of three would state the gap rather than close it. Whoever implements this decides that first, because it changes whether the answer is a column or a sentence | `xs` | [T-239](../tasks/T-239-the-audits-own-record-against-what-it-did.md) | open |
| `PR-116` | project documentation | **Low** | **The unsent harness register's provenance counts three rows where the document holds four, and the sentence forty lines above it says four.** [`upstream/harness.md`](upstream/harness.md)'s opening reads *Four observations, none ranked and none a request*, followed by an italic note recording that `O-C3` was added 2026-08-20 and `O-C4` on 2026-08-22. Its **Provenance** section still reads *All three rows are implementation vintage*. So one document states its own size twice and the two disagree: the header was maintained across both additions and the closing paragraph was not. **This is the one upstream register that was never sent** - its disposition banner records *no route identified* and says a later session may send it without re-deciding - so the wrong count is what a first reader of the delivered document would meet, and that banner is the reason such a reader still exists | `grep -c 'O-C' docs/upstream/harness.md` over the row column returns 4; `grep -n 'Four observations' docs/upstream/harness.md` returns line 16 and `grep -n 'All three rows'` returns line 55. **The vintage claim itself holds for all four** - every row records being found while building rather than while auditing - so the number is the only wrong thing in the sentence | [`upstream/harness.md`](upstream/harness.md)'s Provenance section | One word. **The hypothesis is about the durable half**: this is the fourth hand-kept count this run has found in a document that also contains the rows it counts, after `PR-110`, `PR-111` and `PR-113`, and four instances is the argument for a check rather than for a fourth correction. The three registers under `docs/upstream/` all carry one row per id in one table, close to the shape `findings.py` already reads for a different register - so the candidate is one more reader rather than a new tool. Weigh that against cycle 0's ruling that a second reader gets written against a table that has never held a row | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-117` | project documentation | **Low** | **The adopter report's covering note attributes a version caveat to its index, and the index states nothing of the kind - nor does any of the twenty-seven records.** [`adopter-reports/claimai/README.md`](adopter-reports/claimai/README.md)'s *How to read the set* says *Fourteen of these were stamped rather than re-run, so treat the version as provenance, not as a fresh reproduction. One uncertainty is stated in the index: two versions were installed within the same hour, so a record found in that hour may have seen the earlier one.* The index is the same document's findings table, whose columns are number, kind, severity and title - it carries no version column and no such sentence, and the phrase appears nowhere else in the set. **So the caveat is both unlocatable and unresolvable**: all twenty-seven records stamp `Version seen` as `0.6.0`, no record names the earlier version, and nothing marks which fourteen were stamped rather than reproduced. A reader weighing provenance is told to distrust half the set and given no handle | `grep -rn 'same hour' docs/adopter-reports/claimai/` returns exactly one line, `README.md:51`, which is the sentence making the claim. Reading the `Version seen` cell out of all 27 records returns one value, `0.6.0`, in three spellings. **Nothing else in the set reads this way**: every claim naming a file, a line or a symbol was re-run against the tree this cycle and all of them reproduce, line numbers included | the *How to read the set* section of [`adopter-reports/claimai/README.md`](adopter-reports/claimai/README.md) | **A hypothesis, and the received-document rule complicates it.** The README's own last bullet leaves the `Target` rows verbatim *because editing an evidence record on the way out is worse than an odd line in it*, and that argument covers the records - not the covering note this project publishes them under. The candidate is a bracketed editor's note under the sentence, saying the index carries no such statement and every record reads `0.6.0`, which keeps the sender's text and stops the pointer being followed. Deleting the sentence is the alternative and loses the fact that the sender knew of the uncertainty | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-118` | project documentation | **Low** | **The chrome-row sketch's own description says *what ships today*, and what it draws as today has not shipped since `0.4.0`.** [`sketches/chrome-row-candidates.svg`](sketches/chrome-row-candidates.svg)'s description element reads *What ships today, and two candidates built on one principle... Option X puts them in a second quiet section; option Y folds them into a standalone More control whose menu opens upward*, and a panel label repeats *ships today*. **Option Y was ruled by the owner on 2026-08-12 and shipped.** [T-114](../tasks/T-114-the-chrome-row-layout-the-pager-deserves-the-corner.md) is `done` at `0.4.0`, its review table records *Y ruled 2026-08-12*, and the shell carries the More menu. So the panel labelled *today* is the pre-`0.4.0` row and one of the two panels labelled a candidate is the shipping one, with nothing in the file saying so. **It is a deliverable rather than a scratch file** - T-114's front matter declares it and the record cites it four times - and it is the only file under `docs/sketches/` | `grep -o 'ships today' docs/sketches/chrome-row-candidates.svg` returns two hits, the description and the panel label; `grep -n 'Y ruled' tasks/T-114-*.md` returns the ruling with its date; `grep -c 'moreMenu' shell/deck.js` returns a live menu. **The cycle brief's *orphan sketch* question is answered the other way and is recorded here rather than as a second row**: `git grep -l chrome-row-candidates` returns one file, T-114, so the sketch has a referrer, and that referrer is the record carrying the outcome the sketch does not | [`sketches/chrome-row-candidates.svg`](sketches/chrome-row-candidates.svg)'s title and description elements | **A hypothesis, and the cheap fix may be the wrong shape.** Restamping the labels - *before `0.4.0`* against *shipped* - keeps a picture that drifts again at the next chrome change, and a sketch redrawn to today is a different artifact from the one T-114's ruling rests on. The candidate is the opposite: **date it closed.** One line saying Y was ruled on 2026-08-12 and shipped in `0.4.0` turns *what ships today* into what it always was, a snapshot, and costs nothing a redraw would cost | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-119` | project method | **Low** | **A memory entry tells a session to write a stdout idiom no file in this repository uses, and justifies it by saying every tool here does.** The store's `a-traceback-after-the-write-still-wrote` gives four instructions for a script that dies printing non-ASCII, and the second reads *put `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')` at the top of any script that may print non-ASCII - **this project's own tools do exactly that, so match them***. No tracked file contains `TextIOWrapper`. **44 of the 50 tracked `tools/**/*.py` carry a different form**, guarded: `if hasattr(sys.stdout, "reconfigure"): sys.stdout.reconfigure(encoding="utf-8", errors="replace")`. The effect is the same, so nothing breaks; what fails is the instruction's whole stated purpose, which is to match the neighbours | `grep -rn TextIOWrapper tools/` returns nothing. `grep -rl "stdout.reconfigure" tools/` lists **44** files, against the **50** that `git ls-files 'tools/*.py' 'tools/**/*.py'` lists. `tools/check_all.py` lines 49-50 is the shape they use | the memory entry `a-traceback-after-the-write-still-wrote` - untracked, and this cycle's own subject | Replace the quoted line with the two the tools actually carry, and drop *do exactly that*. **A hypothesis**: the `hasattr` guard may be the point rather than an accident - it is what lets the same file run where stdout cannot be reconfigured - so whoever fixes this states which of the two properties the rule is asking for | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-120` | project method | **Med** | **A memory entry records an annotation in tier 1 that was never made, and describes a passage that never said what it is said to have said.** The store's `decide-detailed-questions-yourself` closes: *This supersedes the ask-first advice in [[research-before-building]] and htmldeck's `CLAUDE.md` "The objectives are still being shaped", both of which close on "the owner prefers being asked". **Both have been annotated to say so, so reading either one first leads here.*** The memory half holds - `research-before-building` carries exactly that annotation. The `CLAUDE.md` half does not: the passage is three sentences closing on *not quietly worked around*, it carries no ask-first clause, no annotation and no pointer, and **no commit in the file's history has ever carried either**. So one of the two stated entry points leads nowhere, and an entry that exists to route a reader to the newer rule claims a repair to a file loaded on every turn that no session made | `grep -n "objectives are still being shaped" -A3 CLAUDE.md` prints the three sentences. `git log -S "prefers being asked" -- CLAUDE.md` and `git log -S "decide-detailed-questions-yourself" -- CLAUDE.md` both return **nothing** over the whole history of the file | the memory entry `decide-detailed-questions-yourself` - and it is the entry rather than [`../CLAUDE.md`](../CLAUDE.md), because the passage is right as it stands and has nothing to supersede | Cut the `CLAUDE.md` half of the sentence and keep the memory half, which is true. **A hypothesis**: it may be pointing at text that moved rather than at text that never existed - `CLAUDE.md` lost 20.1% on 2026-08-22 - so whoever fixes it searches the removed prose first. This cycle searched every commit of the file and found none, which is why the row is written as a claim that was never true rather than as drift | `xs` | [T-251](../tasks/T-251-a-research-note-and-a-memory-entry-describe-a-state-that-changed.md) | open |
| `PR-121` | project method | **Low** | **The one pointer a handoff's own *Resume here* line depends on was reconstructed rather than derived - three days after the memory entry recording that exact failure on that exact task.** `.handoff/processed_20260817-231833_HANDOFF.md` opens *Work T-119*, linked to a `tasks/` file whose slug reads `...-rules-that-cost-more-to-satisfy-than-they-return` - quoted here as a bare slug, because both `taskmd check` and `refcheck.py` bind on the path string rather than on the link syntax, so a row citing a dead pointer in any recognisable form fails the gate that the pointer's own document would have. No such file has ever existed: T-119 was added at `fa3c439` as `T-119-audit-the-ruleset-for-rules-that-cost-more-than-they-return.md` and has carried no other name in any commit. It is **the only broken relative pointer in 1,348 across the 98 archived handoffs**, and the only full-slug link to T-119 anywhere in the archive. The memory entry `derive-a-path-never-reconstruct-it`, written 2026-08-14, names T-119's slug as its **first worked example** | the archive resolver returns *1,348 relative pointer(s) checked, 1 broken*. `git log --all --name-only --pretty=format:` names exactly one `tasks/T-119...` path across the whole history, and it is the correct one. The entry's mtime is 2026-08-14; the archived handoff's name stamps 2026-08-17 | `.handoff/processed_20260817-231833_HANDOFF.md`, which the handoff core's section 1 makes a record rather than a document - so the live question belongs to the memory entry `derive-a-path-never-reconstruct-it` | **A hypothesis, and the cheap fix is the wrong one.** Correcting the record rewrites history and buys nothing; the finding is that a memory rule did not reach the moment it applied, on its own worked example, three days after it was written. Whoever triages this decides whether that is the store's problem or the handoff write step's, and closes the row against one of them - not against the file | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-122` | project method | **Med** | **This repository's ignore rules come from three files, only one of which a clone receives - and both rules covering `.claude/` are among the two that it does not.** [`../.gitignore`](../.gitignore) is tracked, carries ten rules and gives each a reason. Two further sources are invisible to an adopter: `.git/info/exclude` ignores `.claude/worktrees/`, and the machine's global core excludes file ignores `**/.claude/settings.local.json`. So the one directory this cycle's brief names by hand is protected **entirely from outside the repository**, and `git ls-files .claude/` is empty. An adopter who clones and runs an agent in the tree gets a `.claude/settings.local.json` that `git status` reports as untracked, and this project's own commit habit is `git add -A`. The damage is already written down in the memory store - *a committed local settings file disables an adopter's skills when they clone*. `check_all.py`'s `tracked()` docstring shows the project reasoning carefully about what a fresh clone receives; it never asks the same question of the rules that decide it | `git check-ignore -v .claude/settings.local.json` names line 3 of the machine's global ignore file, and `git check-ignore -v .claude/worktrees` names `.git/info/exclude` line 7 - neither path is written here, because this document publishes. `grep -c claude .gitignore` returns **0**. `git ls-files .claude/` returns nothing | [`../.gitignore`](../.gitignore) | Give `.gitignore` the two rules, each with its reason, so the protection ships with the repository that needs it. **A hypothesis**: the right rule may be `.claude/` wholesale rather than two paths - nothing under it is tracked today, so a broad rule costs nothing now and forecloses shipping a settings file later, which is a decision to take rather than a line to write | `xs` | [T-252](../tasks/T-252-the-ignore-rules-come-from-three-files-and-one-ships.md) | open |
| `PR-123` | project method | **Low** | **Nothing in this repository ever looks at what `.gitignore` hides, and one cycle reading it by hand found three leftovers.** `tools/check_all.py` partitions tracked tools and tracked decks; `tools/docs/refcheck.py` reads `.gitignore` precisely in order to **skip** what it names. So the ignored surface has no instrument at all, and each of the three is invisible to every gate. `.claude/settings.local.json` still grants `Bash(python tools/tasks/task.py --help)`, and that file was **deleted on 2026-08-09** at `717e678`, *Retire the pre-split task tool* - the settings file was itself rewritten on 2026-08-13, four days later, and kept the permission. `examples/reference-deck2.pdf` is 277,680 bytes named by no tracked file and written by no tool that emits a `2` suffix, dated 2026-08-13 - the day of the print-geometry diagnosis (**L-76**). And `.gitignore` ignores `control/` and `dist/`, **neither of which appears in any commit of this repository**, while `check_all.py`'s `tracked()` docstring cites both as worked examples of what is *machine-local by design* | `git log --all --name-only --pretty=format:` names no path under `control/` or `dist/`. `git grep -l reference-deck2` returns nothing. `git log --diff-filter=D -- tools/tasks/task.py` returns `717e678`, 2026-08-09. `git ls-files tools/tasks/` returns `lint.py` and `query.py` | no single file, and that is the finding - the three sit in `.claude/settings.local.json`, `examples/` and [`../.gitignore`](../.gitignore) | **A hypothesis, and the general form is the point.** Deleting the three takes minutes and buys nothing durable. The candidate is one command that lists what the tree ignores and asks whether each entry still has a producer - the partition `check_all.py` applies to tools, pointed at the half it deliberately looks away from. Whoever takes it decides first whether an ignored surface is worth a gate at all, since by construction no two adopters have the same one | `s` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-124` | product documentation | **Med** | **The section written so nobody has to infer this project's state says the sample is one project, and a second adopter's twenty-seven findings have been tracked in the tree since 2026-08-28.** [`../README.md`](../README.md)'s *What does not exist yet* opens *It has been installed and used by **one project other than this one**, which found six defects in two days* at line 256 and closes *The sample is one project* at line 263. [`adopter-reports/claimai/`](adopter-reports/claimai/README.md) arrived on 2026-08-28: **twenty-seven numbered records and a README**, from a different outside project that built a twenty-five slide executive board deck under a training-exam deadline between 2026-08-23 and 2026-08-28 and kept 84 task records. **It is not the adopter the paragraph describes** - that one is [T-128](../tasks/T-128-publish-the-adopter-deck-as-a-worked-example.md)'s D6 deck, published here as [`../examples/measure-first/`](../examples/measure-first/measure-first.slides.md), and the six defects the same bullet lists are its `0.1.x`-era set. **This is L-05's own family, on the page [`PUBLISHING.md`](PUBLISHING.md) section 6 names as its worked example**: every pasted figure on the README is correct today and `figures.py` reports `0 stale`, because what went false is a sentence about a state the repository tracks rather than a numeral bound to a command. **Cycle 1 could not have found it** - it read this file on 2026-08-23, five days before the report landed | `ls docs/adopter-reports/` returns one directory, `claimai`, holding 27 numbered records plus a README. `git log --diff-filter=A --date=short --format=%ad -- docs/adopter-reports/claimai/README.md` returns **2026-08-28**. `grep -n "sample is one project" README.md` returns line 263. [T-225](../tasks/T-225-triage-the-claimai-adopter-report.md)'s *Where it came from* calls it *an outside project* and dates it 2026-08-23 to 2026-08-28. `python tools/docs/figures.py` exits 0 and prints *0 stale figure(s)* on the same tree | [`../README.md`](../README.md), the *What does not exist yet* section | **A hypothesis, and the cheap fix understates what happened.** Changing *one* to *two* keeps a sentence that goes false again at the third adopter. The candidate is to say what the section is actually reporting - how many outside projects have used this, and what each found - and to point at [`adopter-reports/`](adopter-reports/claimai/README.md) rather than count in prose, which is the move `PR-12`'s remedy proposes for the ruleset figures. **Whoever takes it decides first whether the second report belongs on the front page at all**, since T-225 has not triaged it and the page must not read as though its findings were accepted before anybody judged them | `xs` | [T-234](../tasks/T-234-the-front-pages-own-figures-and-claims.md) | open |
| `PR-125` | the product | **Low** | **The figure watcher's own prose carries stale figures in two places, and one of them is a stale quotation of the very sentence its fourth binding was written to watch.** [`../tools/docs/figures.py`](../tools/docs/figures.py) line 26 explains the two kinds of number with *a figure that describes a decision - **165** rule rows, **119** hard rules, **25** that need a person - moves when somebody changes the ruleset, which is rare and deliberate*; the same repository derives **177**, **130** and **29** today. Line 705, the comment introducing the binding written for tier 1's bound, says *its live sentence says "15,208 bytes against `tasks/TASK-WORKFLOW.md`'s 11,925"*; that sentence has read **15,572 against 13,324** since 2026-08-22. **Both substances hold** - the illustration still illustrates, and the live sentence still never writes `CLAUDE.md` as a label, which is the comment's actual claim - and five figures have moved under them. `DECLARED_DOCS` names five documents and no `.py` file, so **nothing in this repository reads a tool's prose**. It is `PR-47`'s shape one directory over, and the **fourth** place this audit has found tier 1's bound stated at a value the file no longer carries, after `PR-103`, `PR-109` and `PR-14` | `grep -n "119 hard rules" tools/docs/figures.py` returns line 26 and `grep -n "15,208 bytes" tools/docs/figures.py` returns line 705. `python tools/deck/ruleset.py --counts` prints 177 rule rows and `hard` 130; `README.md` line 236 states the judgement half as **29** and `figures.py` binds that one. `grep -n "still over its own bound" CLAUDE.md` prints *15,572 bytes against `tasks/TASK-WORKFLOW.md`'s 13,324*. `python tools/docs/figures.py` exits 0 on the same tree | [`../tools/docs/figures.py`](../tools/docs/figures.py) - the module docstring, and the comment above the fourth binding | **Batch it** with `PR-47`, which is this finding in `tools/deck/ruleset.py`. **A hypothesis**: an illustration does not need a live figure at all. The docstring's point is that one kind of number moves rarely and deliberately, which survives dropping the three values, and the comment can quote the *shape* of tier 1's sentence rather than its numerals. Restamping both leaves them free to drift again, which is what happened to the last stamp | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-126` | project method | **Low** | **A closed record cites a rule id the ruleset has never declared, and one character is the whole of it.** [T-200](../tasks/T-200-add-a-lobby-slide-and-count-the-argument-not-the-file.md) line 48 reads *DS-108's back-matter stage already exists - `data-stage="back"`, a slide outside the argument*. **The sentence is true of the tree and the id is not**: the back-matter stage was built by [T-108](../tasks/T-108-a-deck-has-no-back-matter-stage-so-the-colophon-is-labelled-with-the-last-argument-stage.md), a task, and the rule carrying it today is **DS-225**. A reader following `DS-108` finds nothing at all, which is worse than a wrong pointer that lands somewhere | `grep -rn "DS-108"` over the tree returns exactly one line, that one, and [`DESIGN-SYSTEM.md`](DESIGN-SYSTEM.md) declares no such row. Found by testing every `DS-nnn`, `L-nnn` and `T-nnn` the cycle's 25 files cite against its own register: **one miss in 25 files and 301,841 bytes** | The T-200 line, and for the class whatever would bind a bare id to its register | Read `T-108`, or `DS-225`. **The class is the half worth ranking**: nothing binds a bare `DS-nnn` to the ruleset - `refcheck.py` reads link targets and `taskmd check` reads section references, and an id written in prose is neither. A pass resolving every `DS-nnn` against the declared set is the shape `tools/docs/lessons.py index` already has for lessons, and it would decide this class tree-wide rather than one line of it | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-127` | project method | **Low** | **A task closed with its own open question unanswered and the task it promised unraised, and the condition it named still holds.** [T-188](../tasks/T-188-raise-the-shipped-motion-density-default-to-100.md) section 1 asks whether `references/build.md` tells a deck author to emit `--motion-density` at all, says to *run it and see a built deck's `:root`* before fixing or dropping it, and states that **if it is real it is a separate defect and gets its own task**. Sections 3 and 4 answer neither, and no task carries it. **Section 4 also drops two of section 1's five acceptance criteria** without a row saying so | `grep -rn "motion-density" skills/` returns nothing, so the skill still never names the token and the question's premise is intact. Nothing in T-188 sections 3 or 4 mentions the skill, `build.md` or the token's authoring. **The answer exists and is written nowhere**: `themes/quarto.css` declares the token, so a deck inherits it and the invalid `calc()` T-188 feared cannot arise - which means the next reader to meet this question re-runs a check that was always going to pass | T-188's record; and, for the class, the closing checklist in [`TASK-WORKFLOW.md`](../tasks/TASK-WORKFLOW.md) section 7 | One sentence in T-188 saying the token comes from the theme rather than from the author, so the question closes on an answer instead of on age. **A hypothesis about the class**: a `done` task may carry an unanswered section 1 open question and nothing looks. Of this cycle's 25 records it is the only silent one - [T-207](../tasks/T-207-two-more-mark-collisions-the-twelve-slide-look-found.md) leaves a question open for the owner and *says so* - so the remedy is a closing-checklist line asking whether every open question is answered, struck, or handed somewhere with a name, not a checker | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |
| `PR-128` | project method | **Low** | **Three closed records state the same section twice, and the mechanism is exact rather than careless.** Each carries **Child fix tasks raised** with `- none` beneath it, then a *What this task found that it was not looking for* block, then **Child fix tasks raised** and `- none` again. `tasks/_task-template.md` declares the label once. **Every task carrying the ad-hoc block duplicates the template's label and no other task does** - 3 of 3 - so the block was inserted ahead of the template's line and the template's line re-added after it rather than moved | [T-213](../tasks/T-213-build-the-presenter-build-and-the-marker-that-keeps-it-unshippable.md) lines 200 and 209, and [T-217](../tasks/T-217-notes-attach-by-position-and-position-is-not-identity.md) lines 173 and 184, are this cycle's; [T-214](../tasks/T-214-ds-142s-checker-is-an-allow-list-of-one-class-name.md) is the third and is outside it. **3 of 225 task files, and all three closed within one day.** `python tools/tasks/lint.py` exits 0 on the same tree: `taskmd check` counts front-matter values, closed records, table rows and section references, and a repeated section label is in none of the four | The three records | Delete the second block in each. **The class is one comparison wide**: a task's sections are already walked to be found, so refusing a label seen twice in one file is a test rather than a feature - but it is upstream's shape and belongs in [`taskmd-audit-method.md`](upstream/taskmd-audit-method.md)'s account rather than in a local checker, on [T-161](../tasks/T-161-decide-whether-to-adopt-the-wide-row-gate-now-that-upstream-ships-one.md)'s precedent: adopt theirs rather than build one, and it caught the wide row five days later with no code here | `xs` | [T-253](../tasks/T-253-the-low-findings-batch.md) - batched, per the method's section 4 | open |

---

## 4. Accepted without action

A `Low` row may close here instead of in a task, with a reason and a date. A `High` or `Medium` row
may not.

| # | Reason | Date |
| :-- | :--- | :--- |
| | *none* | |

---

## 5. Phase 2 — predicted against measured

Written after the remedies exist, never at ranking time — the taskmd skill's `pre-release-audit.md`
owns that rule.

| # | Predicted sev / effort | Measured | What the difference was |
| :-- | :--- | :--- | :--- |
| `PR-06` | Med, `s` | Med, `s` — the severity and the cost both held | **The remedy's own prediction was refused, which is the row this table exists for.** It read *the counts may reconcile once membership is derivable, in which case the defect was only ever that it was not*. They did not. Deriving the partition located one half exactly — `shell/README.md`, 6,380 bytes, counted in both cycle 1 and cycle 16 — and showed the other half unlocatable, so the plan's thirty-seven rows summing to 491 covered at most **488 distinct** files. **Four tracked files were in no cycle, not one**: a double count and two entries the document cannot name ran in opposite directions and partly cancelled, which is why the aggregate looked one short. The finding understated itself |
| | | *the rest not run* | |
